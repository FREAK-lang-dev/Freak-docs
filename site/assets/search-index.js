window.SEARCH_INDEX = [{"p":"index","t":"FREAK V3","a":"freak-v3","h":"FREAK V3","x":"> The complete, verified reference for the language the shipping FREAK V3 compiler actually accepts. FREAK is a programming language written entirely by AI. Its compiler is self-hosted: the compiler that builds FREAK programs is itself written in FREAK. This site documents **V3 \"Maverick\"** \u2014 the shipping, self-hosted generation that lives in `src/compiler/v3/`."},{"p":"index","t":"FREAK V3","a":"why-this-site-exists","h":"Why this site exists","x":"The project ships a specification, `freak-full-bible.md`, that describes the *complete* language: variants, doctrines, closures, generics, lifetimes, squadron concurrency, probability types, and more. That document is explicit that it describes the destination, not the present. The V3 compiler implements a **much smaller** language than the bible. Not a slightly smaller one \u2014 a fundamentally smaller one. Roughly a fifth of the bible's surface compiles today. Every page here documents only what V3 accepts, and proves it: each FREAK snippet on this site is a real file under `examples/`, compiled by a real V3 binary, and \u2014 where it produces output \u2014 executed, with the output captured verbatim."},{"p":"index","t":"FREAK V3","a":"why-this-site-exists","h":"Why this site exists","x":"Nothing here is aspirational. For the feature-by-feature comparison against the specification, see [Bible vs V3](conformance.html). For what to write instead of the features that are missing, see [Not in V3](not-in-v3.html)."},{"p":"index","t":"FREAK V3","a":"hello-freak","h":"Hello, FREAK","x":"-- The smallest FREAK program. -- `say` is always available; no import is needed. task main() -> void { say \"Hello from FREAK\" } freak run hello.fk"},{"p":"index","t":"FREAK V3","a":"the-language-in-one-screen","h":"The language in one screen","x":"This is close to the whole language. If a construct is not on this list, check [Not in V3](not-in-v3.html) before reaching for it. -- comments start with a double dash pilot x = 42 -- binding, type inferred pilot y: word = \"text\" -- explicit type annotation fixed pilot LIMIT: int = 100 -- immutable binding pilot mut counter: int = 0 -- reassignable binding task add(a: int, b: int) -> int { give back a + b -- explicit return, always } shape Point { x: int y: int } -- record type impl Point { task make(v: int) -> Point { give back Point { x: v, y: v } } task sum(self) -> int { give back self.x + self.y } } task main() -> void { pilot p: Point = Point::make(3) say \"sum={p.sum()}\" --"},{"p":"index","t":"FREAK V3","a":"the-language-in-one-screen","h":"The language in one screen","x":"(interpolates paths, not calls) if p.x > 2 { say \"big\" } else { say \"small\" } when p.x { 3 -> say \"three\" _ -> say \"other\" } repeat 3 times { say \"again\" } repeat until counter == 3 { counter += 1 } training arc until counter > 99 max 10 sessions { counter += 1 } pilot names: List<word> = [\"alpha\", \"bravo\"] say names[0] say word_from_int(names.length()) say \"\\x1b[1;32mok\\x1b[0m\" -- ANSI colour, no library needed }"},{"p":"index","t":"FREAK V3","a":"what-makes-v3-unusual","h":"What makes V3 unusual","x":"**One flat namespace.** There are no modules at runtime. `use` lines are stripped before parsing and act only as a hint that tells the build to link an optional standard-library file. Every task and every top-level binding in your file and in the standard library shares one global scope. **Keywords are case-insensitive.** `pilot`, `Pilot` and `PILOT` are the same token. This makes a surprising number of natural names illegal \u2014 `Pilot`, `Result`, `Max`, `Route` and `Check` cannot name your shapes or bindings. See [reserved words](language-basics.html#reserved-words-are-case-insensitive). **Type annotations are one identifier, plus `List<T>`.** `pilot x: int` and `pilot x: List<int>` both"},{"p":"index","t":"FREAK V3","a":"what-makes-v3-unusual","h":"What makes V3 unusual","x":"work; `List` is the only generic form, and it does not nest. **Four scalars, shapes, and two builtin types.** `int`, `num`, `word`, `bool`, `void`, the `shape` records you declare, plus `List<T>` and `ByteBuffer`. No `maybe<T>`, no `result<T,E>`, no `Map<K,V>`, no tuples. Alongside `List<T>` an older untyped array handle survives, addressed by an `int`. **No implicit returns.** Every value-returning path needs `give back`."},{"p":"index","t":"FREAK V3","a":"two-backends","h":"Two backends","x":"V3 emits either LLVM IR or portable C. Backend Flag Status LLVM IR `--llvm` (default) Full surface, including shapes, methods and `std::ui` C `--c` Scalars, control flow, words, arrays and native calls. Shape storage is *not* a claimed executable path If your program uses shapes, use the default LLVM backend. Every example on this site was verified through it."},{"p":"index","t":"FREAK V3","a":"where-to-go-next","h":"Where to go next","x":"[Getting started](getting-started.html) \u2014 build the compiler and run a program [Bindings & types](language-basics.html) \u2014 the value model [Shapes & impl](shapes.html) \u2014 the only user-defined type [Lists & arrays](arrays.html) \u2014 the two collection surfaces [Coloured output](console.html) \u2014 ANSI escapes and terminal handling [Standard library](stdlib.html) \u2014 the complete callable surface [Bible vs V3](conformance.html) \u2014 the specification comparison"},{"p":"getting-started","t":"Getting started","a":"getting-started","h":"Getting started","x":"> Build a real V3 compiler from source, then compile and run your first program."},{"p":"getting-started","t":"Getting started","a":"what-you-need","h":"What you need","x":"**Clang** \u2014 V3 emits LLVM IR or C and hands it to Clang to produce a binary. **Bash** \u2014 for the reconstruction script. Git for Windows supplies a suitable shell. **Python 3** \u2014 only for the test harnesses and this site's tooling. Check Clang is reachable: clang --version"},{"p":"getting-started","t":"Getting started","a":"build-the-compiler-from-source","h":"Build the compiler from source","x":"V3 is self-hosted, so building it is a bootstrap. The repository carries a reviewed, version-controlled C seed at `build/freakc_v3.fk.c` precisely so you never need a pre-existing FREAK binary. The order of the eight compiler sources is fixed \u2014 they are concatenated into one translation unit before compiling: globals.fk helpers.fk lexer.fk parser.fk checker.fk emit_c.fk emit_llvm.fk main.fk Build the seed, then regenerate the compiler from itself: repo=\"$PWD\" work=\"$(mktemp -d)\" cat src/compiler/v3/globals.fk src/compiler/v3/helpers.fk \\ src/compiler/v3/lexer.fk src/compiler/v3/parser.fk \\ src/compiler/v3/checker.fk src/compiler/v3/emit_c.fk \\ src/compiler/v3/emit_llvm.fk"},{"p":"getting-started","t":"Getting started","a":"build-the-compiler-from-source","h":"Build the compiler from source","x":"src/compiler/v3/main.fk > \"$work/freakc_v3.fk\" clang -o \"$work/freakc-seed\" build/freakc_v3.fk.c freakc/runtime/freak_runtime.c \\ -I freakc/runtime -O2 -w -D_CRT_SECURE_NO_WARNINGS -lws2_32 \"$work/freakc-seed\" \"$work/freakc_v3.fk\" --c && mv \"$work/freakc_v3.fk.c\" \"$work/stage1.c\" clang -o \"$work/freakc-stage1\" \"$work/stage1.c\" freakc/runtime/freak_runtime.c \\ -I freakc/runtime -O2 -w -lws2_32 \"$work/freakc-stage1\" \"$work/freakc_v3.fk\" --c && mv \"$work/freakc_v3.fk.c\" \"$work/stage2.c\" clang -o \"$work/freakc-stage2\" \"$work/stage2.c\" freakc/runtime/freak_runtime.c \\ -I freakc/runtime -O2 -w -lws2_32 \"$work/freakc-stage2\" \"$work/freakc_v3.fk\" --c && mv \"$work/freakc_v3.fk.c\" \"$work/stage3.c\""},{"p":"getting-started","t":"Getting started","a":"build-the-compiler-from-source","h":"Build the compiler from source","x":"cmp \"$work/stage2.c\" \"$work/stage3.c\" && echo \"fixed point reached\" The last line is the point of the whole exercise. `stage2.c` and `stage3.c` must be byte-identical: the compiler built from generation 2 has to reproduce its own generated C exactly. That equality is the self-host fixed-point invariant. Native binary bytes are *not* expected to match across operating systems or Clang versions \u2014 only the generated C is. On non-Windows hosts drop `-lws2_32`; use `-lm` on Linux and no extra link flag on macOS. The cross-platform automated equivalent is: python -u tests/v3_fixed_point.py"},{"p":"getting-started","t":"Getting started","a":"build-the-freak-cli","h":"Build the `freak` CLI","x":"The standalone `freakc` you just built takes a file and a backend flag. The public `freak` command is a second aggregate: the same seven compiler files plus the CLI, with `src/cli/main.fk` as the entry point instead of `v3/main.fk`. cat std/version.fk \\ src/compiler/v3/globals.fk src/compiler/v3/helpers.fk \\ src/compiler/v3/lexer.fk src/compiler/v3/parser.fk \\ src/compiler/v3/checker.fk src/compiler/v3/emit_c.fk \\ src/compiler/v3/emit_llvm.fk \\ src/cli/version.fk src/cli/toml.fk src/cli/lockfile.fk \\ src/cli/build.fk src/cli/run.fk src/cli/hangar.fk \\ src/cli/doctor.fk src/cli/audit.fk src/cli/main.fk > \"$work/freakc_cli.fk\" \"$work/freakc-stage2\" \"$work/freakc_cli.fk\" --c mkdir -p"},{"p":"getting-started","t":"Getting started","a":"build-the-freak-cli","h":"Build the `freak` CLI","x":"\"$work/freak-home/bin\" clang -o \"$work/freak-home/bin/freak\" \"$work/freakc_cli.fk.c\" \\ freakc/runtime/freak_runtime.c -I freakc/runtime -O2 -w -lws2_32"},{"p":"getting-started","t":"Getting started","a":"install-the-payload","h":"Install the payload","x":"The compiler is not self-sufficient: it needs the C runtime and the standard library beside it. The canonical inventory is `packaging/distribution-files.manifest`, a list of `source|destination` pairs. while IFS='|' read -r src dst; do case \"$src\" in ''|\\#*) continue ;; esac mkdir -p \"$work/freak-home/$(dirname \"$dst\")\" cp \"$src\" \"$work/freak-home/$dst\" done < packaging/distribution-files.manifest The layout that results: freak-home/ bin/freak the compiler CLI runtime/ freak_runtime.c, freak_llvm_runtime.c, headers std/ math.fk, string.fk, convert.fk, algorithm.fk, ... Both `runtime/freak_abi` and `std/freak_abi` must contain the marker the compiler expects (`freak-v3-abi-1`), or every"},{"p":"getting-started","t":"Getting started","a":"install-the-payload","h":"Install the payload","x":"build stops with an ABI mismatch. Point `FREAK_HOME` at that directory, or leave it unset and let the CLI find the payload next to the executable: export FREAK_HOME=\"$work/freak-home\" freak --version"},{"p":"getting-started","t":"Getting started","a":"your-first-program","h":"Your first program","x":"-- The smallest FREAK program. -- `say` is always available; no import is needed. task main() -> void { say \"Hello from FREAK\" } freak run hello.fk `freak build` leaves a native binary beside the source, named after it: freak build hello.fk # produces hello.exe (or hello) ./hello"},{"p":"getting-started","t":"Getting started","a":"a-program-with-a-bit-more-to-it","h":"A program with a bit more to it","x":"-- FizzBuzz, written the way V3 wants it: a counted loop, a mutable -- counter, and `when` over a computed tag. task tag(n: int) -> word { if n % 15 == 0 { give back \"FizzBuzz\" } if n % 3 == 0 { give back \"Fizz\" } if n % 5 == 0 { give back \"Buzz\" } give back word_from_int(n) } task main() -> void { pilot mut n: int = 1 repeat 15 times { say tag(n) n += 1 } }"},{"p":"getting-started","t":"Getting started","a":"check-your-setup","h":"Check your setup","x":"`freak doctor` validates far more than `clang --version` \u2014 it checks the C headers, native link and execution, the optional LLD linker, the complete runtime and standard-library payload, and a full FREAK compile-link-execute probe. freak doctor freak doctor --fix freak doctor --json"},{"p":"getting-started","t":"Getting started","a":"common-first-errors","h":"Common first errors","x":"Message Cause `unexpected 'use' \u2014 this token cannot start an expression` You ran `freak check`, which does not strip `use` lines or load the standard library. Use `freak build` or `freak run`. `expected an identifier for shape name, found 'Pilot'` Keywords are case-insensitive; `Pilot` is the `pilot` keyword. Rename. `unknown callable 'string_reverse'` Same cause \u2014 `freak check` compiles your file alone, without `std/`. `ABI MISMATCH` `runtime/freak_abi` or `std/freak_abi` does not match the compiler's expected marker. Reinstall the payload. `expected '=', found '<'` Generic type syntax. V3 has none \u2014 see [Not in V3](not-in-v3.html)."},{"p":"cli","t":"Compiler CLI","a":"compiler-cli","h":"Compiler CLI","x":"> Every subcommand and flag the shipping V3 `freak` binary actually recognises. freak <command> [file.fk] [options]"},{"p":"cli","t":"Compiler CLI","a":"commands","h":"Commands","x":"Command What it does `freak build <file.fk>` Compile to a native binary beside the source `freak run <file.fk>` Build, then execute `freak check <file.fk>` Lex, parse and type-check only \u2014 **no standard library** `freak transpile <file.fk>` Emit `.c` or `.ll` and stop `freak init` Scaffold a new project `freak doctor` Validate the toolchain and payload `freak upgrade` Download and stage a newer FREAK `freak hangar <cmd>` Package manager (also a standalone `hangar` binary) `freak version`, `freak --version`, `freak -V` Print the version `freak help`, `freak --help`, `freak -h` Print the help screen `freak flex` Print the banner `freak test` Source-checkout shim around `python"},{"p":"cli","t":"Compiler CLI","a":"commands","h":"Commands","x":"tests/suite/run_tests.py` `freak audit-science` List every `for science,` call site `freak audit-trust` List every `trust me` block with its honor level `freak audit-miracles` List every `deus_ex_machina` block `freak foreshadow-audit` Report unpaid `foreshadow` bindings `freak audit-conformance` Verify the implementation status table against the code **`freak check` is not a fast `freak build`.** It compiles your file *alone*. It does not strip `use` lines and it does not link the standard library, so anything from `std/` \u2014 `string_reverse`, `std_gcd`, `ver_parse`, `json_parse` \u2014 reports `unknown callable`. Use `check` for pure-builtin code; use `build` or `run` for everything else. The"},{"p":"cli","t":"Compiler CLI","a":"commands","h":"Commands","x":"audit subcommands shell out to `python -m freakc`. They need the Python bootstrap package and the repository's audit inputs; the native V3 binary does not contain that auditor. The same is true of `freak test`."},{"p":"cli","t":"Compiler CLI","a":"options","h":"Options","x":"Flag Effect `--llvm` LLVM IR backend. **Default.** Full language surface `--c` Portable C backend. No claimed executable shape storage `--opt=N` Optimisation level, `0`\u2013`3`. Default `2` `--target=TRIPLE` Cross-compile target triple `--strict-borrow` Enable the opt-in Phase-1 ownership checker Flags may appear in any order after the file: freak run game.fk --c --opt=3 freak build game.fk --target=x86_64-linux-gnu freak build game.fk --strict-borrow `-o output_path` does not exist. The output name is always the source basename plus `.exe` (Windows) or no extension elsewhere. `--voice=`, `--clearance=` and `--build-mode=` are also V4-only."},{"p":"cli","t":"Compiler CLI","a":"--strict-borrow","h":"`--strict-borrow`","x":"Without this flag V3 does no ownership checking at all. With it, a Phase-1 checker runs after type checking and enforces three rules: **Immutable bindings.** `pilot x = ...` cannot be reassigned; `pilot mut x = ...` can. Reassigning without `mut` reports *\"This binding was sworn to silence.\"* **Single-owner moves** for `word`, arrays and user shapes. Use after move reports *\"Shirogane. You gave this away.\"* **Copy primitives.** `int`, `num` and `bool` are copied on assignment; both bindings stay valid. Nominal member validation \u2014 checking that a field or method you name actually exists on the shape \u2014 always runs, flag or not."},{"p":"cli","t":"Compiler CLI","a":"backends-compared","h":"Backends compared","x":"LLVM (`--llvm`) C (`--c`) Scalars, control flow, words yes yes Arrays and native calls yes yes `shape` construction, fields, methods yes **not a claimed executable path** Dotted `{shape.field}` interpolation yes not claimed `std::ui` windows Windows only, Win32/GDI no `std::http` yes not linked Debug info LineTablesOnly DWARF via Clang The rule of thumb: if your program declares a `shape`, build it with the default LLVM backend."},{"p":"cli","t":"Compiler CLI","a":"hangar","h":"Hangar","x":"`hangar` is the package manager, reachable as `freak hangar <cmd>` or as a standalone binary. Subcommand Status `hangar init [name]` works `hangar add <name> [constraint]` works `hangar add <name> <repo> [version]` works `hangar remove <name>` works `hangar install` works `hangar install freak` works `hangar update [package]` works `hangar outdated` works `hangar version [patch\\|minor\\|major]` works `hangar audit [--fix]` works `hangar login`, `hangar publish [--dry-run]` works `hangar search <query>` not in this build A `hangar.toml` as the tool actually writes it: [project] name = \"demo\" version = \"0.1.0\" [dependencies] muvluv = \"^2.0\" Dependency constraints are matched by"},{"p":"cli","t":"Compiler CLI","a":"hangar","h":"Hangar","x":"`std/version.fk`, whose semver comparison and `^`-constraint logic you can call directly \u2014 see [Standard library](stdlib.html#stdversion). **Installing a package does not make it usable.** `freak build` never reads `hangar_modules/` or `[dependencies]`, and `use pkg::{\u2026}` is stripped to a comment before parsing. Dependencies have to be concatenated into your source by hand. See [Hangar & packages](hangar.html) for the full picture and the working pattern."},{"p":"cli","t":"Compiler CLI","a":"environment","h":"Environment","x":"Variable Meaning `FREAK_HOME` Authoritative payload root. Overrides checkout-relative and executable-adjacent discovery Discovery order for `runtime/` and `std/`: `$FREAK_HOME` the repository root, if the executable sits inside a checkout the directory beside the executable the default installed home Both `runtime/freak_abi` and `std/freak_abi` must carry the marker the compiler expects, or the build stops before parsing."},{"p":"cli","t":"Compiler CLI","a":"generated-files","h":"Generated files","x":"`freak build hello.fk` leaves behind: hello.fk your source hello.fk.ll LLVM IR (or hello.fk.c with --c) hello.exe the binary hello.pdb debug info, Windows `freak transpile` produces only the `.ll` or `.c`."},{"p":"hangar","t":"Hangar & packages","a":"hangar-packages","h":"Hangar & packages","x":"> Hangar manages dependencies. V3 cannot consume them. This page explains the gap and what to do instead. Hangar is FREAK's package manager, shipping as `freak hangar <cmd>` and as a standalone `hangar` binary. It initialises projects, edits manifests, resolves semver constraints, downloads packages and publishes them. **An installed package is not visible to the compiler.** `freak build` never reads `hangar_modules/` and never reads `[dependencies]`. A `use pkg::{\u2026}` line is rewritten to a comment before parsing, so nothing is imported and the first call into the package fails with `unknown callable`. Hangar today is a manifest and download tool. The link step that would make a dependency"},{"p":"hangar","t":"Hangar & packages","a":"hangar-packages","h":"Hangar & packages","x":"usable does not exist in V3. Verified against the shipping compiler: $ freak hangar init demo INITIALIZED demo hangar.toml src/main.fk hangar_modules/ $ cat hangar_modules/greeter/greeter.fk task greeter_hello(name: word) -> word { give back \"hello, \" + name } $ cat src/main.fk use greeter::{greeter_hello} task main() -> void { say greeter_hello(\"Takeru\") } $ freak build src/main.fk type error: unknown callable 'greeter_hello' (line 4) X BUILD FAILED"},{"p":"hangar","t":"Hangar & packages","a":"what-does-get-linked","h":"What does get linked","x":"`cli_load_std` in `src/cli/build.fk` is the whole of the build's source resolution. It concatenates a fixed list ahead of your file: Source When `std/math.fk`, `std/string.fk`, `std/convert.fk`, `std/algorithm.fk`, `std/json.fk`, `std/version.fk` Always `std/runtime.fk`, `std/http.fk` Always, LLVM backend only `std/math3d.fk` The source text contains `use std::math3d` `std/zip.fk` The source text contains `use std::zip` `std/ui/window.fk` The source text contains `use std::ui` `packages/cockpit/src/*.fk` The source text contains `use cockpit` Those four `use` forms are **textual triggers**, not imports \u2014 the build greps your source for the string and links a hard-coded path. Everything else"},{"p":"hangar","t":"Hangar & packages","a":"what-does-get-linked","h":"What does get linked","x":"already in that list is in scope whether you write `use` or not. The `cockpit` trigger resolves `packages/cockpit/src`, a **repository-relative path**. It only works when you build from inside a Freak-lang checkout, not from an installed toolchain \u2014 and it is the only package-shaped thing the build can link at all."},{"p":"hangar","t":"Hangar & packages","a":"commands","h":"Commands","x":"All of these work. What they cannot do is make a package compile. Command Effect `hangar init [name]` Scaffold `hangar.toml`, `src/main.fk`, `hangar_modules/` `hangar add <name> [constraint]` Add a dependency to the manifest `hangar add <name> <repo> [version]` Add a dependency from a git repo `hangar remove <name>` Remove it `hangar install` Download all dependencies into `hangar_modules/` `hangar install freak` Install or update the FREAK toolchain itself `hangar update [package]` Re-resolve and update `hangar.lock` `hangar outdated` Report packages behind their constraint `hangar version` Print the project version `hangar version patch\\|minor\\|major` Bump it in the manifest `hangar"},{"p":"hangar","t":"Hangar & packages","a":"commands","h":"Commands","x":"audit` / `hangar audit --fix` Verify or repair package integrity `hangar login` Authenticate with the registry `hangar publish [--dry-run]` Publish to the registry `hangar search` from the specification does not exist in this build."},{"p":"hangar","t":"Hangar & packages","a":"hangartoml","h":"hangar.toml","x":"What `hangar init` actually writes: [project] name = \"demo\" version = \"0.1.0\" And after `hangar add muvluv \"^2.0\"`: [project] name = \"demo\" version = \"0.1.0\" [dependencies] muvluv = \"^2.0\" The specification's \u00a76.2 shows a different, larger manifest \u2014 `[unit]` rather than `[project]`, plus `author`, `entry`, `edition`, a `[build]` table with `mode`/`target`/`voice`, and `[profile.*]` sections. None of that is written or read by the shipping tool. Only `[project]` and `[dependencies]` matter. Version constraints are resolved by `std/version.fk`, which is real, working semver: `ver_satisfies(\"1.4.2\", \"^1.4\")` is `true`. You can call that machinery directly \u2014 see [Standard"},{"p":"hangar","t":"Hangar & packages","a":"hangartoml","h":"hangar.toml","x":"library](stdlib.html#stdversion). `hangar.lock` records resolved versions."},{"p":"hangar","t":"Hangar & packages","a":"reusing-code-for-real","h":"Reusing code, for real","x":"Because the compiler takes exactly one file and everything lands in one flat global namespace, a \"library\" is just source you concatenate ahead of your program: cat lib/text.fk src/app.fk > build/combined.fk freak build build/combined.fk Verified working, including against a file downloaded into `hangar_modules/`: cat hangar_modules/greeter/greeter.fk src/app.fk > combined.fk freak build combined.fk # BUILD SUCCESSFUL Order matters only for `shape` declarations: a shape must be registered before a constructor for it is parsed, so put library shapes first. Tasks can be in any order \u2014 every task is indexed before any body is checked. This is exactly how the compiler builds itself. The eight"},{"p":"hangar","t":"Hangar & packages","a":"reusing-code-for-real","h":"Reusing code, for real","x":"V3 sources and the CLI sources are `cat`-ed into one translation unit; the file split is a maintenance convention, not a module system. See [Getting started](getting-started.html#build-the-compiler-from-source)."},{"p":"hangar","t":"Hangar & packages","a":"the-concatenated-pattern","h":"The concatenated pattern","x":"-- Reusing code across files in V3. -- -- The compiler takes exactly one source file, and Hangar packages are -- never linked into it, so a \"library\" is just source you concatenate -- ahead of your program: -- -- cat lib/text.fk src/app.fk > build/combined.fk -- freak build build/combined.fk -- -- This file is what that produces. Everything lands in one flat global -- namespace, so a library must prefix its names to avoid collisions -- -- with your code, and with the std/ tasks the build already links. -- =================================================================== -- lib/text.fk -- the \"package\" -- =================================================================== shape TextStats {"},{"p":"hangar","t":"Hangar & packages","a":"the-concatenated-pattern","h":"The concatenated pattern","x":"words: int longest: word } task text_is_blank(c: word) -> bool { give back c == \" \" or c == \"\\n\" or c == \"\\t\" or c == \"\\r\" } task text_count_words(source: word) -> int { pilot mut total: int = 0 pilot mut in_word: bool = false pilot mut i: int = 0 repeat source.length() times { if text_is_blank(source.char_at(i)) { in_word = false } else { if not in_word { total += 1 } in_word = true } i += 1 } give back total } task text_longest_word(source: word) -> word { pilot mut best: word = \"\" pilot mut current: word = \"\" pilot mut i: int = 0 repeat source.length() times { pilot c: word = source.char_at(i) if text_is_blank(c) { if current.length() > best.length() { best = current } current = \"\" }"},{"p":"hangar","t":"Hangar & packages","a":"the-concatenated-pattern","h":"The concatenated pattern","x":"else { current = current + c } i += 1 } if current.length() > best.length() { best = current } give back best } task text_analyse(source: word) -> TextStats { give back TextStats { words: text_count_words(source), longest: text_longest_word(source) } } -- =================================================================== -- src/app.fk -- your program -- =================================================================== task main() -> void { pilot report: TextStats = text_analyse(\"the beta will not negotiate with humanity\") say word_from_int(report.words) say report.longest -- `use` lines are stripped before parsing, so leaving one in a -- concatenated build is harmless -- and equally, it"},{"p":"hangar","t":"Hangar & packages","a":"the-concatenated-pattern","h":"The concatenated pattern","x":"imports nothing. }"},{"p":"hangar","t":"Hangar & packages","a":"conventions-that-make-it-survivable","h":"Conventions that make it survivable","x":"One flat namespace across your code, every linked `std/` file, and every library you concatenate. Collisions are silent until they are a redefinition error, so: **Prefix everything a library exports.** `text_count_words`, not `count_words`. The standard library already does this \u2014 `string_*`, `std_*`, `ver_*`, `json_*`, `array_*`. **Watch for `std/` collisions.** Defining your own `array_find` or `string_trim` clashes with a task the build already linked. **Remember reserved words are case-insensitive.** A library cannot export anything named `Result`, `Max`, `Route` or `Check` in any casing. See [reserved words](language-basics.html#reserved-words-are-case-insensitive). **Keep a build"},{"p":"hangar","t":"Hangar & packages","a":"conventions-that-make-it-survivable","h":"Conventions that make it survivable","x":"script.** A one-line `cat` in a `.sh`/`.bat` is your build system. Regenerate the combined file every time."},{"p":"hangar","t":"Hangar & packages","a":"a-makefile-shaped-example","h":"A Makefile-shaped example","x":"#!/bin/sh set -e mkdir -p build cat lib/text.fk lib/report.fk src/app.fk > build/combined.fk freak build build/combined.fk mv build/combined.exe build/app.exe Line numbers in diagnostics refer to the combined file. The compiler maintains a source map for the std prefix it adds itself, but it cannot know about your `cat`, so keep the concatenation order stable if you want stable offsets."},{"p":"hangar","t":"Hangar & packages","a":"what-ships-when-this-is-fixed","h":"What ships when this is fixed","x":"Real imports need the module system from \u00a76 and \u00a717 of the specification \u2014 `launch` visibility, `use module::{names}`, `launch(package)`, glob imports, namespace collision rules. All of that is V4 work; `launch` does not currently parse at all. Until then, Hangar is useful for versioning and distributing source, and `cat` is the linker."},{"p":"tutorial-first-program","t":"Tutorial 1: Your first program","a":"tutorial-1-your-first-program","h":"Tutorial 1: Your first program","x":"> Build and run a FREAK program from nothing, then grow it into something with a shape and a method. About 15 minutes. This assumes you have a working `freak` on your PATH. If not, do [Getting started](getting-started.html) first \u2014 you need to build the compiler from source, because V3 is self-hosted."},{"p":"tutorial-first-program","t":"Tutorial 1: Your first program","a":"step-1-say-something","h":"Step 1 \u2014 Say something","x":"Create `greet.fk`: task main() -> void { say \"Valkyries, launch\" } Run it: freak run greet.fk Two things are already worth noticing. `say` needs no import \u2014 it is a compiler builtin, always in scope. And `main` is optional: statements written at the top level of a file run in order, so this would work too: say \"Valkyries, launch\" Use `main` anyway. It gives you somewhere to put local bindings."},{"p":"tutorial-first-program","t":"Tutorial 1: Your first program","a":"step-2-bindings","h":"Step 2 \u2014 Bindings","x":"`pilot` introduces a binding. Add a couple: task main() -> void { pilot callsign: word = \"Valkyrie-1\" pilot sorties: int = 12 say callsign say word_from_int(sorties) } The annotation is optional \u2014 `pilot sorties = 12` infers `int`. Note `word_from_int`: `say` accepts `int` directly, but the moment you want to glue a number onto a word you must convert it, because `+` will not mix types. There are five types worth knowing at this stage: `int`, `num` (float), `word` (string), `bool`, and `void`. Full list in [Bindings & types](language-basics.html). **Do not name anything `Pilot`.** V3 matches keywords case-insensitively, so `Pilot`, `PILOT` and `pilot` are the same token, and none can be an"},{"p":"tutorial-first-program","t":"Tutorial 1: Your first program","a":"step-2-bindings","h":"Step 2 \u2014 Bindings","x":"identifier. The same trap catches `Result`, `Max`, `Check`, `Route`, `Move`, `Copy`, `Some`, `Ok`, `Err` and `Done`. Pick a synonym \u2014 this tutorial uses `Aviator`."},{"p":"tutorial-first-program","t":"Tutorial 1: Your first program","a":"step-3-interpolation","h":"Step 3 \u2014 Interpolation","x":"Instead of converting and concatenating, put the value in the string: task main() -> void { pilot callsign: word = \"Valkyrie-1\" pilot sorties: int = 12 say \"{callsign} has flown {sorties} sorties\" } `{path}` substitutes a binding. A *path* is an identifier plus optional `.field` hops \u2014 `callsign`, `unit.name`, `self.frame.thrust`. Interpolation substitutes paths, **never calls**. `\"{rank()}\"` prints those characters literally rather than calling anything. Compute into a binding first \u2014 this is the single most common surprise in V3."},{"p":"tutorial-first-program","t":"Tutorial 1: Your first program","a":"step-4-a-task-of-your-own","h":"Step 4 \u2014 A task of your own","x":"`task` declares a function; `give back` returns from it. task rank_for(sorties: int) -> word { if sorties >= 50 { give back \"veteran\" } if sorties >= 10 { give back \"regular\" } give back \"rookie\" } task main() -> void { say rank_for(12) } There is **no implicit return**. A block-bodied task that returns a value needs `give back` on every path \u2014 a bare expression at the end is evaluated and discarded. The arrow shorthand `=> expr` from the specification does not parse in V3. Order does not matter: every task is indexed before any body is checked, so `main` can call a task declared below it."},{"p":"tutorial-first-program","t":"Tutorial 1: Your first program","a":"step-5-a-shape","h":"Step 5 \u2014 A shape","x":"`shape` is V3's record type, and the only user-defined type there is. shape Aviator { name: word sorties: int } task main() -> void { pilot takeru: Aviator = Aviator { name: \"Shirogane Takeru\", sorties: 12 } say takeru.name say word_from_int(takeru.sorties) } Every field must be supplied by name at construction. Fields are readable and assignable, and they chain: `unit.frame.thrust`."},{"p":"tutorial-first-program","t":"Tutorial 1: Your first program","a":"step-6-methods","h":"Step 6 \u2014 Methods","x":"`impl` attaches tasks to a shape. Whether the first parameter is `self` decides how you call it. impl Aviator { -- No self: an associated method, called Aviator::recruit(...) task recruit(name: word) -> Aviator { give back Aviator { name: name, sorties: 0 } } -- With self: an instance method, called value.rank() task rank(self) -> word { if self.sorties >= 50 { give back \"veteran\" } give back \"rookie\" } } Write `self` bare, never `self: Aviator` \u2014 the compiler fills in the owning shape."},{"p":"tutorial-first-program","t":"Tutorial 1: Your first program","a":"step-7-loops","h":"Step 7 \u2014 Loops","x":"Three forms, no `for each`: repeat 12 times { takeru.sorties += 1 } -- counted repeat until done { work() } -- condition, tested first training arc until ready max 8 sessions { } -- bounded, cannot spin forever To reassign a binding you must declare it `pilot mut`. Without `--strict-borrow` the compiler will not enforce that, but write it anyway \u2014 it documents intent and the checker will demand it once you turn the flag on."},{"p":"tutorial-first-program","t":"Tutorial 1: Your first program","a":"the-finished-program","h":"The finished program","x":"Everything above, assembled: -- Tutorial 1, finished program. -- -- A greeter that takes a name, decides how formal to be, and reports. -- Everything here is core V3: bindings, a shape, a method, a task, -- interpolation, a conditional and a counted loop. shape Aviator { name: word sorties: int } impl Aviator { -- An associated method: called as Aviator::recruit(...). task recruit(name: word) -> Aviator { give back Aviator { name: name, sorties: 0 } } -- An instance method: `self` is the receiver. task rank(self) -> word { if self.sorties >= 50 { give back \"veteran\" } if self.sorties >= 10 { give back \"regular\" } give back \"rookie\" } task greeting(self) -> word { -- Interpolation"},{"p":"tutorial-first-program","t":"Tutorial 1: Your first program","a":"the-finished-program","h":"The finished program","x":"substitutes PATHS only, never calls. Writing -- \"{self.rank()}\" would print those characters literally, so the -- call result goes into a binding first. pilot rank: word = self.rank() give back \"{self.name} - {self.sorties} sorties, rank {rank}\" } } task main() -> void { pilot mut takeru: Aviator = Aviator::recruit(\"Shirogane Takeru\") -- Fly a few missions. repeat 12 times { takeru.sorties += 1 } say takeru.greeting() pilot rank: word = takeru.rank() say \"logged as: {rank}\" if takeru.sorties > 10 { say \"cleared for the next operation\" } else { say \"needs more flight hours\" } }"},{"p":"tutorial-first-program","t":"Tutorial 1: Your first program","a":"where-to-go-next","h":"Where to go next","x":"[Tutorial 2](tutorial-cli-tool.html) \u2014 a real command-line tool with files and colour [Tutorial 3](tutorial-data.html) \u2014 lists, and modelling data properly [Control flow](control-flow.html) \u2014 the loop and branch forms in detail [Not in V3](not-in-v3.html) \u2014 what to write instead of the features the specification promises"},{"p":"tutorial-cli-tool","t":"Tutorial 2: A command-line tool","a":"tutorial-2-a-command-line-tool","h":"Tutorial 2: A command-line tool","x":"> Read arguments, read a file, count what is in it, and print a coloured report that respects `NO_COLOR`. About 25 minutes. By the end you will have a small `wc`-style tool. Along the way: the argument API, file I/O without `result<T,E>`, hand-rolled text scanning, ANSI colour, and exit codes. Start from [Tutorial 1](tutorial-first-program.html) if you have not written any FREAK yet."},{"p":"tutorial-cli-tool","t":"Tutorial 2: A command-line tool","a":"step-1-arguments","h":"Step 1 \u2014 Arguments","x":"There is no `process::args()` returning a list. V3 rejects it deliberately rather than handing you the runtime's raw `argv` as a fake handle. Use the indexed pair instead: task main() -> void { say word_from_int(process::args_count()) pilot mut i: int = 0 repeat process::args_count() times { say \"arg {i}: \" + process::arg(i) i += 1 } } Argument 0 is the executable path, so the first real argument is index 1. That loop is the standard V3 iteration idiom: a counted `repeat` plus a manual index. There is no `for each`."},{"p":"tutorial-cli-tool","t":"Tutorial 2: A command-line tool","a":"step-2-reading-a-file","h":"Step 2 \u2014 Reading a file","x":"`fs::*` are builtins. There is no `result<T,E>` anywhere in V3, so failures come back as sentinel values \u2014 an empty `word`, or `false`: task main() -> void { pilot path: word = \"notes.txt\" if not fs::exists(path) { say \"no such file: \" + path process::exit(1) } pilot text: word = fs::read(path) say word_from_int(text.length()) } `process::exit(code)` ends the program immediately. Check `fs::exists` before reading \u2014 an unreadable file and an empty file both come back as `\"\"`. `fs::delete` returns `true` when the file is gone, whether this call removed it or it was already absent. It only returns `false` when the path could not be unlinked."},{"p":"tutorial-cli-tool","t":"Tutorial 2: A command-line tool","a":"step-3-making-the-tool-self-contained","h":"Step 3 \u2014 Making the tool self-contained","x":"So it always has something to work on, write a sample when no argument is given: task sample_path() -> word { pilot path: word = \"sample.txt\" fs::write(path, \"the beta do not negotiate\\nhumanity answers with steel\\n\") give back path } task target_path() -> word { if process::args_count() > 1 { give back process::arg(1) } give back sample_path() }"},{"p":"tutorial-cli-tool","t":"Tutorial 2: A command-line tool","a":"step-4-counting","h":"Step 4 \u2014 Counting","x":"No regex, no `.split()` into a list. Walk the characters: task count_words(text: word) -> int { pilot mut total: int = 0 pilot mut in_word: bool = false pilot mut i: int = 0 repeat text.length() times { pilot c: word = text.char_at(i) pilot blank: bool = c == \" \" or c == \"\\n\" or c == \"\\t\" or c == \"\\r\" if blank { in_word = false } else { if not in_word { total += 1 } in_word = true } i += 1 } give back total } `.char_at(i)` returns a one-character `word` \u2014 there is no `char` type. Note `and` / `or` take `bool` only; there is no truthiness, so `if count` is an error where `if count > 0` is fine. Counting lines is the same shape of loop, with one adjustment so a trailing newline does not"},{"p":"tutorial-cli-tool","t":"Tutorial 2: A command-line tool","a":"step-4-counting","h":"Step 4 \u2014 Counting","x":"invent an extra line: task count_lines(text: word) -> int { if text.length() == 0 { give back 0 } pilot mut total: int = 1 pilot mut i: int = 0 repeat text.length() times { if text.char_at(i) == \"\\n\" { total += 1 } i += 1 } if text.char_at(text.length() - 1) == \"\\n\" { total -= 1 } give back total }"},{"p":"tutorial-cli-tool","t":"Tutorial 2: A command-line tool","a":"step-5-colour","h":"Step 5 \u2014 Colour","x":"There is no colour library. You build ANSI escape sequences as `word` values. `\\x1b` is the ESC byte: pilot C_RESET = \"\\x1b[0m\" pilot C_BOLD = \"\\x1b[1m\" pilot C_RED = \"\\x1b[1;31m\" pilot C_GREEN = \"\\x1b[32m\" Then concatenate: say C_RED + \"error\" + C_RESET + \": no such file\" Windows consoles are switched into ANSI mode automatically \u2014 the runtime calls `freak_enable_ansi()` at the start of every FREAK program, on both backends. You do not need to do anything platform-specific. The full code tables \u2014 attributes, 16 colours, backgrounds, 256-colour, truecolour, cursor control \u2014 are in [Coloured output](console.html)."},{"p":"tutorial-cli-tool","t":"Tutorial 2: A command-line tool","a":"step-6-respecting-no-color","h":"Step 6 \u2014 Respecting NO_COLOR","x":"Nothing does this for you. The trick the CLI uses is to make the constants `pilot mut` and blank them, so every call site keeps working unchanged: pilot mut C_RESET = \"\\x1b[0m\" pilot mut C_RED = \"\\x1b[1;31m\" task configure_output() -> void { if process::env(\"NO_COLOR\") != \"\" { C_RESET = \"\" C_RED = \"\" } } Call `configure_output()` first thing in `main`. There is no TTY detection in V3, so your program cannot tell whether stdout is a pipe. Escapes are emitted even when output is redirected. Honouring `NO_COLOR` is the only politeness available."},{"p":"tutorial-cli-tool","t":"Tutorial 2: A command-line tool","a":"the-finished-tool","h":"The finished tool","x":"-- Tutorial 2, finished program. -- -- A small command-line tool: read a file, count its lines, words and -- characters, then print a coloured report. Falls back to a sample file -- it writes itself when given no argument, so it always has input. pilot mut C_RESET = \"\\x1b[0m\" pilot mut C_BOLD = \"\\x1b[1m\" pilot mut C_DIM = \"\\x1b[2m\" pilot mut C_RED = \"\\x1b[1;31m\" pilot mut C_GREEN = \"\\x1b[32m\" pilot mut C_CYAN = \"\\x1b[36m\" -- Honour the NO_COLOR convention. Blanking the constants keeps every -- call site working unchanged. task configure_output() -> void { if process::env(\"NO_COLOR\") != \"\" { C_RESET = \"\" C_BOLD = \"\" C_DIM = \"\" C_RED = \"\" C_GREEN = \"\" C_CYAN = \"\" } } task sample_path() ->"},{"p":"tutorial-cli-tool","t":"Tutorial 2: A command-line tool","a":"the-finished-tool","h":"The finished tool","x":"word { pilot path: word = \"tut_sample.txt\" fs::write(path, \"the beta do not negotiate\\nhumanity answers with steel\\nand with pilots\\n\") give back path } -- Which file are we working on? Argument 0 is the executable itself. task target_path() -> word { if process::args_count() > 1 { give back process::arg(1) } give back sample_path() } task count_words(text: word) -> int { pilot mut total: int = 0 pilot mut in_word: bool = false pilot mut i: int = 0 repeat text.length() times { pilot c: word = text.char_at(i) pilot blank: bool = c == \" \" or c == \"\\n\" or c == \"\\t\" or c == \"\\r\" if blank { in_word = false } else { if not in_word { total += 1 } in_word = true } i += 1 } give back total } task"},{"p":"tutorial-cli-tool","t":"Tutorial 2: A command-line tool","a":"the-finished-tool","h":"The finished tool","x":"count_lines(text: word) -> int { if text.length() == 0 { give back 0 } pilot mut total: int = 1 pilot mut i: int = 0 repeat text.length() times { if text.char_at(i) == \"\\n\" { total += 1 } i += 1 } -- A trailing newline does not start a new line. if text.char_at(text.length() - 1) == \"\\n\" { total -= 1 } give back total } task row(label: word, value: int) -> void { say \" \" + C_DIM + label + C_RESET + \" \" + C_BOLD + word_from_int(value) + C_RESET } task main() -> void { configure_output() pilot path: word = target_path() if not fs::exists(path) { say C_RED + \"error\" + C_RESET + \": no such file: \" + path process::exit(1) } pilot text: word = fs::read(path) say C_CYAN + C_BOLD + \"report\" +"},{"p":"tutorial-cli-tool","t":"Tutorial 2: A command-line tool","a":"the-finished-tool","h":"The finished tool","x":"C_RESET + C_DIM + \" for \" + path + C_RESET row(\"lines \", count_lines(text)) row(\"words \", count_words(text)) row(\"characters\", text.length()) if count_words(text) > 5 { say \" \" + C_GREEN + \"ok\" + C_RESET + \" enough material to work with\" } fs::delete(\"tut_sample.txt\") } Run it against your own file: freak run wordcount.fk README.md NO_COLOR=1 freak run wordcount.fk README.md"},{"p":"tutorial-cli-tool","t":"Tutorial 2: A command-line tool","a":"what-you-would-reach-for-next-and-cannot","h":"What you would reach for next, and cannot","x":"Worth knowing before you plan a bigger tool: You might want V3 reality `result<T, E>` and `?` None. Sentinels and `process::exit` `.split(\",\")` into a list `string_split` returns an encoded word, not a list. Walk characters A map for counting words No `Map<K,V>`. Two parallel collections and `array_find` `for each line in lines` Counted `repeat` plus an index Writing to stderr No stderr writer. Everything goes to stdout A `-o` output flag Output path is fixed to the source basename See [Not in V3](not-in-v3.html) for substitutes."},{"p":"tutorial-data","t":"Tutorial 3: Modelling data","a":"tutorial-3-modelling-data","h":"Tutorial 3: Modelling data","x":"> Shapes, nested shapes, methods, and both of V3's collection surfaces \u2014 including the restriction that catches everyone. About 25 minutes. V3 gives you one user-defined type (`shape`) and two ways to hold a sequence. This tutorial builds a squadron roster and shows where each tool fits."},{"p":"tutorial-data","t":"Tutorial 3: Modelling data","a":"step-1-a-shape-with-behaviour","h":"Step 1 \u2014 A shape with behaviour","x":"shape Member { callsign: word sorties: int } impl Member { task enlist(callsign: word) -> Member { give back Member { callsign: callsign, sorties: 0 } } task veteran(self) -> bool { give back self.sorties >= 20 } } `enlist` has no `self`, so it is an associated method: `Member::enlist(\"V-1\")`. `veteran` takes `self`, so it is an instance method: `m.veteran()`. Constructor-style associated methods are the idiomatic way to build a shape with defaults, since fields have no default values."},{"p":"tutorial-data","t":"Tutorial 3: Modelling data","a":"step-2-nesting","h":"Step 2 \u2014 Nesting","x":"Shapes hold shapes, and access chains: shape Airframe { model: word thrust: int } shape Member { callsign: word sorties: int frame: Airframe } Construct inline: pilot m: Member = Member { callsign: \"Valkyrie-1\", sorties: 0, frame: Airframe { model: \"Takemikazuchi\", thrust: 880 } } say word_from_int(m.frame.thrust) say \"{m.callsign} flies a {m.frame.model}\" Dotted paths work inside interpolation, including `{self.frame.thrust}` from within a method. Shapes are handles into runtime storage, not values. Passing one to a task and mutating a field is visible to the caller, and assigning one binding to another aliases the same storage. To get an independent copy, construct a new shape."},{"p":"tutorial-data","t":"Tutorial 3: Modelling data","a":"step-3-lists","h":"Step 3 \u2014 Lists","x":"`List<T>` is the typed sequence. The element is a scalar or a shape name: pilot names: List<word> = [\"Valkyrie-1\", \"Valkyrie-2\", \"Valkyrie-3\"] say word_from_int(names.length()) say names[1] Three rules to internalise: **`.length()` is a method**, not a field. `names.length` reports *\"non-shape value has no fields\"*. **Indexed assignment needs `pilot mut`**, otherwise *\"indexed assignment requires a mutable list binding\"*. **The literal infers the type.** `[1, 2, 3]` is a `List<int>`; all elements must share one type. `List::filled(value, count)` builds a pre-populated list, which is the way to size one up front: pilot mut tally: List<int> = List::filled(0, 3) tally[0] = 24 There is no"},{"p":"tutorial-data","t":"Tutorial 3: Modelling data","a":"step-3-lists","h":"Step 3 \u2014 Lists","x":"`push`, `pop`, `sort` or iterator. It is an indexable sequence, nothing more."},{"p":"tutorial-data","t":"Tutorial 3: Modelling data","a":"step-4-the-restriction","h":"Step 4 \u2014 The restriction","x":"This is the one that catches people: shape Roster { members: List<Member> -- does not compile } type error: V3 owned shape fields do not yet support List values A `List<T>` may be a binding, a task parameter and a return type \u2014 but **not a shape field**. So the \"container shape holding a list of children\" pattern is unavailable. Work around it one of three ways: Keep the list beside the shape rather than inside it. Store a legacy `int` array handle in the field. Pass lists through tasks, which is allowed: task total_sorties(counts: List<int>) -> int { pilot mut sum: int = 0 pilot mut i: int = 0 repeat counts.length() times { sum += counts[i] i += 1 } give back sum }"},{"p":"tutorial-data","t":"Tutorial 3: Modelling data","a":"step-5-the-legacy-handle","h":"Step 5 \u2014 The legacy handle","x":"Alongside `List<T>` there is an older untyped array: `array_new()` returns an `int` handle, and every element is a `word`. pilot h: int = array_new() array_push(h, \"alpha\") array_push(h, \"bravo\") say word_from_int(array_len(h)) say array_get(h, 0) array_release(h) It is worth keeping around for two reasons: it can **grow** (`array_push`), and it is the only thing the `std/algorithm.fk` helpers accept. array_sort_int(h) say array_join(h, \", \") say word_from_int(array_max_int(h)) Those helpers are declared `handle: int`, so passing a `List<word>` fails: type error: call to 'array_join' argument 1 expects int, got List<word> The `array_*` **builtins** are more forgiving \u2014 `array_len`,"},{"p":"tutorial-data","t":"Tutorial 3: Modelling data","a":"step-5-the-legacy-handle","h":"Step 5 \u2014 The legacy handle","x":"`array_get`, `array_set`, `array_push` accept either. Builtins bridge; library tasks do not. Two helpers are broken in the shipping compiler: `array_sort_word()` segfaults, and `array_unique()` returns an empty array. Use `array_sort_int`, and deduplicate by hand."},{"p":"tutorial-data","t":"Tutorial 3: Modelling data","a":"step-6-choosing-between-them","h":"Step 6 \u2014 Choosing between them","x":"Need Use Typed elements, fixed size `List<T>` Numbers without converting to `word` `List<int>` / `List<num>` Growing a collection Legacy handle + `array_push` Sorting, joining, searching Legacy handle + `std/algorithm.fk` A field inside a shape Legacy handle \u2014 `List` is rejected Returning a collection from a task Either; or mutate a handle passed in `array_release(h)` frees a legacy array. V3 has no garbage collector, so a long-running program that allocates in a loop must release."},{"p":"tutorial-data","t":"Tutorial 3: Modelling data","a":"the-finished-program","h":"The finished program","x":"-- Tutorial 3, finished program. -- -- Model a squadron with shapes, keep the members in a List, and compute -- a summary. Shows the two collection surfaces working side by side and -- the one restriction that catches people: a shape cannot hold a List. shape Airframe { model: word thrust: int } shape Member { callsign: word sorties: int frame: Airframe } impl Member { task enlist(callsign: word, model: word, thrust: int) -> Member { give back Member { callsign: callsign, sorties: 0, frame: Airframe { model: model, thrust: thrust } } } task veteran(self) -> bool { give back self.sorties >= 20 } task line(self) -> word { give back \"{self.callsign} {self.frame.model} thrust"},{"p":"tutorial-data","t":"Tutorial 3: Modelling data","a":"the-finished-program","h":"The finished program","x":"{self.frame.thrust} sorties {self.sorties}\" } } -- A List may be a parameter and a return type. It may NOT be a field on -- a shape: that reports -- \"V3 owned shape fields do not yet support List values\" task total_sorties(counts: List<int>) -> int { pilot mut sum: int = 0 pilot mut i: int = 0 repeat counts.length() times { sum += counts[i] i += 1 } give back sum } task main() -> void { pilot mut ichi: Member = Member::enlist(\"Valkyrie-1\", \"Takemikazuchi\", 880) pilot mut ni: Member = Member::enlist(\"Valkyrie-2\", \"Shiranui\", 640) pilot mut san: Member = Member::enlist(\"Valkyrie-3\", \"Gekishin\", 520) repeat 24 times { ichi.sorties += 1 } repeat 8 times { ni.sorties += 1 } repeat 31 times {"},{"p":"tutorial-data","t":"Tutorial 3: Modelling data","a":"the-finished-program","h":"The finished program","x":"san.sorties += 1 } say ichi.line() say ni.line() say san.line() -- A typed List holds the numbers we want to aggregate. pilot counts: List<int> = [ichi.sorties, ni.sorties, san.sorties] say \"total sorties: \" + word_from_int(total_sorties(counts)) say \"squadron size: \" + word_from_int(counts.length()) -- Names in a List<word>, indexed like any other list. pilot names: List<word> = [ichi.callsign, ni.callsign, san.callsign] pilot mut i: int = 0 repeat names.length() times { say \" member \" + word_from_int(i) + \": \" + names[i] i += 1 } -- Counting veterans needs the shapes themselves, so loop over them -- directly rather than trying to put Members in a List. pilot mut veterans: int = 0 if"},{"p":"tutorial-data","t":"Tutorial 3: Modelling data","a":"the-finished-program","h":"The finished program","x":"ichi.veteran() { veterans += 1 } if ni.veteran() { veterans += 1 } if san.veteran() { veterans += 1 } say \"veterans: \" + word_from_int(veterans) -- The legacy handle is still the right tool when you need the -- std/algorithm.fk helpers, which are declared with `handle: int`. pilot ranking: int = array_new() array_push(ranking, word_from_int(ichi.sorties)) array_push(ranking, word_from_int(ni.sorties)) array_push(ranking, word_from_int(san.sorties)) array_sort_int(ranking) say \"sorted sorties: \" + array_join(ranking, \", \") say \"busiest pilot flew \" + word_from_int(array_max_int(ranking)) array_release(ranking) }"},{"p":"tutorial-data","t":"Tutorial 3: Modelling data","a":"where-to-go-next","h":"Where to go next","x":"[Lists & arrays](arrays.html) \u2014 the full reference for both surfaces [Shapes & impl](shapes.html) \u2014 operator doctrines and backend caveats [Standard library](stdlib.html) \u2014 `ByteBuffer`, `word_builder`, JSON, semver [Bible vs V3](conformance.html) \u2014 how far this is from the specification"},{"p":"language-basics","t":"Bindings & types","a":"bindings-types","h":"Bindings & types","x":"> The value model: five types, one binding keyword, and a case-insensitive keyword table that will bite you."},{"p":"language-basics","t":"Bindings & types","a":"bindings","h":"Bindings","x":"`pilot` introduces a binding. The type annotation is optional; when present it follows the name after a colon. -- Bindings are declared with `pilot`. -- `fixed pilot` marks an immutable binding. -- `pilot mut` opts a binding into reassignment. task main() -> void { pilot answer: int = 42 pilot ratio: num = 3.5 pilot callsign: word = \"Shirogane\" pilot ready: bool = true fixed pilot MAX_SORTIES: int = 12 pilot mut score: int = 0 score = score + 10 score += 5 say word_from_int(answer) say format_num(ratio) say callsign say word_from_bool(ready) say word_from_int(MAX_SORTIES) say word_from_int(score) } Three forms exist: Form Meaning `pilot x = expr` Ordinary binding `pilot mut x = expr`"},{"p":"language-basics","t":"Bindings & types","a":"bindings","h":"Bindings","x":"Explicitly reassignable `fixed pilot x = expr` Immutable binding `mut` and `fixed` only change behaviour under `--strict-borrow`. Without that flag V3 does no ownership or mutability checking, and all three forms behave identically. Write `mut` anyway \u2014 it documents intent and it is what the checker will demand when you turn it on. Assignment operators: `=`, `+=`, `-=`, `*=`, `/=`, `%=`."},{"p":"language-basics","t":"Bindings & types","a":"type-inference","h":"Type inference","x":"The annotation can always be omitted. V3 infers from the initialiser. -- The type annotation is optional. V3 infers from the initializer. -- When you do annotate, the annotation is a single identifier: -- `int`, `num`, `word`, `bool`, or the name of a shape. task main() -> void { pilot a = 7 -- int pilot b = 2.5 -- num pilot c = \"text\" -- word pilot d = false -- bool say word_from_int(a) say format_num(b) say c say word_from_bool(d) }"},{"p":"language-basics","t":"Bindings & types","a":"the-type-table","h":"The type table","x":"Type Notes `int` 64-bit signed integer. The default for whole-number literals `num` 64-bit float. Any literal containing `.` `word` UTF-8 string `bool` `true` / `false`, and the aliases `yes` / `no` / `hai` / `iie` `void` Absence of a value. Only a return type `List<T>` Typed sequence. `T` is a scalar or a shape name; no nesting `ByteBuffer` Builtin binary read/write cursor, with real methods *a shape name* Any type you declared with `shape` There is no `uint`, `tiny`, `char`, `big`, `float`, `float32` or `never`; and no `maybe<T>`, `result<T,E>`, `Map<K,V>`, `Set<T>`, tuple, fixed array or raw pointer."},{"p":"language-basics","t":"Bindings & types","a":"the-type-annotation-grammar","h":"The type-annotation grammar","x":"The parser takes one identifier, plus a single special case for `List`: pilot a: int = 1 -- fine pilot b: word = \"x\" -- fine pilot c: Rect = Rect { w: 1, h: 2 } -- fine, Rect is a shape pilot d: List<int> = [1, 2, 3] -- fine pilot e: ByteBuffer = ByteBuffer::new() -- none of these parse: -- pilot f: maybe<int> = some(1) -- pilot g: [int; 4] = [1, 2, 3, 4] -- pilot h: List<List<int>> = [] -- task i<T>(x: T) -> T { give back x } `List<T>` is the only generic form in the language, and it is hard-coded in `parser_take_type` rather than being a general mechanism \u2014 the element must be a plain identifier, so `List<List<int>>` is rejected with *\"expected '>' after list element type\"*. The same"},{"p":"language-basics","t":"Bindings & types","a":"the-type-annotation-grammar","h":"The type-annotation grammar","x":"grammar applies to shape fields, task parameters, task return types and `extern` signatures. `List<T>` and `ByteBuffer` are recent additions to V3. Much of the rest of the language still assumes the older untyped `int` handle, so the two surfaces coexist \u2014 see [Lists & arrays](arrays.html) for which calls accept which."},{"p":"language-basics","t":"Bindings & types","a":"numeric-literals","h":"Numeric literals","x":"A literal with a `.` is `num`; otherwise `int`. pilot i = 42 -- int pilot f = 42.0 -- num There is **no negative literal**. `-5` parses as unary minus applied to `5`, which is fine inside an expression but not as an initialiser in every position. The idiom used throughout the compiler's own source is a subtraction: pilot below_zero: int = 0 - 5 say word_from_int(-below_zero) There are also no numeric suffixes: `42u`, `3.14f`, `42t` and `999b` are all V4."},{"p":"language-basics","t":"Bindings & types","a":"mixing-int-and-num","h":"Mixing int and num","x":"Arithmetic that involves a `num` on either side produces `num`. Convert explicitly at the boundary: pilot n: int = 7 pilot f: num = n.to_num() pilot back: int = f.to_int() Conversion Result `intValue.to_num()` `num` `intValue.to_word()` `word` `numValue.to_int()` `int` `numValue.to_word()` `word` `boolValue.to_word()` `word` `wordValue.to_int()` `int` `wordValue.to_num()` `num` `word_from_int(i)` `word` `word_from_bool(b)` `word` `format_num(f)` `word` `parse_num(w)` `num` `word_to_int(w)` `int` `char_to_word(code)` `word`"},{"p":"language-basics","t":"Bindings & types","a":"reserved-words-are-case-insensitive","h":"Reserved words are case-insensitive","x":"This is the single most surprising rule in V3, and the one that produces the most confusing errors. The lexer lowercases every identifier before comparing it against the keyword table. `pilot`, `Pilot`, `PILOT` and `PiLoT` are all the same keyword token. A keyword can never be an identifier, so none of those can name a shape, task, field or binding. -- V3 matches keywords CASE-INSENSITIVELY. `pilot`, `Pilot` and `PILOT` -- are all the same keyword token, so none of them can be used as a name. -- -- This bites most often on: Pilot, Result, Max, Route, Check, Move, -- Copy, Some, Ok, Err, Got, Use, In, As, Each, Times, Done, Max. -- -- Pick a synonym instead. These all compile: shape Aviator"},{"p":"language-basics","t":"Bindings & types","a":"reserved-words-are-case-insensitive","h":"Reserved words are case-insensitive","x":"{ tag: word } task outcome(value: int) -> word { if value > 0 { give back \"positive\" } give back \"non-positive\" } task main() -> void { pilot a: Aviator = Aviator { tag: \"V-1\" } pilot verdict: word = outcome(3) pilot upper_bound: int = 100 say a.tag say verdict say word_from_int(upper_bound) } Naming a shape `Pilot` produces: error: expected an identifier for shape name, found 'Pilot'"},{"p":"language-basics","t":"Bindings & types","a":"the-full-reserved-list","h":"The full reserved list","x":"Every word below is reserved **in any casing**: pilot fixed task say shape impl doctrine launch use as in lend mut move copy break continue if else when repeat times until done for each check result got nobody some ok err sessions max foreshadow payoff route sadly deus_ex_machina isekai eventually and or not nakama tsundere extern true false yes no hai iie Multi-word keywords, lexed greedily: give back or else trust me for each for science training arc bringing back only on PLUS ULTRA FINAL FORM The names that catch people most often are `Pilot`, `Result`, `Max`, `Route`, `Check`, `Move`, `Copy`, `Some`, `Ok`, `Err`, `Got`, `Use`, `In`, `As`, `Each`, `Done` and `Times`. Pick a synonym:"},{"p":"language-basics","t":"Bindings & types","a":"the-full-reserved-list","h":"The full reserved list","x":"`Aviator`, `Outcome`, `upper_bound`, `verdict`. Several of these words are reserved but have **no grammar at all** in V3 \u2014 `doctrine`, `use`, `launch`, `check`, `lend`, `route`, `foreshadow`, `isekai`, `trust me`, `for each`. They lex as keywords and then fail in the parser with *\"this token cannot start an expression\"*. They still cannot be used as identifiers."},{"p":"language-basics","t":"Bindings & types","a":"top-level-code","h":"Top-level code","x":"A file does not need a `main`. Statements written at the top level run in order, and top-level bindings are visible to every task in the file. -- A file does not need a `main`. Top-level statements run in order, -- and top-level bindings are visible to every task in the file. pilot squadron: word = \"Valkyries\" pilot strength: int = 4 task roster() -> word { give back \"{squadron} x{strength}\" } say \"booting\" say roster() strength = 5 say roster() This is a real difference from the bible, which forbids arbitrary executable statements at root scope and requires root bindings to be `fixed pilot` constants. V3 allows both."},{"p":"language-basics","t":"Bindings & types","a":"comments","h":"Comments","x":"`--` starts a comment that runs to the end of the line. There is no block comment form. -- a whole-line comment pilot x = 1 -- a trailing comment"},{"p":"tasks","t":"Tasks","a":"tasks","h":"Tasks","x":"> Functions are called tasks. One declaration form, one return keyword, no shorthands."},{"p":"tasks","t":"Tasks","a":"declaring-a-task","h":"Declaring a task","x":"task name(param: type, param: type) -> returntype { body } `-> returntype` may be omitted, in which case the return type is `void`. Parameter and return types follow the same grammar as everywhere else \u2014 one identifier, or `List<T>`. See [the type-annotation grammar](language-basics.html#the-type-annotation-grammar). -- `task` declares a function. `give back` returns a value. -- A block-bodied task that returns a non-void type must use `give back` -- on every path; there is no implicit tail return. task add(a: int, b: int) -> int { give back a + b } task shout(message: word) -> void { say message.to_upper() } task classify(n: int) -> word { if n < 0 { give back \"negative\" } if n == 0 { give"},{"p":"tasks","t":"Tasks","a":"declaring-a-task","h":"Declaring a task","x":"back \"zero\" } give back \"positive\" } task main() -> void { say word_from_int(add(20, 22)) shout(\"stand by\") say classify(0 - 3) say classify(0) say classify(9) }"},{"p":"tasks","t":"Tasks","a":"give-back","h":"`give back`","x":"`give back expr` returns a value. `give back` on its own returns from a `void` task. There is **no implicit tail return**. A bare expression at the end of a block is evaluated and discarded. Every path that must produce a value needs its own `give back`: task area(r: num) -> num { math::pow(r, 2.0) * 3.14159 -- computed, then thrown away } task area_ok(r: num) -> num { give back math::pow(r, 2.0) * 3.14159 } The checker reports a missing return on a value-returning task, pointing at the `task` keyword of the offending declaration. The arrow shorthand `task square(x: num) => x * x` is in the bible but does **not** parse in V3 \u2014 you get `expected '{', found '=>'`. Neither does the `done`"},{"p":"tasks","t":"Tasks","a":"give-back","h":"`give back`","x":"block terminator: `task f() -> int` \u2026 `done` fails with `expected '{', found 'give back'`. Braces are the only block delimiter."},{"p":"tasks","t":"Tasks","a":"calling","h":"Calling","x":"task main() -> void { say word_from_int(add(1, 2)) } Arguments are positional. Named call arguments \u2014 `connect(host: \"localhost\", port: 8080)` \u2014 are V4 and do not parse. Argument count and argument types are both checked against the declaration."},{"p":"tasks","t":"Tasks","a":"order-does-not-matter","h":"Order does not matter","x":"Every task in the file is indexed before any body is checked, so a task may call one declared later, and may call itself. -- Tasks may call themselves, and may call tasks declared later in the -- file: every task is indexed before any body is checked. task factorial(n: int) -> int { if n <= 1 { give back 1 } give back n * factorial(n - 1) } task fib(n: int) -> int { if n < 2 { give back n } give back fib(n - 1) + fib(n - 2) } task main() -> void { say word_from_int(factorial(6)) say word_from_int(fib(12)) }"},{"p":"tasks","t":"Tasks","a":"main","h":"`main`","x":"If a task called `main` exists it becomes the program entry point. If it does not, top-level statements are the program. You can have both \u2014 top-level statements run first, then `main`."},{"p":"tasks","t":"Tasks","a":"parameters-are-by-value","h":"Parameters are by value","x":"There is no `lend` / `lend mut` in V3: the borrow syntax from the bible does not parse in a parameter list. Every parameter is passed by value. -- does not parse in V3: -- task show(lend p: Point) -> void { say word_from_int(p.x) } -- write this instead: task show(p: Point) -> void { say word_from_int(p.x) } For shapes this matters: a shape parameter is a handle into runtime storage, so mutating a field inside a task is visible to the caller. Treat shape arguments as shared, and return a new value when you want isolation."},{"p":"tasks","t":"Tasks","a":"instance-and-associated-methods","h":"Instance and associated methods","x":"A task declared inside `impl Shape { ... }` whose first parameter is `self` is an instance method; without `self` it is an associated method. Both are covered in [Shapes & impl](shapes.html#methods)."},{"p":"tasks","t":"Tasks","a":"the-pipe-operator","h":"The pipe operator","x":"`|>` rewrites a call so the piped value becomes its first argument. -- `|>` feeds the left value in as the FIRST argument of the call on the -- right, so `x |> f(y)` is exactly `f(x, y)`. task double(n: int) -> int { give back n * 2 } task offset(n: int, by: int) -> int { give back n + by } task main() -> void { say word_from_int(5 |> double()) say word_from_int(5 |> offset(3)) say word_from_int(5 |> double() |> offset(1)) } `x |> f()` is exactly `f(x)`, and `x |> f(y)` is exactly `f(x, y)`. The right side must be a plain task name \u2014 you cannot pipe into a method or a shape constructor."},{"p":"tasks","t":"Tasks","a":"extern-tasks","h":"`extern` tasks","x":"A task implemented in C is declared with `extern`, on one line, with no body. See [extern & FFI](ffi.html). extern task abs(v: int) -> int"},{"p":"tasks","t":"Tasks","a":"what-tasks-cannot-do","h":"What tasks cannot do","x":"Feature Status Generic parameters `task f<T>(...)` V4 \u2014 `expected '(', found '<'` Named call arguments V4 Default parameter values V4 Variadic parameters V4 Closures / lambdas `\\|x\\| => x * 2` V4 \u2014 `|` cannot start an expression Arrow bodies `=> expr` V4 `done` as a block terminator V4 Returning `maybe<T>` or `result<T,E>` V4 \u2014 no such types `?` error propagation, `or else` V4 Visibility modifiers `launch` / `launch(package)` V4"},{"p":"control-flow","t":"Control flow","a":"control-flow","h":"Control flow","x":"> Two branch forms, three loop forms, and a bounded loop you will not find in other languages."},{"p":"control-flow","t":"Control flow","a":"if-else-if-else","h":"if / else if / else","x":"Conditions take no parentheses. Braces are mandatory even for one statement. -- `if` / `else if` / `else`. Conditions take no parentheses. task describe(power: int) -> word { if power > 9000 { give back \"over nine thousand\" } else if power > 5000 { give back \"high\" } else if power > 1000 { give back \"moderate\" } else { give back \"low\" } } task main() -> void { say describe(9001) say describe(6000) say describe(2000) say describe(10) } `else if` chains to any depth. Internally V3 flattens these rather than nesting them, which is one of the architectural fixes that made the self-hosted compiler possible."},{"p":"control-flow","t":"Control flow","a":"when","h":"when","x":"`when` matches a subject against literal arms. `_` is the catch-all. -- `when` matches a subject against literal arms. `_` is the catch-all. -- Each arm takes one statement; use a block to run several. task main() -> void { pilot code: int = 2 when code { 1 -> say \"launch\" 2 -> { say \"hold\" say \"await orders\" } 3 -> say \"abort\" _ -> say \"unknown\" } pilot phase: word = \"sortie\" when phase { \"briefing\" -> say \"in the briefing room\" \"sortie\" -> say \"in the air\" _ -> say \"elsewhere\" } } Rules: Each arm is `pattern -> statement`. One statement \u2014 wrap several in a block. Arms are separated by nothing at all; newlines are enough. The subject may be `int`, `word`, `bool` or `num`. Arm patterns are"},{"p":"control-flow","t":"Control flow","a":"when","h":"when","x":"**expressions compared for equality**, not patterns. There is no destructuring, no binding, no guards, no ranges. Exhaustiveness is not checked. Without a `_` arm and with no match, nothing runs. `when` is a **statement**, not an expression. It cannot appear on the right of `=` or inside `give back`. To produce a value from a match, assign inside the arms: task main() -> void { pilot code: int = 2 pilot mut label: word = \"\" when code { 1 -> label = \"launch\" 2 -> label = \"hold\" _ -> label = \"unknown\" } say label } Destructuring arms \u2014 `BETA::Soldier { position } -> engage_at(position)` \u2014 need variants, which V3 does not have. So does `check` over `maybe` / `result`, and `check route`. See"},{"p":"control-flow","t":"Control flow","a":"when","h":"when","x":"[Not in V3](not-in-v3.html)."},{"p":"control-flow","t":"Control flow","a":"loops","h":"Loops","x":"V3 has exactly three loop forms. -- V3 has three loop forms: a counted loop, a condition loop, -- and the bounded `training arc`. task main() -> void { -- repeat N times pilot mut total: int = 0 repeat 5 times { total += 2 } say word_from_int(total) -- repeat until CONDITION (tested before each pass) pilot mut countdown: int = 3 repeat until countdown == 0 { say word_from_int(countdown) countdown -= 1 } -- break and continue work in every loop form pilot mut seen: int = 0 repeat 10 times { seen += 1 if seen == 3 { continue } if seen == 6 { break } } say word_from_int(seen) }"},{"p":"control-flow","t":"Control flow","a":"repeat-n-times","h":"repeat N times","x":"repeat COUNT times { body } `COUNT` is any `int` expression, evaluated **once** before the loop. There is no loop variable \u2014 keep your own counter if you need the index. pilot mut i: int = 0 repeat array_len(items) times { say array_get(items, i) i += 1 }"},{"p":"control-flow","t":"Control flow","a":"repeat-until","h":"repeat until","x":"repeat until CONDITION { body } The condition is tested **before** each pass, so the body may run zero times. Note the sense: the loop continues while the condition is *false*."},{"p":"control-flow","t":"Control flow","a":"training-arc","h":"training arc","x":"training arc until CONDITION max N sessions { body } A loop with a compulsory iteration cap. It stops when the condition becomes true **or** when the cap is reached \u2014 whichever comes first \u2014 so it cannot spin forever. -- `training arc` is a loop with a mandatory session cap. -- It stops when the condition becomes true OR when the cap is hit, -- so it cannot spin forever. task main() -> void { pilot mut power: int = 0 training arc until power >= 100 max 8 sessions { power += 20 } say word_from_int(power) -- The cap wins when the condition is never reachable. pilot mut stuck: int = 0 training arc until stuck > 1000 max 4 sessions { stuck += 1 } say word_from_int(stuck) } It lowers to roughly:"},{"p":"control-flow","t":"Control flow","a":"training-arc","h":"training arc","x":"int64_t sessions = 0; while (!(CONDITION) && sessions < N) { body; sessions++; } This makes it genuinely useful for bounded numeric search, where you want a hard guarantee of termination: -- A slightly larger program: integer square root by bisection, driven by -- a `training arc` so the loop is provably bounded. shape Bounds { lo: int hi: int } task isqrt(target: int) -> int { if target < 2 { give back target } pilot mut b: Bounds = Bounds { lo: 1, hi: target } training arc until b.lo >= b.hi max 64 sessions { pilot mid: int = (b.lo + b.hi + 1) / 2 if mid * mid > target { b.hi = mid - 1 } else { b.lo = mid } } give back b.lo } task main() -> void { say word_from_int(isqrt(0)) say"},{"p":"control-flow","t":"Control flow","a":"training-arc","h":"training arc","x":"word_from_int(isqrt(1)) say word_from_int(isqrt(15)) say word_from_int(isqrt(16)) say word_from_int(isqrt(17)) say word_from_int(isqrt(1000000)) } The `with growth` variant \u2014 which makes the compiler verify that the body actually mutates the condition's subject \u2014 is V4. Plain `training arc` is fully supported."},{"p":"control-flow","t":"Control flow","a":"break-and-continue","h":"break and continue","x":"Both work in all three loop forms. repeat 10 times { if should_skip { continue } if should_stop { break } process() }"},{"p":"control-flow","t":"Control flow","a":"bare-blocks","h":"Bare blocks","x":"A `{ ... }` on its own is a statement. It groups code but does **not** create a new scope for bindings \u2014 a `pilot` declared inside remains visible afterwards. task main() -> void { pilot x: int = 1 { say word_from_int(x) } }"},{"p":"control-flow","t":"Control flow","a":"eventually","h":"eventually","x":"`eventually { ... }` is the cleanup block. Its V3 behaviour differs sharply from the specification. -- `eventually { ... }` marks a cleanup block. -- -- IMPORTANT V3 caveat: V3 emits the block INLINE at the point it is -- written. It is not deferred to the end of scope and it does not run -- on `give back`, `break` or `panic`. Place it where you want it to run. task main() -> void { say \"engaging\" eventually { say \"cleanup\" } say \"disengaged\" } V3 emits the block **inline, where you wrote it**. It is not deferred to the end of scope, it does not run in LIFO order with other `eventually` blocks, and it does not run on `give back`, `break` or `panic`. Treat it as a labelled section, not as"},{"p":"control-flow","t":"Control flow","a":"eventually","h":"eventually","x":"`defer`. True deferred semantics are V4."},{"p":"control-flow","t":"Control flow","a":"no-for-each","h":"No `for each`","x":"`for each` lexes as a keyword but has no grammar in V3: error: unexpected 'for each' \u2014 this token cannot start an expression Iterate with an index instead: pilot mut i: int = 0 repeat array_len(items) times { say array_get(items, i) i += 1 }"},{"p":"control-flow","t":"Control flow","a":"summary-of-what-is-missing","h":"Summary of what is missing","x":"Construct Status `for each x in list` V4 `for each (i, x) in list.enumerate()` V4 `check` over `maybe` / `result` V4 `check route` V4 `when` with destructuring or guards V4 `when` in expression position V4 `training arc ... with growth` V4 `xm3 { a \\|\\| b }` and all squadron concurrency V4 `prob[0.3] chance { }`, `prob_when` V4 `isekai { } bringing back { }` V4 `eventually if cond { }` V4 True deferred `eventually` V4"},{"p":"operators","t":"Operators","a":"operators","h":"Operators","x":"> Full precedence table, exact type rules, and the four anime operators V3 really implements."},{"p":"operators","t":"Operators","a":"precedence","h":"Precedence","x":"Loosest binding first. Everything on one row is left-associative. Level Operators 1 `or` 2 `and` 3 `==` `!=` `<` `>` `<=` `>=` 4 `+` `-` `NAKAMA` 5 `*` `/` `%` 6 (prefix) `not` `-` `PLUS ULTRA` `FINAL FORM` `TSUNDERE` 7 (postfix) `.field` `.method()` `[index]` `\\|>` Parentheses group as expected. -- Arithmetic, comparison and logic. task main() -> void { pilot a: int = 17 pilot b: int = 5 say word_from_int(a + b) say word_from_int(a - b) say word_from_int(a * b) say word_from_int(a / b) say word_from_int(a % b) say word_from_bool(a > b) say word_from_bool(a == 17) say word_from_bool(a != b) say word_from_bool(a <= 17) say word_from_bool(a > b and b > 0) say word_from_bool(a < b or b > 0)"},{"p":"operators","t":"Operators","a":"precedence","h":"Precedence","x":"say word_from_bool(not false) -- Unary minus applies to an expression. A negative *literal* -- is written as a subtraction: `0 - 5`. pilot neg: int = 0 - 5 say word_from_int(-neg) }"},{"p":"operators","t":"Operators","a":"type-rules","h":"Type rules","x":"The checker is strict \u2014 these are the exact rules, not approximations. Operator Accepts Produces `+` both numeric (`int`/`num`), **or** both `word` `num` if either side is `num`, else `int`; `word` for word+word `-` `*` `/` both numeric `num` if either side is `num`, else `int` `%` **both `int`** `int` `NAKAMA` **both `int`** `int` `and` `or` **both `bool`** `bool` `==` `!=` equality-compatible types `bool` `<` `>` `<=` `>=` both numeric `bool` `not` `bool` `bool` `-` (prefix) numeric same as operand `PLUS ULTRA` `FINAL FORM` `TSUNDERE` **`int` only** `int` Two consequences worth internalising: **`%` is integer-only.** `7.5 % 2.0` is a type error, not a float remainder. **`and` / `or` do"},{"p":"operators","t":"Operators","a":"type-rules","h":"Type rules","x":"not coerce.** There is no truthiness. `if count and flag` is an error unless `count` is already `bool`. `+` is the only overloaded operator: numeric addition and word concatenation. say \"Muv\" + \"-\" + \"Luv\" say word_from_int(2 + 3) `**` (exponent) does not exist \u2014 you get `expected ',', found '**'`. Use `math::pow(base, exp)` for `num`, or `std_pow(base, exp)` for `int`."},{"p":"operators","t":"Operators","a":"negative-numbers","h":"Negative numbers","x":"There is no negative literal. `-5` is unary minus applied to `5`. In an initialiser position, write the subtraction the compiler's own source uses: pilot below: int = 0 - 5 say word_from_int(-below)"},{"p":"operators","t":"Operators","a":"the-anime-operators","h":"The anime operators","x":"Four of the bible's anime operators are implemented. Their V3 semantics are **not** what the specification describes \u2014 the specification defines emotional scaling formulas; V3 implements simple integer arithmetic. -- The anime operators that V3 actually implements. -- The three unary forms are PREFIX and accept `int` only. task main() -> void { pilot base: int = 6 say word_from_int(FINAL FORM base) -- base * base say word_from_int(PLUS ULTRA base) -- base * 2 say word_from_int(TSUNDERE base) -- 0 - base -- NAKAMA is infix, at the same precedence as + and -. say word_from_int(3 NAKAMA 4) } Operator Form V3 lowering Bible says `FINAL FORM` prefix, `int` `x * x` postfix `base FINAL FORM`, plus"},{"p":"operators","t":"Operators","a":"the-anime-operators","h":"The anime operators","x":"a 5-second build pause `PLUS ULTRA` prefix, `int` `x * 2` infix `base PLUS ULTRA emotion` \u2192 `base * (1 + e\u00b2)` `TSUNDERE` prefix, `int` `0 - x` postfix; `!x` for bool, `-x` for num `NAKAMA` infix, `int` `a + b` `a + b + (a * b * 0.1)` Position and type both differ from the specification. In V3 all three unary operators are **prefix** and accept **`int` only**. `base FINAL FORM` fails with `expected ',', found 'FINAL FORM'`, and `TSUNDERE true` fails with `operator 'TSUNDERE' does not accept bool`. Keyword matching is case-insensitive, so `plus ultra`, `Plus Ultra` and `PLUS ULTRA` all lex to the same token. Both words must be present \u2014 `PLUS` alone is an ordinary identifier."},{"p":"operators","t":"Operators","a":"the-pipe-operator","h":"The pipe operator","x":"`|>` binds at postfix precedence and rewrites a call so the left value becomes its **first** argument. -- `|>` feeds the left value in as the FIRST argument of the call on the -- right, so `x |> f(y)` is exactly `f(x, y)`. task double(n: int) -> int { give back n * 2 } task offset(n: int, by: int) -> int { give back n + by } task main() -> void { say word_from_int(5 |> double()) say word_from_int(5 |> offset(3)) say word_from_int(5 |> double() |> offset(1)) } x |> f() is f(x) x |> f(y) is f(x, y) x |> f() |> g() is g(f(x)) The right-hand side must be a bare task name followed by an optional argument list. You cannot pipe into a method call, a shape constructor, or a namespaced builtin like"},{"p":"operators","t":"Operators","a":"the-pipe-operator","h":"The pipe operator","x":"`math::sqrt`."},{"p":"operators","t":"Operators","a":"postfix-forms","h":"Postfix forms","x":"Form Meaning `value.field` Shape field access `value.method(args)` Instance method or builtin method call `Shape::method(args)` Associated method call `namespace::call(args)` Builtin namespace call (`math::sqrt`, `fs::read`, \u2026) `word[i]` Indexing \u2014 **only on `word`**, yielding a one-character `word` `value \\|> task()` Pipe Indexing an array handle with `[i]` does not work. Arrays use `array_get(handle, i)` \u2014 see [Arrays](arrays.html)."},{"p":"operators","t":"Operators","a":"operator-overloading","h":"Operator overloading","x":"Writing `impl Add for YourShape` registers an implementation, and the method is callable by name. V3 does **not** rewrite the `+` token to dispatch to it \u2014 see [Shapes & impl](shapes.html#operator-doctrines). -- `impl Doctrine for Shape` registers an operator implementation. -- V3 does not accept a `doctrine` *declaration*, but it does accept -- this impl form for the built-in operator doctrines, and the method -- is callable by name. shape Vec2 { x: int y: int } impl Add for Vec2 { task add(self, other: Vec2) -> Vec2 { give back Vec2 { x: self.x + other.x, y: self.y + other.y } } } impl Vec2 { task show(self) -> word { give back \"({self.x}, {self.y})\" } } task main() -> void { pilot a:"},{"p":"operators","t":"Operators","a":"operator-overloading","h":"Operator overloading","x":"Vec2 = Vec2 { x: 1, y: 2 } pilot b: Vec2 = Vec2 { x: 10, y: 20 } pilot c: Vec2 = a.add(b) say c.show() }"},{"p":"operators","t":"Operators","a":"not-implemented","h":"Not implemented","x":"Operator Status `**` exponent V4 \u2014 use `math::pow` / `std_pow` `?` error propagation V4 `or else` fallback V4 `\\|\\|` as an xm3 branch separator V4 \u2014 lexes as one token but has no grammar `as?` downcast V4 `*ptr`, `.read()`, `.write()`, `.offset()`, `.cast<U>()` V4 `..` ranges V4"},{"p":"words","t":"Words & interpolation","a":"words-interpolation","h":"Words & interpolation","x":"> `word` is the string type. Its methods are compiler builtins, and `{path}` interpolation has precise rules."},{"p":"words","t":"Words & interpolation","a":"literals-and-escapes","h":"Literals and escapes","x":"A `word` literal is double-quoted. Escape Meaning `\\n` newline `\\r` carriage return `\\t` tab `\\\"` double quote `\\\\` backslash `\\xNN` byte with hex value `NN` `\\x1b` is the ESC byte, which is how you produce coloured console output \u2014 see [Coloured output](console.html). `\\x00` is rejected at lex time: *\"embedded NUL escape is not supported\"*. V3 words use a NUL-terminated runtime representation, so a word can never contain a zero byte. A malformed hex escape (fewer than two hex digits) is also a lex error. An unterminated literal reports *\"unterminated string literal\"* pointing at the opening quote."},{"p":"words","t":"Words & interpolation","a":"methods","h":"Methods","x":"All of these are compiler builtins \u2014 no import, and they work under `freak check` as well as `freak build`. -- `word` is FREAK's string type. These methods are compiler builtins, -- available with no import. task main() -> void { pilot s: word = \" Muv-Luv Alternative \" say s.trim() say word_from_int(s.length()) say s.trim().to_upper() say s.trim().to_lower() say word_from_bool(s.contains(\"Luv\")) say word_from_bool(s.trim().starts_with(\"Muv\")) say word_from_bool(s.trim().ends_with(\"ive\")) say s.trim().replace(\"Luv\", \"Love\") say s.trim().substring(0, 3) say s.trim().char_at(0) -- Concatenation with `+`, or with the builtin. say \"Muv\" + \"-\" + \"Luv\" say word_concat(\"XM\", \"3\") -- Numeric"},{"p":"words","t":"Words & interpolation","a":"methods","h":"Methods","x":"conversion in both directions. say word_from_int(\"108\".to_int()) say format_num(\"2.5\".to_num()) say char_to_word(70) } Method Signature Notes `.length()` `-> int` Character count `.trim()` `-> word` Strips leading and trailing whitespace `.to_upper()` `-> word` `.to_lower()` `-> word` `.contains(needle)` `word -> bool` `.starts_with(prefix)` `word -> bool` `.ends_with(suffix)` `word -> bool` `.replace(old, new)` `word, word -> word` Replaces every occurrence `.substring(start, len)` `int, int -> word` Start index and **length**, not an end index `.char_at(i)` `int -> word` A one-character `word`, not a `char` type `.to_int()` `-> int` `.to_num()` `-> num` `.checksum()` `-> int` Runtime hash"},{"p":"words","t":"Words & interpolation","a":"methods","h":"Methods","x":"`.repeated(n)` `int -> word` The word repeated `n` times `.substring(start, len)` takes a **length** as its second argument. The bible describes `slice(from, to)`; V3's builtin is not that function. `\"abcdef\".substring(1, 3)` yields `\"bcd\"`."},{"p":"words","t":"Words & interpolation","a":"snapshot-helpers","h":"Snapshot helpers","x":"Six further `word` methods exist for the compiler's own line-and-field serialisation format. They are part of the builtin table and will type-check in your code, but they are internal plumbing rather than a general-purpose API: `.snapshot_escape()`, `.snapshot_unescape()`, `.snapshot_line_count()`, `.snapshot_line(i)`, `.snapshot_lines()`, `.snapshot_field_count()`, `.snapshot_field_raw(i)`."},{"p":"words","t":"Words & interpolation","a":"concatenation-and-conversion","h":"Concatenation and conversion","x":"say \"Muv\" + \"-\" + \"Luv\" -- operator say word_concat(\"XM\", \"3\") -- builtin, identical `+` on two words concatenates. `+` on a word and a number is a type error \u2014 convert first: pilot n: int = 42 say \"answer: \" + word_from_int(n) say \"answer: {n}\" -- or interpolate Builtin Signature `word_from_int(i)` `int -> word` `word_from_bool(b)` `bool -> word` `word_to_int(w)` `word -> int` `word_concat(a, b)` `word, word -> word` `word_join(handle)` `int -> word` `char_to_word(code)` `int -> word` `format_num(f)` `num -> word` `parse_num(w)` `word -> num` `format_num` prints a `num` without a trailing `.0` when it is integral \u2014 `format_num(12.0)` gives `12`."},{"p":"words","t":"Words & interpolation","a":"indexing","h":"Indexing","x":"`word[i]` yields a one-character `word`. It is the only indexable type in V3. pilot s: word = \"abc\" say s[1] -- b say s.char_at(1) -- b, identical"},{"p":"words","t":"Words & interpolation","a":"interpolation","h":"Interpolation","x":"`{path}` inside a double-quoted word substitutes a binding's value. -- `{path}` inside a double-quoted word interpolates a binding. -- A path is an identifier plus zero or more `.field` hops, and must -- resolve to word, int, num or bool. Anything that is not a valid path -- stays literal text. -- (`Aviator`, not `Pilot`: V3 keywords are case-insensitive, so `Pilot` -- is the `pilot` keyword and cannot name a shape.) shape Aviator { name: word score: int } impl Aviator { task summary(self) -> word { give back \"{self.name} scored {self.score}\" } } task main() -> void { pilot callsign: word = \"Valkyrie\" pilot sorties: int = 12 pilot rate: num = 0.75 pilot active: bool = true say"},{"p":"words","t":"Words & interpolation","a":"interpolation","h":"Interpolation","x":"\"callsign={callsign} sorties={sorties}\" say \"rate={rate} active={active}\" pilot p: Aviator = Aviator { name: \"Meiya\", score: 98 } say \"shape: {p.name} / {p.score}\" say p.summary() -- Not a path, so it is printed literally: say \"literal {1 + 2} braces\" } The rules are exact: A **path** is an identifier followed by zero or more `.field` hops: `name`, `p.score`, `self.engine.thrust`. The path must resolve to `word`, `int`, `num` or `bool`. Any other type is a type error. Every segment must be a valid identifier \u2014 so it cannot be a keyword, in any casing. A matched `{...}` whose body is **not** a valid path is left as literal text, braces included. `\"literal {1 + 2} braces\"` prints exactly"},{"p":"words","t":"Words & interpolation","a":"interpolation","h":"Interpolation","x":"that. An unmatched `{` is literal text. Unknown bindings are a compile error: *\"unknown interpolation binding 'x'\"*. Interpolation substitutes **paths only** \u2014 never calls, never expressions. `\"{p.sum()}\"` is not a call; it is literal text, because `sum()` is not a valid path segment. Compute into a binding first: ```fk pilot total: int = p.sum() say \"sum={total}\" ``` Dotted shape interpolation, including `{self.field}` inside a method, is verified working on the LLVM backend. It is **not** a claimed executable path on the C backend."},{"p":"words","t":"Words & interpolation","a":"building-words-incrementally","h":"Building words incrementally","x":"Repeated `+` concatenation copies the whole accumulated word each time. `word_builder::*` avoids that. The builder is an `int` handle, not a shape. -- `word_builder::*` accumulates a word without the quadratic cost of -- repeated `+` concatenation. The builder is an `int` handle. task main() -> void { pilot b: int = word_builder::new() word_builder::append(b, \"Mission: \") word_builder::append(b, \"Valkyries\") word_builder::append_char(b, 32) -- a space, by codepoint word_builder::append_int(b, 4) say word_from_int(word_builder::length(b)) -- `finish` consumes the builder and returns the accumulated word. say word_builder::finish(b) -- with_capacity pre-allocates; clear reuses; discard frees"},{"p":"words","t":"Words & interpolation","a":"building-words-incrementally","h":"Building words incrementally","x":"unused. pilot c: int = word_builder::with_capacity(64) word_builder::append(c, \"scratch\") word_builder::clear(c) word_builder::append(c, \"reused\") say word_builder::finish(c) } Call Signature Notes `word_builder::new()` `-> int` `word_builder::with_capacity(n)` `int -> int` Pre-allocate `word_builder::reserve(b, n)` `int, int -> void` Grow the capacity `word_builder::append(b, w)` `int, word -> void` `word_builder::append_char(b, code)` `int, int -> void` By codepoint `word_builder::append_int(b, n)` `int, int -> void` `word_builder::length(b)` `int -> int` `word_builder::capacity(b)` `int -> int` `word_builder::clear(b)` `int -> void` Keep the buffer, drop the content"},{"p":"words","t":"Words & interpolation","a":"building-words-incrementally","h":"Building words incrementally","x":"`word_builder::finish(b)` `int -> word` Consumes the builder `word_builder::discard(b)` `int -> void` Free without producing a word `finish` consumes the builder \u2014 do not reuse the handle afterwards. Use `clear` to keep building with the same allocation, and `discard` when you are abandoning it without producing a word. This is V3's answer to the bible's `WordBuilder` from \u00a77.2. The API is namespaced calls over a handle rather than methods on an object."},{"p":"words","t":"Words & interpolation","a":"standard-library-word-tasks","h":"Standard-library word tasks","x":"`std/string.fk` is ordinary FREAK source linked in by `freak build` and `freak run`. Its tasks are global and take the word as a normal argument. -- std/string.fk ships as plain FREAK source and is always linked in by -- `freak build` / `freak run`. Its tasks are global; there is no import. task main() -> void { say string_repeat(\"ha\", 3) say string_reverse(\"FREAK\") say word_from_int(string_count(\"banana\", \"an\")) say string_pad_left(\"7\", 4, \"0\") say string_pad_right(\"7\", 4, \".\") say word_from_int(string_index_of(\"alternative\", \"native\")) say word_from_bool(is_digit(\"5\")) say word_from_bool(is_alpha(\"x\")) say word_from_bool(is_whitespace(\" \")) } Task Signature `string_repeat(s, count)`"},{"p":"words","t":"Words & interpolation","a":"standard-library-word-tasks","h":"Standard-library word tasks","x":"`word, int -> word` `string_reverse(s)` `word -> word` `string_count(haystack, needle)` `word, word -> int` `string_index_of(s, needle)` `word, word -> int`, `-1` when absent `string_pad_left(s, width, pad)` `word, int, word -> word` `string_pad_right(s, width, pad)` `word, int, word -> word` `string_split(s, delim)` `word, word -> word` (an encoded word, not an array) `string_join(parts, sep)` `word, word -> word` `string_trim(s)` `word -> word` `string_substring(s, start, end)` `word, int, int -> word` \u2014 an **end index**, unlike the builtin `string_replace(s, old, new)` `word, word, word -> word` `string_contains` / `string_starts_with` / `string_ends_with` `-> bool` `is_digit(c)` /"},{"p":"words","t":"Words & interpolation","a":"standard-library-word-tasks","h":"Standard-library word tasks","x":"`is_alpha(c)` / `is_whitespace(c)` `word -> bool` These duplicate several builtin methods. The builtins are cheaper and work under `freak check`; the `std/` tasks exist because the standard library is itself written in FREAK. Where the two disagree \u2014 `substring` \u2014 prefer the builtin and mind the length-vs-end-index difference."},{"p":"words","t":"Words & interpolation","a":"splitting-into-an-array","h":"Splitting into an array","x":"`string_split` returns an encoded word, not an array handle. To get a real array, walk the characters yourself: -- A complete small program: split a sentence into words, count them, -- and report the longest one. Uses shapes, arrays, loops and std tasks. shape Report { total: int longest: word } impl Report { task show(self) -> word { give back \"total={self.total} longest={self.longest}\" } } task split_words(text: word, out: int) -> void { pilot mut current: word = \"\" pilot mut i: int = 0 repeat text.length() times { pilot c: word = text.char_at(i) if c == \" \" { if current.length() > 0 { array_push(out, current) current = \"\" } } else { current = current + c } i += 1 } if current.length() >"},{"p":"words","t":"Words & interpolation","a":"splitting-into-an-array","h":"Splitting into an array","x":"0 { array_push(out, current) } } task analyse(text: word) -> Report { pilot words: int = array_new() split_words(text, words) pilot mut longest: word = \"\" pilot mut i: int = 0 repeat array_len(words) times { pilot w: word = array_get(words, i) if w.length() > longest.length() { longest = w } i += 1 } -- `result` is a reserved word in V3, so the binding is `summary`. pilot summary: Report = Report { total: array_len(words), longest: longest } array_release(words) give back summary } task main() -> void { pilot r: Report = analyse(\"the beta will not negotiate with humanity\") say r.show() say word_from_int(r.total) say r.longest.to_upper() }"},{"p":"shapes","t":"Shapes & impl","a":"shapes-impl","h":"Shapes & impl","x":"> `shape` is the only user-defined type in V3. `impl` attaches methods to it."},{"p":"shapes","t":"Shapes & impl","a":"declaring-a-shape","h":"Declaring a shape","x":"shape Name { field: type field: type } Fields are `name: type`, where the type is `int`, `num`, `word`, `bool`, or another shape's name. Separators are optional: newlines, commas, or a trailing comma before `}` all work. A shape field may **not** hold a `List<T>`, even though the annotation parses everywhere else: ```text type error: V3 owned shape fields do not yet support List values ``` Keep the list in a separate binding, or store a legacy `int` array handle in the field instead. `List<T>` *is* allowed as a task parameter and return type. -- `shape` declares a record type. Each field is `name: type`, -- where the type is a single identifier. shape Vector2 { x: int y: int } shape Squad {"},{"p":"shapes","t":"Shapes & impl","a":"declaring-a-shape","h":"Declaring a shape","x":"callsign: word members: int ready: bool } task main() -> void { pilot v: Vector2 = Vector2 { x: 3, y: 4 } say word_from_int(v.x) say word_from_int(v.y) -- Fields are assignable. v.x = 10 say word_from_int(v.x) pilot s: Squad = Squad { callsign: \"Valkyries\", members: 4, ready: true } say s.callsign say word_from_int(s.members) say word_from_bool(s.ready) }"},{"p":"shapes","t":"Shapes & impl","a":"construction","h":"Construction","x":"pilot v: Vector2 = Vector2 { x: 3, y: 4 } Every field must be supplied, by name, in any order. Field labels are checked against the declaration; an unknown or missing field is a compile error. The parser only treats `Name { ... }` as a constructor when `Name` is already a **registered shape**. A shape must therefore be declared before it is constructed in the token stream. Declaring shapes at the top of the file, as every example here does, keeps this from ever mattering."},{"p":"shapes","t":"Shapes & impl","a":"field-access-and-assignment","h":"Field access and assignment","x":"say word_from_int(v.x) v.x = 10 Compound assignment works on fields too: `v.x += 1`. Access chains through nested shapes, including inside string interpolation. -- Shapes may hold other shapes. Field access chains through them, -- including inside string interpolation. shape Engine { thrust: int } shape Tsf { name: word engine: Engine } impl Tsf { task report(self) -> word { give back \"{self.name} thrust={self.engine.thrust}\" } } task main() -> void { pilot unit: Tsf = Tsf { name: \"Takemikazuchi\", engine: Engine { thrust: 880 } } say unit.name say word_from_int(unit.engine.thrust) say unit.report() }"},{"p":"shapes","t":"Shapes & impl","a":"methods","h":"Methods","x":"`impl Shape { ... }` may contain only `task` declarations. -- `impl Shape { ... }` attaches methods. -- A method whose first parameter is `self` is an instance method, -- called as `value.method(args)`. -- A method without `self` is an associated method, -- called as `Shape::method(args)`. shape Rect { w: int h: int } impl Rect { task square(side: int) -> Rect { give back Rect { w: side, h: side } } task area(self) -> int { give back self.w * self.h } task perimeter(self) -> int { give back 2 * (self.w + self.h) } task scaled(self, factor: int) -> Rect { give back Rect { w: self.w * factor, h: self.h * factor } } } task main() -> void { pilot r: Rect = Rect { w: 3, h: 5 } say"},{"p":"shapes","t":"Shapes & impl","a":"methods","h":"Methods","x":"word_from_int(r.area()) say word_from_int(r.perimeter()) pilot big: Rect = r.scaled(3) say word_from_int(big.area()) pilot sq: Rect = Rect::square(4) say word_from_int(sq.area()) } The receiver decides the call form: Declaration Kind Call `task area(self) -> int` instance method `value.area()` `task scaled(self, f: int) -> Rect` instance method with args `value.scaled(3)` `task square(side: int) -> Rect` associated method `Rect::square(4)` `self` is written bare \u2014 never `self: Rect`. The compiler substitutes the owning shape's type once the `impl` target is known, so `self.field` resolves properly and duplicate field/method names across shapes do not collide. Methods are compiled to a flat"},{"p":"shapes","t":"Shapes & impl","a":"methods","h":"Methods","x":"global task named `Shape_method`. This is why two shapes may both define `area` without conflict, and why the checker tracks `impl` provenance separately: a free task literally named `Rect_area` is **not** accepted as proof that `Rect.area` exists."},{"p":"shapes","t":"Shapes & impl","a":"operator-doctrines","h":"Operator doctrines","x":"V3 accepts `impl Doctrine for Shape` and records the doctrine name as provenance. The methods become callable exactly like any other method. -- `impl Doctrine for Shape` registers an operator implementation. -- V3 does not accept a `doctrine` *declaration*, but it does accept -- this impl form for the built-in operator doctrines, and the method -- is callable by name. shape Vec2 { x: int y: int } impl Add for Vec2 { task add(self, other: Vec2) -> Vec2 { give back Vec2 { x: self.x + other.x, y: self.y + other.y } } } impl Vec2 { task show(self) -> word { give back \"({self.x}, {self.y})\" } } task main() -> void { pilot a: Vec2 = Vec2 { x: 1, y: 2 } pilot b: Vec2 = Vec2 { x: 10, y: 20 } pilot"},{"p":"shapes","t":"Shapes & impl","a":"operator-doctrines","h":"Operator doctrines","x":"c: Vec2 = a.add(b) say c.show() } **The operator token is not rewritten.** Implementing `Add` does not make `+` work on your shape: ```text type error: operator '+' does not accept Vec2 and Vec2 ``` Call the method by name \u2014 `a.add(b)`. The bible's `pilot v = v1 + v2` dispatch is V4. The doctrine name is **not validated**. `impl Whatever for Vec2 { ... }` compiles cleanly \u2014 the name is recorded and otherwise ignored. There is no check that the doctrine exists, that its methods are all implemented, or that signatures match. Use `Add`, `Sub`, `Mul`, `Div`, `Neg`, `Eq` by convention, and expect V4 to start enforcing them. `doctrine` **declarations** do not parse at all: error: unexpected"},{"p":"shapes","t":"Shapes & impl","a":"operator-doctrines","h":"Operator doctrines","x":"'doctrine' \u2014 this token cannot start an expression So a doctrine is never a real contract in V3, and there is no generic bound (`task f<T: Displayable>(...)`), no `dyn Doctrine`, and no dynamic dispatch."},{"p":"shapes","t":"Shapes & impl","a":"shapes-as-values","h":"Shapes as values","x":"Shapes live in runtime storage and are handled by reference. Two consequences: Passing a shape to a task and mutating a field inside is **visible to the caller**. There is no implicit copy. Assigning one shape binding to another aliases the same storage. To get an independent value, construct a new one \u2014 a small `copy` associated method is the usual idiom: impl Rect { task copy_of(self) -> Rect { give back Rect { w: self.w, h: self.h } } } Under `--strict-borrow` the Phase-1 checker additionally treats user shapes as single-owner move types, so a use after move is reported."},{"p":"shapes","t":"Shapes & impl","a":"backend-support","h":"Backend support","x":"Shape construction, field access, method execution and dotted `{shape.field}` interpolation are verified executable evidence on the **LLVM backend only**. The C backend (`--c`) may transpile shape declarations, but packaged shape runtime storage is explicitly not a claimed executable path. If your program declares a shape, build it with the default LLVM backend."},{"p":"shapes","t":"Shapes & impl","a":"what-shapes-cannot-do","h":"What shapes cannot do","x":"Feature Status Generic fields `shape Pair<A, B>` V4 Field default values V4 `variant` sum types V4 `alias` type aliases V4 `doctrine` declarations V4 Doctrine bounds on generics V4 `dyn Doctrine` and vtables V4 Real operator dispatch through `Add` etc. V4 Field visibility / `launch` on fields V4 `@layout(C)`, `@repr(u32)` V4 `Shared<T>` / `Weak<T>` V4 A destructor or `drop` hook V4"},{"p":"arrays","t":"Lists & arrays","a":"lists-arrays","h":"Lists & arrays","x":"> V3 has two collection surfaces: a typed `List<T>`, and an older untyped handle. Know which one you are holding. This is the one part of V3 with genuine overlap, because `List<T>` was added on top of a pre-existing untyped array. Both still work, and they are not interchangeable everywhere. `List<T>` Legacy handle Type `List<int>`, `List<word>`, `List<Shape>`, \u2026 `int` Created by `[a, b, c]`, `List::filled(v, n)` `array_new()` Element type Checked Always `word` Length `list.length()` `array_len(h)` Read `list[i]` `array_get(h, i)` Write `list[i] = v` (needs `pilot mut`) `array_set(h, i, v)` Append \u2014 `array_push(h, v)` Accepted by `array_*` builtins yes, when `List<word>` yes Accepted by"},{"p":"arrays","t":"Lists & arrays","a":"lists-arrays","h":"Lists & arrays","x":"`std/algorithm.fk` tasks **no** yes"},{"p":"arrays","t":"Lists & arrays","a":"the-list-type","h":"The List type","x":"The element type must be a scalar \u2014 `int`, `num`, `word`, `bool` \u2014 or the name of a shape. There is no nesting: `List<List<int>>` does not parse. -- `List<T>` is V3's typed sequence. The element type must be a scalar -- (`int`, `num`, `word`, `bool`) or the name of a shape. Nesting is not -- allowed: there is no `List<List<int>>`. shape Contact { tag: word } -- A List may be a parameter and a return type. It may NOT be a shape -- field: that reports \"V3 owned shape fields do not yet support List -- values\". task count_all(items: List<word>) -> int { give back items.length() } task first_three() -> List<int> { give back [1, 2, 3] } task main() -> void { -- An array literal infers List<T>"},{"p":"arrays","t":"Lists & arrays","a":"the-list-type","h":"The List type","x":"from its elements. pilot names: List<word> = [\"Takeru\", \"Meiya\", \"Sumika\"] say word_from_int(names.length()) say names[1] -- Indexed assignment requires `pilot mut`. pilot mut scores: List<int> = [10, 20, 30] scores[0] = 99 say word_from_int(scores[0]) -- Every scalar element type works. pilot ratios: List<num> = [1.5, 2.5] say format_num(ratios[1]) pilot flags: List<bool> = [true, false] say word_from_bool(flags[0]) -- Shapes too, and field access chains off the element. pilot contacts: List<Contact> = [Contact { tag: \"BETA-1\" }] say contacts[0].tag -- List::filled(value, count) builds a pre-populated list. pilot zeros: List<int> = List::filled(0, 4) say word_from_int(zeros.length()) --"},{"p":"arrays","t":"Lists & arrays","a":"the-list-type","h":"The List type","x":"Passed to a task, and returned from one. say word_from_int(count_all(names)) say word_from_int(first_three()[2]) -- Iterate with an index; there is no `for each`. pilot mut i: int = 0 repeat names.length() times { say names[i] i += 1 } } Rules worth pinning down: **`[a, b, c]` infers `List<T>`** from its elements, all of which must share one type. Numbers stay numbers; no conversion to `word` is needed. **`.length()` is a method**, not a field. `list.length` reports *\"non-shape value has no fields\"*. **Indexed assignment needs `pilot mut`.** Otherwise you get *\"indexed assignment requires a mutable list binding; declare it with pilot mut\"*. **`List::filled(value, count)`** builds a"},{"p":"arrays","t":"Lists & arrays","a":"the-list-type","h":"The List type","x":"pre-populated list and takes its element type from `value`. Indexing a shape element chains: `contacts[0].tag`. There is still no `push`, `pop`, `insert`, `remove`, `sort` or iterator on `List<T>`. It is a fixed-shape, indexable sequence. To build one incrementally, use the legacy handle, or size it with `List::filled` and assign by index."},{"p":"arrays","t":"Lists & arrays","a":"where-a-list-may-appear","h":"Where a List may appear","x":"Position Allowed `pilot` / `fixed pilot` binding yes Task parameter yes Task return type yes **Shape field** **no** A shape field cannot hold a list: ```text type error: V3 owned shape fields do not yet support List values ``` Store a legacy `int` array handle in the field instead, or keep the list in a separate binding beside the shape. `List<T>` is newer than most of V3 and the surface is thin. Treat it as the right choice for *holding* typed data, and the legacy handle as the right choice for *building* and for reaching the `std/algorithm.fk` helpers."},{"p":"arrays","t":"Lists & arrays","a":"the-legacy-handle","h":"The legacy handle","x":"`array_new()` returns an `int` handle to a runtime array whose elements are always `word`. Store other types by converting them in and out. -- V3 arrays are runtime handles of `word` elements, addressed by an -- `int` handle. Store other types by converting them to `word`. task main() -> void { pilot xs: int = array_new() array_push(xs, \"alpha\") array_push(xs, \"bravo\") array_push(xs, \"charlie\") say word_from_int(array_len(xs)) say array_get(xs, 0) say array_get(xs, 2) array_set(xs, 1, \"BRAVO\") say array_get(xs, 1) -- Iterate with an index and a counted loop. pilot mut i: int = 0 repeat array_len(xs) times { say array_get(xs, i) i += 1 } array_release(xs) } Call Signature `array_new()` `->"},{"p":"arrays","t":"Lists & arrays","a":"the-legacy-handle","h":"The legacy handle","x":"int` `array_push(h, value)` `word-array, word -> void` `array_get(h, i)` `word-array, int -> word` `array_set(h, i, value)` `word-array, int, word -> void` `array_len(h)` `word-array -> int` `array_release(h)` `word-array -> void` `word_join(h)` `int -> word` `word-array` is a checker-internal marker meaning \"either an `int` handle or a `List<word>`\" \u2014 it is not a type you can write in source. That is why the `array_*` builtins accept both, while a user-declared task with `handle: int` accepts only the handle."},{"p":"arrays","t":"Lists & arrays","a":"mixing-the-two","h":"Mixing the two","x":"-- An array literal infers `List<T>` from its elements. Numbers stay -- numbers -- there is no need to convert them to `word` any more. -- -- The older untyped `array_*` handle still exists alongside it; see -- arrays.html for when each one applies. task main() -> void { pilot names: List<word> = [\"Takeru\", \"Meiya\", \"Sumika\"] say word_from_int(names.length()) say names[1] pilot numbers: List<int> = [3, 1, 2] say word_from_int(numbers.length()) say word_from_int(numbers[0]) -- The array_* builtins accept a List<word> as well as a raw handle. say word_from_int(array_len(names)) say array_get(names, 2) -- std/algorithm.fk tasks are declared with `handle: int`, so they take -- only the legacy"},{"p":"arrays","t":"Lists & arrays","a":"mixing-the-two","h":"Mixing the two","x":"handle. Build one with array_new() to use them. pilot h: int = array_new() array_push(h, word_from_int(3)) array_push(h, word_from_int(1)) array_push(h, word_from_int(2)) array_sort_int(h) say array_join(h, \",\") say word_from_int(array_sum_int(h)) say word_from_int(array_max_int(h)) say word_from_int(array_binary_search_int(h, 3)) array_reverse(h) say array_join(h, \"|\") say word_from_bool(array_contains(h, \"2\")) array_release(h) } The rule that follows from the table above: **builtins bridge, library tasks do not.** pilot names: List<word> = [\"b\", \"a\"] say word_from_int(array_len(names)) -- fine: builtin, accepts word-array say array_get(names, 0) -- fine -- array_join is a task in"},{"p":"arrays","t":"Lists & arrays","a":"mixing-the-two","h":"Mixing the two","x":"std/algorithm.fk declared `handle: int`: -- say array_join(names, \",\") -- type error: call to 'array_join' argument 1 expects int, got List<word> If you need the library helpers, build a legacy handle with `array_new()`."},{"p":"arrays","t":"Lists & arrays","a":"iterating","h":"Iterating","x":"There is no `for each` in either surface. Keep an index and use a counted loop: pilot mut i: int = 0 repeat items.length() times { say items[i] i += 1 } `repeat N times` evaluates its count once, so growing the collection inside the loop will not extend the iteration."},{"p":"arrays","t":"Lists & arrays","a":"stdalgorithmfk-helpers","h":"std/algorithm.fk helpers","x":"Plain FREAK source, linked by `freak build` and `freak run` (not `freak check`). Every helper takes a **legacy `int` handle**. Task Signature Notes `array_sort_int(h)` `int -> void` In-place insertion sort, numeric order `array_binary_search_int(h, target)` `int, int -> int` Index or `-1`; needs a sorted array `array_reverse(h)` `int -> void` In place `array_find(h, target)` `int, word -> int` Index or `-1` `array_contains(h, target)` `int, word -> bool` `array_count(h, target)` `int, word -> int` `array_copy(h)` `int -> int` New handle, shallow copy `array_join(h, sep)` `int, word -> word` `array_sum_int(h)` `int -> int` `array_max_int(h)` / `array_min_int(h)` `int -> int` **Two of these"},{"p":"arrays","t":"Lists & arrays","a":"stdalgorithmfk-helpers","h":"std/algorithm.fk helpers","x":"are broken**, re-confirmed against the compiler that verified this site: - `array_sort_word(h)` **segfaults**. It routes through `extern task freak_word_compare(a: word, b: word) -> int`, and that declaration does not bridge the LLVM backend's `word` handle representation. See [extern & FFI](ffi.html#type-mapping). - `array_unique(h)` **returns an empty array**. To sort words, keep a parallel `int` key array and sort that, or write an insertion sort in your own code comparing with `.char_at()`."},{"p":"arrays","t":"Lists & arrays","a":"memory","h":"Memory","x":"`array_release(h)` frees a legacy array. V3 has no garbage collector and no automatic drop, so a long-running program that allocates in a loop must release. Do not use a handle after releasing it, and do not release twice. `List<T>` values have no explicit release call."},{"p":"arrays","t":"Lists & arrays","a":"passing-collections-to-tasks","h":"Passing collections to tasks","x":"A legacy handle is just an `int`, so it passes freely and the callee mutates the caller's array \u2014 the standard way to return a collection: task fill(out: int) -> void { array_push(out, \"alpha\") array_push(out, \"bravo\") } task main() -> void { pilot items: int = array_new() fill(items) say word_from_int(array_len(items)) array_release(items) } A `List<T>` can also be a parameter or return type, since `parser_take_type` accepts the annotation in both positions."},{"p":"arrays","t":"Lists & arrays","a":"maps-and-sets","h":"Maps and sets","x":"Still none. `Map<K,V>`, `Set<T>` and `Lineup<T>` do not exist, and the `{ \"k\": v }` map literal does not parse. For small lookups, use two parallel collections and `array_find`."},{"p":"console","t":"Coloured output","a":"coloured-output","h":"Coloured output","x":"> There is no colour library. You write ANSI escape sequences into `word` values yourself \u2014 so this page is the full code reference. FREAK has no `std::term`, no `Color` type and no formatting helpers. Colour works because two things are true: The lexer supports `\\xNN` byte escapes and passes them through to the backend untouched. Every compiled FREAK program enables ANSI processing at startup. That is enough. Colour constants are ordinary `word` bindings, concatenated with `+`. The compiler's own CLI is written this way, so it doubles as the reference implementation. -- Coloured console output. There is no colour library: you build the -- ANSI escape sequences yourself as `word` values and"},{"p":"console","t":"Coloured output","a":"coloured-output","h":"Coloured output","x":"concatenate them. -- -- `\\x1b` is the ESC byte. The V3 lexer preserves `\\xNN` escapes all the -- way through to the backend, so they reach the terminal intact. pilot C_RESET = \"\\x1b[0m\" pilot C_BOLD = \"\\x1b[1m\" pilot C_DIM = \"\\x1b[2m\" pilot C_RED = \"\\x1b[31m\" pilot C_GREEN = \"\\x1b[32m\" pilot C_CYAN = \"\\x1b[36m\" pilot C_BRED = \"\\x1b[1;31m\" -- 24-bit truecolour: ESC [ 38;2;R;G;B m pilot C_PINK = \"\\x1b[38;2;255;100;200m\" task rgb(r: int, g: int, b: int) -> word { give back \"\\x1b[38;2;\" + word_from_int(r) + \";\" + word_from_int(g) + \";\" + word_from_int(b) + \"m\" } task main() -> void { say C_BRED + \"error\" + C_RESET + \": engine failure\" say C_GREEN + \"ok\" + C_RESET + \": all systems nominal\" say"},{"p":"console","t":"Coloured output","a":"coloured-output","h":"Coloured output","x":"C_DIM + \"hint: check the coolant line\" + C_RESET say C_BOLD + C_CYAN + \"XM3\" + C_RESET + \" reflex link online\" say C_PINK + \"freak\" + C_RESET + \" 0.14.1\" say rgb(255, 180, 0) + \"amber warning\" + C_RESET }"},{"p":"console","t":"Coloured output","a":"anatomy-of-a-sequence","h":"Anatomy of a sequence","x":"Everything below is a **CSI** sequence \u2014 `ESC [` followed by parameters and a single final letter that selects the operation: \\x1b [ params <final> \u2502 \u2502 \u2514\u2500\u2500 the operation: `m` = set graphics, `H` = move cursor, \u2026 \u2502 \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 numeric parameters, `;` separated \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 ESC, decimal 27, written \\x1b in a FREAK word `\\x1b` is the only part that needs escaping; `[`, the digits, the semicolons and the final letter are ordinary characters. Sequences ending in `m` are **SGR** (Select Graphic Rendition) \u2014 colour and text style. Everything else moves the cursor or edits the screen. Multiple parameters combine in one sequence, which is cheaper and more reliable than emitting several: say"},{"p":"console","t":"Coloured output","a":"anatomy-of-a-sequence","h":"Anatomy of a sequence","x":"\"\\x1b[1;4;31m\" + \"bold underlined red\" + \"\\x1b[0m\" -- A tour of the SGR (Select Graphic Rendition) code space, to confirm -- every family survives the V3 lexer, both backends and the runtime. -- -- Form: ESC [ params m where ESC is \\x1b pilot RESET = \"\\x1b[0m\" task sgr(code: word) -> word { give back \"\\x1b[\" + code + \"m\" } task swatch(label: word, code: word) -> void { say sgr(code) + label + RESET } task main() -> void { -- Attributes swatch(\"bold\", \"1\") swatch(\"dim\", \"2\") swatch(\"italic\", \"3\") swatch(\"underline\", \"4\") swatch(\"blink\", \"5\") swatch(\"reverse\", \"7\") swatch(\"hidden\", \"8\") swatch(\"strike\", \"9\") -- Standard foreground 30-37 swatch(\"black\", \"30\") swatch(\"red\", \"31\")"},{"p":"console","t":"Coloured output","a":"anatomy-of-a-sequence","h":"Anatomy of a sequence","x":"swatch(\"green\", \"32\") swatch(\"yellow\", \"33\") swatch(\"blue\", \"34\") swatch(\"magenta\", \"35\") swatch(\"cyan\", \"36\") swatch(\"white\", \"37\") -- Bright foreground 90-97 swatch(\"bright red\", \"91\") swatch(\"bright green\", \"92\") swatch(\"bright cyan\", \"96\") -- Background 40-47 and bright 100-107 swatch(\"on red\", \"41\") swatch(\"on blue\", \"44\") swatch(\"on bright yellow\", \"103\") -- Combined parameters in one sequence swatch(\"bold + underline + red\", \"1;4;31\") swatch(\"white on magenta\", \"37;45\") -- 256-colour: 38;5;N foreground, 48;5;N background swatch(\"256 fg 208\", \"38;5;208\") swatch(\"256 bg 22\", \"48;5;22\") -- 24-bit truecolour: 38;2;R;G;B and 48;2;R;G;B swatch(\"rgb fg\", \"38;2;255;100;200\") swatch(\"rgb bg\","},{"p":"console","t":"Coloured output","a":"anatomy-of-a-sequence","h":"Anatomy of a sequence","x":"\"48;2;20;20;90\") -- Selective reset: 22 clears bold/dim, 24 clears underline, -- 39 resets foreground, 49 resets background. say sgr(\"1\") + \"bold \" + sgr(\"22\") + \"normal again\" + RESET say sgr(\"31\") + \"red \" + sgr(\"39\") + \"default again\" + RESET } That output block is the program's real bytes, re-rendered as HTML. Two swatches look odd on purpose: **blink** appears unstyled, because this page does not animate it and many terminals ignore it too; and **hidden** appears blank, because that is what `8` does. Non-SGR sequences are dropped from the rendering rather than shown as garbage."},{"p":"console","t":"Coloured output","a":"sgr-attributes","h":"SGR: attributes","x":"Each attribute has a matching off-switch, so you can turn one thing off without resetting everything. On Off Effect `0` \u2014 Reset all attributes and colours `1` `22` Bold / increased intensity `2` `22` Dim / faint `3` `23` Italic `4` `24` Underline `5` `25` Slow blink `6` `25` Rapid blink \u2014 rarely implemented `7` `27` Reverse video (swap foreground and background) `8` `28` Conceal / hidden `9` `29` Crossed out / strikethrough `21` `24` Double underline \u2014 sometimes \"bold off\" instead `51` `54` Framed `52` `54` Encircled `53` `55` Overlined Note that `22` clears **both** bold and dim, since they share an intensity channel."},{"p":"console","t":"Coloured output","a":"sgr-foreground-colour","h":"SGR: foreground colour","x":"Code Colour Code Bright variant `30` Black `90` Bright black (grey) `31` Red `91` Bright red `32` Green `92` Bright green `33` Yellow `93` Bright yellow `34` Blue `94` Bright blue `35` Magenta `95` Bright magenta `36` Cyan `96` Bright cyan `37` White `97` Bright white `39` **Default foreground**"},{"p":"console","t":"Coloured output","a":"sgr-background-colour","h":"SGR: background colour","x":"Code Colour Code Bright variant `40` Black `100` Bright black (grey) `41` Red `101` Bright red `42` Green `102` Bright green `43` Yellow `103` Bright yellow `44` Blue `104` Bright blue `45` Magenta `105` Bright magenta `46` Cyan `106` Bright cyan `47` White `107` Bright white `49` **Default background**"},{"p":"console","t":"Coloured output","a":"sgr-extended-colour","h":"SGR: extended colour","x":"`38` sets the foreground and `48` the background, each with two sub-forms selected by the next parameter. Sequence Meaning `\\x1b[38;5;Nm` Foreground from the 256-colour palette, `N` = 0\u2013255 `\\x1b[48;5;Nm` Background from the 256-colour palette `\\x1b[38;2;R;G;Bm` Foreground, 24-bit truecolour, each channel 0\u2013255 `\\x1b[48;2;R;G;Bm` Background, 24-bit truecolour `\\x1b[58;5;Nm` Underline colour \u2014 an extension, patchy support `\\x1b[59m` Default underline colour The 256-colour index is laid out in three blocks: Range Contents `0`\u2013`7` The standard colours, same as `30`\u2013`37` `8`\u2013`15` The bright colours, same as `90`\u2013`97` `16`\u2013`231` A 6\u00d76\u00d76 RGB cube `232`\u2013`255` 24 greys, dark to light The cube index"},{"p":"console","t":"Coloured output","a":"sgr-extended-colour","h":"SGR: extended colour","x":"is `16 + 36*r + 6*g + b`, where each of `r`, `g`, `b` is 0\u20135: task cube(r: int, g: int, b: int) -> word { give back \"\\x1b[38;5;\" + word_from_int(16 + 36 * r + 6 * g + b) + \"m\" } Truecolour is the simplest option when you know the exact shade you want: task rgb(r: int, g: int, b: int) -> word { give back \"\\x1b[38;2;\" + word_from_int(r) + \";\" + word_from_int(g) + \";\" + word_from_int(b) + \"m\" }"},{"p":"console","t":"Coloured output","a":"cursor-and-screen-control","h":"Cursor and screen control","x":"These are CSI sequences with a final letter other than `m`. They pass through V3 exactly the same way \u2014 they are just bytes \u2014 but they act on the terminal rather than on the text, so they cannot be shown in a captured output block. Sequence Effect `\\x1b[nA` Cursor up `n` rows `\\x1b[nB` Cursor down `n` rows `\\x1b[nC` Cursor forward `n` columns `\\x1b[nD` Cursor back `n` columns `\\x1b[nE` Cursor to start of line, `n` rows down `\\x1b[nF` Cursor to start of line, `n` rows up `\\x1b[nG` Cursor to column `n` `\\x1b[row;colH` Cursor to an absolute position, 1-based `\\x1b[nJ` Erase display \u2014 `0` to end, `1` to start, `2` all, `3` all plus scrollback `\\x1b[nK` Erase line \u2014 `0` to end, `1` to start, `2`"},{"p":"console","t":"Coloured output","a":"cursor-and-screen-control","h":"Cursor and screen control","x":"whole line `\\x1b[nS` Scroll up `n` lines `\\x1b[nT` Scroll down `n` lines `\\x1b[s` Save cursor position `\\x1b[u` Restore saved cursor position `\\x1b[?25l` Hide the cursor `\\x1b[?25h` Show the cursor `\\x1b[?1049h` Switch to the alternate screen buffer `\\x1b[?1049l` Return to the main screen buffer A progress line, using carriage return plus erase-to-end: task main() -> void { pilot mut i: int = 0 repeat 5 times { i += 1 say \"\\r\\x1b[K\" + \"working \" + word_from_int(i) + \"/5\" } say \"\" } `say` always appends a newline, so a true in-place progress indicator is not possible with it \u2014 each call moves to the next line regardless. The snippet above redraws on successive lines. V3 has no unbuffered"},{"p":"console","t":"Coloured output","a":"cursor-and-screen-control","h":"Cursor and screen control","x":"write, so if you need real single-line progress you must go through the C runtime."},{"p":"console","t":"Coloured output","a":"ansi-is-enabled-for-you","h":"ANSI is enabled for you","x":"On Windows, escapes only work when the console has `ENABLE_VIRTUAL_TERMINAL_PROCESSING` set. The runtime does this for **every** FREAK program, not just the CLI: `freak_enable_ansi()` in `freakc/runtime/freak_runtime.c` sets the flag on both stdout and stderr. The C backend emits a direct `freak_enable_ansi();` call into `main`. The LLVM backend calls it via `freak_llvm_setup_args`, which the emitted `main` invokes with `argc`/`argv`. So nothing platform-specific is needed on your side. On POSIX the call is a no-op."},{"p":"console","t":"Coloured output","a":"the-escape-itself","h":"The escape itself","x":"`\\x1b` is one of a small set of byte escapes the lexer understands: Escape Byte `\\n` newline `\\r` carriage return `\\t` tab `\\\"` double quote `\\\\` backslash `\\xNN` any byte, two hex digits The lexer's hex branch re-emits `\\xNN` as that literal two-character sequence specifically so the C and LLVM emitters pass it straight into the generated string constant. Nothing in the pipeline interprets it. `\\x00` is rejected at lex time \u2014 *\"embedded NUL escape is not supported\"* \u2014 because V3 words are NUL-terminated at runtime. A malformed hex escape (fewer than two hex digits) is also a lex error."},{"p":"console","t":"Coloured output","a":"the-clis-palette","h":"The CLI's palette","x":"`src/cli/version.fk` declares its whole palette as top-level `pilot` bindings. That file is plain FREAK compiled by V3, so it is a worked example you can copy. Group Constants Styles `C_RESET` `C_BOLD` `C_DIM` `C_ITALIC` `C_ULINE` `C_BLINK` `C_STRIKE` Standard `C_RED` `C_GREEN` `C_YELLOW` `C_BLUE` `C_MAGENTA` `C_CYAN` `C_WHITE` Bold `C_BRED` `C_BGREEN` `C_BYELLOW` `C_BBLUE` `C_BMAGENTA` `C_BCYAN` `C_BWHITE` Truecolour gradient `C_G1`\u2026`C_G6` \u2014 pink \u2192 purple \u2192 blue \u2192 cyan Background `C_BG_DARK` = `\\x1b[48;2;20;20;30m` The gradient is what produces the banner from `freak help`: pilot C_G1 = \"\\x1b[38;2;255;100;200m\" pilot C_G2 = \"\\x1b[38;2;220;80;220m\" pilot C_G3 = \"\\x1b[38;2;180;70;240m\""},{"p":"console","t":"Coloured output","a":"box-drawing-and-symbols","h":"Box drawing and symbols","x":"The same file declares Unicode decorations as raw UTF-8 **byte** escapes, because V3 has no `char` type and no `\\u` escape \u2014 you spell out the encoding: pilot BOX_TL = \"\\xe2\\x95\\xad\" -- U+256D round corner pilot BOX_H = \"\\xe2\\x94\\x80\" -- U+2500 horizontal pilot BOX_V = \"\\xe2\\x94\\x82\" -- U+2502 vertical pilot SYM_CHECK = \"\\xe2\\x9c\\x93\" -- U+2713 check mark `.length()` counts characters, not bytes, so a box-drawing character is one character even though it is three bytes. Padding maths built on `.length()` stays correct."},{"p":"console","t":"Coloured output","a":"respecting-the-users-terminal","h":"Respecting the user's terminal","x":"The CLI honours the `NO_COLOR` convention in `cli_configure_output()`. Nothing does this for you automatically \u2014 the runtime enables ANSI, it does not decide whether you *should* use it. Copy the pattern: pilot mut C_RESET = \"\\x1b[0m\" pilot mut C_BRED = \"\\x1b[1;31m\" task configure_output() -> void { if process::env(\"NO_COLOR\") != \"\" { C_RESET = \"\" C_BRED = \"\" } } task main() -> void { configure_output() say C_BRED + \"error\" + C_RESET + \": no colour when NO_COLOR is set\" } Setting the constants to the empty word is exactly what `cli_disable_colors()` does \u2014 every call site keeps working and concatenates nothing. The CLI checks four variables: Variable Effect in the CLI `NO_COLOR` Any"},{"p":"console","t":"Coloured output","a":"respecting-the-users-terminal","h":"Respecting the user's terminal","x":"non-empty value disables colour `FREAK_NO_COLOR` Truthy value disables colour `FREAK_ASCII` Force ASCII box drawing and symbols `FREAK_UNICODE` Force Unicode even when it would otherwise degrade On Windows without an explicit override it degrades decorations to ASCII unless it detects a modern terminal via `WT_SESSION`, `TERM_PROGRAM`, `ANSICON` or `ConEmuANSI`. There is no TTY detection anywhere. Neither the CLI nor your program can tell whether stdout is a pipe, so escapes are emitted even when output is redirected. That is why tooling around FREAK \u2014 including this site's verification harness \u2014 has to strip ANSI from captured compiler output."},{"p":"console","t":"Coloured output","a":"the-compilers-own-diagnostics","h":"The compiler's own diagnostics","x":"V3 hardcodes red for errors rather than using the palette, because the compiler files are concatenated *ahead* of the CLI's constants: say \"\\x1b[1;31merror\\x1b[0m: \" + msg That line is in `src/compiler/v3/helpers.fk`, with variants in `parser.fk` (the >100-error bail-out) and `main.fk` (the abort summary). The caret line under a diagnostic is coloured the same way."},{"p":"console","t":"Coloured output","a":"support-and-practical-notes","h":"Support and practical notes","x":"**Always reset.** An unclosed sequence leaks into the shell prompt after your program exits. **Safe floor:** the 8 standard colours and `1`/`4`/`0`. Universally supported. **Widely safe:** bright colours, backgrounds, 256-colour, truecolour, `2`, `3`, `7`, `9`. **Patchy:** `5`/`6` blink (often ignored), `8` conceal, `21` double underline, `51`\u2013`55` framed/encircled/overlined, `58` underline colour. **Combine parameters** in one sequence rather than emitting several. Colour goes through `say`, which writes to stdout. There is no stderr writer in V3, so diagnostics and normal output share one stream."},{"p":"anime-layer","t":"Anime layer","a":"anime-layer","h":"Anime layer","x":"> FREAK's signature feature set. Four pieces of it compile in V3; the rest is specified but unimplemented. The bible's Section 5 describes a large narrative-flavoured layer: foreshadowing debts, route types, honour-gated unsafe blocks, death-flag analysis, and `deus_ex_machina` miracles. V3 implements a small slice of it."},{"p":"anime-layer","t":"Anime layer","a":"what-compiles","h":"What compiles","x":"Construct Status in V3 `training arc until C max N sessions { }` **Works** \u2014 a real bounded loop `eventually { }` **Parses and emits**, but inline, not deferred `@annotation` **Parses**, then ignored. One per statement, no arguments `PLUS ULTRA`, `FINAL FORM`, `TSUNDERE`, `NAKAMA` **Work**, with different semantics from the spec Everything else in Section 5 fails at the parser."},{"p":"anime-layer","t":"Anime layer","a":"training-arc","h":"training arc","x":"The one anime construct that is genuinely useful and genuinely implemented. It is a loop with a mandatory session cap, so it cannot spin forever. -- `training arc` is a loop with a mandatory session cap. -- It stops when the condition becomes true OR when the cap is hit, -- so it cannot spin forever. task main() -> void { pilot mut power: int = 0 training arc until power >= 100 max 8 sessions { power += 20 } say word_from_int(power) -- The cap wins when the condition is never reachable. pilot mut stuck: int = 0 training arc until stuck > 1000 max 4 sessions { stuck += 1 } say word_from_int(stuck) } Full details in [Control flow](control-flow.html#training-arc)."},{"p":"anime-layer","t":"Anime layer","a":"eventually","h":"eventually","x":"-- `eventually { ... }` marks a cleanup block. -- -- IMPORTANT V3 caveat: V3 emits the block INLINE at the point it is -- written. It is not deferred to the end of scope and it does not run -- on `give back`, `break` or `panic`. Place it where you want it to run. task main() -> void { say \"engaging\" eventually { say \"cleanup\" } say \"disengaged\" } V3 emits the block **inline, at the point you wrote it**. It is not deferred to end of scope, it does not run in LIFO order with other `eventually` blocks, and it does not run on `give back`, `break` or `panic`. It is a labelled section, not `defer`. `eventually if cond { }` does not parse: `expected '{', found 'if'`."},{"p":"anime-layer","t":"Anime layer","a":"annotations","h":"Annotations","x":"V3's parser recognises `@` followed by one identifier, discards it, and carries on. No annotation has any semantic effect. -- V3 parses a bare `@name` annotation and then ignores it. -- It carries no semantics, and it must NOT take arguments: -- `@rival(meiya)` does not parse as an annotation in V3. @protagonist task hero() -> word { give back \"Shirogane Takeru\" } @deprecated task legacy() -> word { give back \"old path\" } @season_finale task main() -> void { say hero() say legacy() } Three hard limits: **No arguments.** `@rival(meiya)` breaks \u2014 the parser consumes `@rival`, then tries to parse `(meiya)` as the next statement and reports `unknown binding 'meiya'`. The same applies to"},{"p":"anime-layer","t":"Anime layer","a":"annotations","h":"Annotations","x":"`@experiment(\"...\")`, `@classified(\"...\")` and `@i_know_what_im_doing(\"...\")`. **One per statement.** Stacking two annotations fails with `unexpected '@'`. **No validation.** Any identifier is accepted; unknown names are not warned about, and `@season_finale` is not checked for uniqueness. So `@protagonist` grants no power level, `@nakige` requires no caller prefix, `@deprecated` produces no warning, and `@classified` redacts nothing."},{"p":"anime-layer","t":"Anime layer","a":"the-anime-operators","h":"The anime operators","x":"Four operators work, with V3-specific meanings. -- The anime operators that V3 actually implements. -- The three unary forms are PREFIX and accept `int` only. task main() -> void { pilot base: int = 6 say word_from_int(FINAL FORM base) -- base * base say word_from_int(PLUS ULTRA base) -- base * 2 say word_from_int(TSUNDERE base) -- 0 - base -- NAKAMA is infix, at the same precedence as + and -. say word_from_int(3 NAKAMA 4) } Operator V3 form V3 lowering `FINAL FORM x` prefix, `int` `x * x` `PLUS ULTRA x` prefix, `int` `x * 2` `TSUNDERE x` prefix, `int` `0 - x` `a NAKAMA b` infix, `int` `a + b` The specification puts all three unary forms in postfix position, allows `num` and `bool`, and"},{"p":"anime-layer","t":"Anime layer","a":"the-anime-operators","h":"The anime operators","x":"defines emotional scaling formulas. V3 does none of that \u2014 see [Operators](operators.html#the-anime-operators)."},{"p":"anime-layer","t":"Anime layer","a":"what-does-not-parse","h":"What does not parse","x":"Each of these lexes as a keyword and then fails in the parser with *\"this token cannot start an expression\"*: Construct Bible section `foreshadow pilot x = ...` / `payoff x` \u00a75.2 `route TrueRoute`, `check route`, `only on R from x` \u00a75.3 `deus_ex_machina \"20+ words\" { }` \u00a75.5 `isekai { } bringing back { }` \u00a75.7 `trust me \"reason\" on my honor as .level { }` \u00a74.5 `direct_order [arch] { asm }` \u00a74.5 `sadly call()` \u00a75.1 `for science, call()` \u00a75.1 `knowing this will hurt, call()` \u00a75.1 `training arc ... with growth` \u00a75.6 `prob[0.3] chance { }`, `prob_when` \u00a72.2 `declare x was v in timeline \"t\"` \u00a72.3 `mood`, `.chill` and mood arithmetic \u00a72.4 `knowing` is not even a keyword in V3's lexer \u2014 it parses"},{"p":"anime-layer","t":"Anime layer","a":"what-does-not-parse","h":"What does not parse","x":"as an ordinary identifier, and the error surfaces at the comma instead."},{"p":"anime-layer","t":"Anime layer","a":"the-audit-commands","h":"The audit commands","x":"The audit subcommands still exist and still work, because they are **text scanners running outside the compiler**: freak audit-science # every `for science,` call site freak audit-trust # every `trust me` block and its honor level freak audit-miracles # every `deus_ex_machina` block freak foreshadow-audit # unpaid `foreshadow` bindings They shell out to `python -m freakc`, so they need the Python bootstrap package and the repository's audit inputs. They scan source text \u2014 which means they can report on constructs the native V3 compiler cannot compile. A file full of `foreshadow` declarations will audit cleanly and fail to build."},{"p":"anime-layer","t":"Anime layer","a":"writing-anime-flavoured-v3-today","h":"Writing anime-flavoured V3 today","x":"The layer you can actually use is narrow, but it is not nothing: @protagonist task engage(power: int) -> word { pilot mut charge: int = power training arc until charge >= 9000 max 12 sessions { charge = PLUS ULTRA charge } eventually { say \"systems nominal\" } if charge >= 9000 { give back \"over nine thousand\" } give back \"insufficient\" } Bounded loops, prefix power scaling, a cleanup section and decorative annotations. Everything else is [V4](not-in-v3.html)."},{"p":"ffi","t":"extern & FFI","a":"extern-ffi","h":"extern & FFI","x":"> V3's foreign-function interface is one line long, and that is the whole of it."},{"p":"ffi","t":"extern & FFI","a":"the-declaration-form","h":"The declaration form","x":"extern task name(param: type, param: type) -> returntype One line, no body, no block, no attributes. Parameter and return types use the usual [type grammar](language-basics.html#the-type-annotation-grammar), but only a small set makes sense at a C boundary: `int`, `num`, `bool`, `void`. See [type mapping](#type-mapping) below before reaching for `word` \u2014 and never for `List<T>` or `ByteBuffer`, which have no C representation. -- V3's `extern` is a single flat declaration -- no `extern [C] { ... }` -- block, no calling convention, no `link=`. Parameter and return types -- are single identifiers. -- -- The declared symbol must exist at link time and must not start with -- the reserved"},{"p":"ffi","t":"extern & FFI","a":"the-declaration-form","h":"The declaration form","x":"`__freak_` prefix. extern task abs(v: int) -> int task main() -> void { say word_from_int(abs(0 - 41)) say word_from_int(abs(41)) } The declared symbol must be resolvable by the linker when Clang links the program. Because V3 already links the C runtime and the platform C library, ordinary libc symbols are available with no extra flags \u2014 `abs` above is `abs(3)` from the C library."},{"p":"ffi","t":"extern & FFI","a":"the-reserved-symbol-namespace","h":"The reserved symbol namespace","x":"V3 mangles source task symbols into a reserved `__freak_` namespace so generated code can never be shadowed by user names. Two rules follow: An `extern task` whose native name begins with `__freak_` is a **hard type error**. This stops a raw ABI symbol from being captured by generated locals, parameters, globals or task symbols. A source task cannot redeclare a compiler builtin call name. Declaring `task say(...)` or `extern task fs::read(...)` is rejected before either backend emits code."},{"p":"ffi","t":"extern & FFI","a":"type-mapping","h":"Type mapping","x":"There are no `std::ffi` aliases in V3 \u2014 no `c_int`, `c_size`, `usize`, `wchar`. You declare the FREAK type and the backend picks the representation. FREAK type LLVM representation `int` `i64` `num` `double` `bool` `i64` (0 / 1) `word` `i64` handle into the runtime's word table `void` `void` **`word` is not `char*`.** On the LLVM backend a `word` is an `i64` handle managed by the FREAK runtime, not a pointer to bytes. Declaring `extern task puts(s: word) -> int` will compile and then pass a handle where C expects a pointer. This is not hypothetical: `std/algorithm.fk` declares `extern task freak_word_compare(a: word, b: word) -> int` and the resulting `array_sort_word` **segfaults**. Treat"},{"p":"ffi","t":"extern & FFI","a":"type-mapping","h":"Type mapping","x":"`word` across `extern` as unsafe until you have checked the specific runtime function's real C signature. The safe subset is `int`, `num`, `bool` and `void`. For string work, prefer the builtin `word` methods and the runtime functions FREAK already exposes."},{"p":"ffi","t":"extern & FFI","a":"calling","h":"Calling","x":"An extern task is called exactly like a FREAK task, and its arguments are type-checked against the declaration: extern task abs(v: int) -> int task main() -> void { say word_from_int(abs(0 - 41)) }"},{"p":"ffi","t":"extern & FFI","a":"backend-differences","h":"Backend differences","x":"The C backend emits a plain C call and relies on the symbol being declared by an included header or implicitly. The LLVM backend emits a `declare` for the symbol with the mapped signature. A signature that disagrees with the real C function is undefined behaviour on both \u2014 there is no cross-check."},{"p":"ffi","t":"extern & FFI","a":"what-the-bible-specifies-and-v3-does-not-have","h":"What the bible specifies and V3 does not have","x":"Section 16 of the bible describes a full FFI contract. Essentially none of it is in V3: Feature Status `extern [C] { ... }` blocks V4 \u2014 V3 takes exactly one `extern task` per declaration Calling conventions (`cdecl`, `stdcall`, `system`, \u2026) V4 `link=\"user32\"` library binding V4 `@link_name(\"symbol\")` V4 Variadics `args: ...` V4 `std::ffi` type aliases V4 Raw pointers `*T`, `*const T`, `*mut T` V4 `*ptr`, `.read()`, `.write()`, `.offset()`, `.cast<U>()`, `.is_null()` V4 `@layout(C)`, `@layout(C, packed=N)`, `@layout(transparent)` V4 `@repr(u32)` on fieldless variants V4 `@extern_callback(\"C\")` and panic-abort trampolines V4 `@allow_unwinder` V4 `trust me \"...\" on my honor as .level { }` V4 \u2014"},{"p":"ffi","t":"extern & FFI","a":"what-the-bible-specifies-and-v3-does-not-have","h":"What the bible specifies and V3 does not have","x":"does not parse `alloc`, `free`, `size_of<T>()`, `align_of<T>()` V4 `std::os::windows` / `macos` / `linux` V4 Conditional imports `use[target_os=\"windows\"] ...` V4 There is no `trust me` block in V3, which means there is no unsafe boundary to gate \u2014 and equally, no honour-level audit trail on the code you actually compile. `freak audit-trust` scans source text for `trust me` blocks that the compiler itself would reject."},{"p":"ffi","t":"extern & FFI","a":"practical-guidance","h":"Practical guidance","x":"Keep the FFI surface to scalars: extern task abs(v: int) -> int extern task labs(v: int) -> int task main() -> void { say word_from_int(abs(0 - 7)) } If you need to hand a string to C, do it from the C side instead: add your function to `freakc/runtime/freak_runtime.c`, expose it the way the existing runtime bridges do, and call it through the builtin table rather than through `extern`. That is how `fs::read`, `tcp::send` and the `ui::*` family are wired, and it is the only path that handles the `word` representation correctly."},{"p":"stdlib","t":"Standard library","a":"standard-library","h":"Standard library","x":"> Every callable V3 recognises: the compiler builtins, and the FREAK-source modules linked into your build. There are two distinct surfaces, and the difference matters. **Compiler builtins** are known to the type checker itself. They always exist, need no import, and work under `freak check`. **Standard-library tasks** live in `std/*.fk` as ordinary FREAK source. The CLI concatenates those files ahead of yours during `freak build` and `freak run`. They are global tasks in the same flat namespace as your own. `freak check` compiles your file **alone**. Anything from `std/` reports `unknown callable`. Use `freak build` or `freak run` to type-check code that uses the standard library."},{"p":"stdlib","t":"Standard library","a":"how-modules-are-loaded","h":"How modules are loaded","x":"`use` lines are rewritten to comments before parsing, so they never reach the grammar. They survive only as a **textual trigger** telling the build to link an optional module. Module Loaded `std/math.fk`, `std/string.fk`, `std/convert.fk`, `std/algorithm.fk`, `std/json.fk`, `std/version.fk` Always `std/runtime.fk`, `std/http.fk` Always, LLVM backend only `std/math3d.fk` Only if the source text contains `use std::math3d` `std/zip.fk` Only if the source text contains `use std::zip` `std/ui/window.fk` Only if the source text contains `use std::ui` `packages/cockpit/src/*.fk` Only if the source text contains `use cockpit` So `use std::ui` is not an import \u2014 it is a build flag written in import"},{"p":"stdlib","t":"Standard library","a":"how-modules-are-loaded","h":"How modules are loaded","x":"syntax. Everything else is already in scope without it."},{"p":"stdlib","t":"Standard library","a":"prelude-builtins","h":"Prelude builtins","x":"Call Signature Notes `say(value)` scalar `-> void` Prints with a newline. Accepts `int`, `num`, `word`, `bool` \u2014 **not** a shape `ask(prompt)` `word -> word` Reads a line from stdin `panic(message)` `word -> void` Aborts the process `say` takes a statement form as well as a call form \u2014 `say \"x\"` and `say(\"x\")` are identical, because `(\"x\")` is just a parenthesised expression."},{"p":"stdlib","t":"Standard library","a":"conversion-builtins","h":"Conversion builtins","x":"Call Signature `word_from_int(i)` `int -> word` `word_from_bool(b)` `bool -> word` `word_to_int(w)` `word -> int` `word_concat(a, b)` `word, word -> word` `word_join(handle)` `int -> word` `char_to_word(code)` `int -> word` `format_num(f)` `num -> word` `parse_num(w)` `word -> num`"},{"p":"stdlib","t":"Standard library","a":"word-methods","h":"`word` methods","x":"Covered in full in [Words & interpolation](words.html#methods): `.length()`, `.trim()`, `.to_upper()`, `.to_lower()`, `.contains()`, `.starts_with()`, `.ends_with()`, `.replace()`, `.substring()`, `.char_at()`, `.to_int()`, `.to_num()`, `.checksum()`, `.repeated(n)`, plus seven internal `snapshot_*` helpers. Scalar methods: `int.to_num()`, `int.to_word()`, `num.to_int()`, `num.to_word()`, `bool.to_word()`."},{"p":"stdlib","t":"Standard library","a":"lists-and-arrays","h":"Lists and arrays","x":"`array_new`, `array_push`, `array_get`, `array_set`, `array_len`, `array_release`, `word_join`, plus typed `List<T>` with `.length()`, indexing and `List::filled(value, count)` \u2014 see [Lists & arrays](arrays.html)."},{"p":"stdlib","t":"Standard library","a":"wordbuilder","h":"WordBuilder","x":"`word_builder::new`, `with_capacity`, `reserve`, `append`, `append_char`, `append_int`, `length`, `capacity`, `clear`, `finish`, `discard` \u2014 see [Words & interpolation](words.html#building-words-incrementally)."},{"p":"stdlib","t":"Standard library","a":"bytebuffer","h":"ByteBuffer","x":"The only builtin type with real method syntax. Constructed with `ByteBuffer::new()` or `ByteBuffer::with_capacity(n)`. -- `ByteBuffer` is a real builtin type with methods, unlike the `int` -- handles used elsewhere. It is a seekable binary read/write cursor. task main() -> void { pilot buf: ByteBuffer = ByteBuffer::new() buf.write_word(\"hi\") buf.write_int(7) buf.write_byte(255) say word_from_int(buf.length()) say word_from_int(buf.position()) -- Rewind and read the same values back. buf.seek(0) say buf.read_word(2) say word_from_int(buf.read_int()) say word_from_int(buf.read_byte()) say word_from_int(buf.remaining()) -- status() reports the last failure; 0 means clean. say"},{"p":"stdlib","t":"Standard library","a":"bytebuffer","h":"ByteBuffer","x":"word_from_int(buf.status()) buf.release() } Method Signature Notes `.length()` `-> int` Bytes written `.capacity()` `-> int` `.position()` `-> int` The read/seek cursor `.remaining()` `-> int` Bytes from the cursor to the end `.reserve(n)` `int -> void` `.seek(pos)` `int -> void` Move the read cursor `.clear()` `-> void` `.truncate(n)` `int -> void` `.write_byte(b)` `int -> void` `.write_int(n)` `int -> void` `.write_int_be(n)` `int -> void` Big-endian `.write_word(w)` `word -> void` `.read_byte()` `-> int` `.read_int()` `-> int` `.read_int_be()` `-> int` `.read_word(len)` `int -> word` `.slice(from, len)` `int, int -> ByteBuffer` `.to_word()` `-> word` `.status()` `-> int` Last failure"},{"p":"stdlib","t":"Standard library","a":"bytebuffer","h":"ByteBuffer","x":"code; `0` is clean `.clear_status()` `-> void` `.release()` `-> void` Free the buffer Writes append to the end; `position()` is the **read** cursor, so it stays at `0` until you read or `seek`. Errors are reported out-of-band through `status()` rather than by return value, since V3 has no `result<T, E>`. This is the closest thing V3 has to the bible's `std::bytes` \u00a77.14."},{"p":"stdlib","t":"Standard library","a":"math-builtins","h":"`math::` builtins","x":"Floating point, over `num`. -- Two numeric surfaces exist side by side: -- `math::*` are compiler builtins over `num` (floating point). -- `std_*` come from std/math.fk and work on `int`. task main() -> void { -- builtins, num say format_num(math::sqrt(144.0)) say format_num(math::pow(2.0, 10.0)) say format_num(math::floor(3.9)) say format_num(math::ceil(3.1)) say format_num(math::sin(0.0)) say format_num(math::cos(0.0)) -- std/math.fk, int say word_from_int(std_abs(0 - 9)) say word_from_int(std_max(3, 8)) say word_from_int(std_min(3, 8)) say word_from_int(std_clamp(42, 0, 10)) say word_from_int(std_pow(2, 8)) say word_from_int(std_gcd(84, 30)) say word_from_int(std_lcm(4, 6)) say"},{"p":"stdlib","t":"Standard library","a":"math-builtins","h":"`math::` builtins","x":"word_from_int(std_factorial(6)) say word_from_int(std_fibonacci(12)) say word_from_bool(std_is_even(4)) say word_from_int(std_sign(0 - 3)) } Call Signature `math::sqrt(x)` `num -> num` `math::pow(base, exp)` `num, num -> num` `math::sin(x)` / `math::cos(x)` / `math::tan(x)` `num -> num` `math::floor(x)` / `math::ceil(x)` `num -> num` That is the entire `math::` namespace. No `log`, `exp`, `asin`, `atan2`, `abs`, `gcd`, `simd` \u2014 those bible entries are V4."},{"p":"stdlib","t":"Standard library","a":"fs-builtins","h":"`fs::` builtins","x":"-- `fs::*` are compiler builtins. Paths are plain `word` values. task main() -> void { fs::write(\"sortie.log\", \"launch\\n\") fs::append(\"sortie.log\", \"engage\\n\") if fs::exists(\"sortie.log\") { say fs::read(\"sortie.log\").trim() } fs::make_dir(\"hangar_out\") say word_from_bool(fs::exists(\"hangar_out\")) -- `fs::delete` is file-only in V3, and reports true when the file is -- gone -- whether this call removed it or it was already absent. say word_from_bool(fs::delete(\"sortie.log\")) say word_from_bool(fs::exists(\"sortie.log\")) } Call Signature Notes `fs::read(path)` `word -> word` Empty word on failure `fs::write(path, content)` `word, word -> void` `fs::append(path, content)` `word, word -> void`"},{"p":"stdlib","t":"Standard library","a":"fs-builtins","h":"`fs::` builtins","x":"`fs::exists(path)` `word -> bool` `fs::delete(path)` `word -> bool` **File only.** `true` when the file was removed *or was already absent*; `false` when it could not be unlinked `fs::list_dir(path)` `word -> word` Encoded listing `fs::make_dir(path)` `word -> void` There is no `fs::copy`, `fs::move`, `fs::is_file`, `fs::is_dir`, `dir::create_all` or `dir::delete`, and no `result<T, E>` \u2014 errors surface as empty words or `false`."},{"p":"stdlib","t":"Standard library","a":"process-builtins","h":"`process::` builtins","x":"-- Process and clock builtins. -- Note: `process::args()` is deliberately rejected by V3. Use the -- indexed pair `process::args_count()` / `process::arg(i)` instead. task main() -> void { pilot argc: int = process::args_count() say word_from_int(argc) pilot mut i: int = 0 repeat argc times { say \"arg {i}: \" + process::arg(i) i += 1 } pilot started: int = time::now_ms() pilot mut spin: int = 0 repeat 1000 times { spin += 1 } pilot elapsed: int = time::now_ms() - started say word_from_bool(elapsed >= 0) say word_from_bool(process::env(\"PATH\").length() > 0) } Call Signature `process::args_count()` `-> int` `process::arg(i)` `int -> word` `process::env(name)` `word -> word`"},{"p":"stdlib","t":"Standard library","a":"process-builtins","h":"`process::` builtins","x":"`process::exec(cmd)` `word -> int` `process::exec_capture(cmd)` `word -> word` `process::exit(code)` `int -> void` `process::input()` `-> word` `process::pid()` `-> int` `process::set_env(name, value)` `word, word -> void` `process::args()` is **deliberately rejected** by V3 rather than exposing the runtime's raw `argv` pointer as a fake array handle. Use the indexed pair `process::args_count()` and `process::arg(i)`. Argument 0 is the executable path."},{"p":"stdlib","t":"Standard library","a":"time-builtins","h":"`time::` builtins","x":"Call Signature `time::now_ms()` `-> int` \u2014 milliseconds since the epoch `time::monotonic_ns()` `-> int` \u2014 nanoseconds from a monotonic clock Use `monotonic_ns()` for measuring elapsed time; it does not jump when the wall clock is adjusted. There is no `sleep`, no `Instant`, no `Duration`, and no duration literals like `500.milliseconds`."},{"p":"stdlib","t":"Standard library","a":"tcp-builtins","h":"`tcp::` builtins","x":"Call Signature `tcp::connect(host, port)` `word, int -> int` \u2014 socket handle `tcp::send(sock, data)` `int, word -> int` `tcp::recv(sock, max)` `int, int -> word` `tcp::recv_all(sock, max)` `int, int -> word` `tcp::close(sock)` `int -> void` `std/http.fk` builds on these and is linked automatically on the LLVM backend."},{"p":"stdlib","t":"Standard library","a":"the-newer-socket-api","h":"The newer socket API","x":"A second, lower-level socket surface exists alongside the calls above. It adds listening, accepting, timeouts and `ByteBuffer`-based transfer. Call Signature `tcp::socket_connect(host, port)` `word, int -> int` `tcp::socket_listen(host, port)` `word, int -> int` `tcp::socket_accept(listener)` `int -> int` `tcp::socket_local_port(sock)` `int -> int` `tcp::socket_status(sock)` `int -> int` `tcp::socket_eof(sock)` `int -> bool` `tcp::socket_set_timeout(sock, ms)` `int, int -> void` `tcp::socket_send(sock, buf, off, len)` `int, ByteBuffer, int, int -> int` `tcp::socket_send_all(sock, buf, off, len)` `int, ByteBuffer, int, int -> int` `tcp::socket_receive(sock, buf, max)` `int, ByteBuffer, int"},{"p":"stdlib","t":"Standard library","a":"the-newer-socket-api","h":"The newer socket API","x":"-> int` `tcp::socket_close(sock)` `int -> void` This is the first V3 API that can write a server, since `socket_listen` and `socket_accept` have no equivalent in the older `tcp::connect` family. Like `ByteBuffer`, it reports failure through a `status()`-style code rather than a `result<T, E>`."},{"p":"stdlib","t":"Standard library","a":"ui-builtins","h":"`ui::` builtins","x":"A raw, indexed windowing API: `ui::create_window`, `ui::destroy_window`, `ui::poll_events`, `ui::begin_frame`, `ui::end_frame`, `ui::clear`, `ui::fill_rect`, `ui::stroke_rect`, `ui::fill_circle`, `ui::draw_line`, `ui::draw_text`, `ui::measure_text`, `ui::get_width`, `ui::get_height`, `ui::set_clip`, `ui::reset_clip`, and an `ui::event_*` family (`kind`, `key`, `pressed`, `character`, `mouse_x`, `mouse_y`, `button`, `repeat`, `scroll_dy`, `width`, `height`, `gained`). `std::ui` runs on the **LLVM backend, on Windows, through the Win32/GDI runtime only**. There is no macOS or Linux native backend, and the C backend has no executable shape storage for UI programs. `WindowConfig.vsync` is"},{"p":"stdlib","t":"Standard library","a":"ui-builtins","h":"`ui::` builtins","x":"retained but ignored, events use the raw indexed API rather than an owned event list, and COCKPIT is a source preview, not a frozen package. Geometry crosses this ABI as pixel-addressed `int`. Convert from `num` before calling."},{"p":"stdlib","t":"Standard library","a":"stdmath-integer-maths","h":"`std::math` \u2014 integer maths","x":"-- Two numeric surfaces exist side by side: -- `math::*` are compiler builtins over `num` (floating point). -- `std_*` come from std/math.fk and work on `int`. task main() -> void { -- builtins, num say format_num(math::sqrt(144.0)) say format_num(math::pow(2.0, 10.0)) say format_num(math::floor(3.9)) say format_num(math::ceil(3.1)) say format_num(math::sin(0.0)) say format_num(math::cos(0.0)) -- std/math.fk, int say word_from_int(std_abs(0 - 9)) say word_from_int(std_max(3, 8)) say word_from_int(std_min(3, 8)) say word_from_int(std_clamp(42, 0, 10)) say word_from_int(std_pow(2, 8)) say word_from_int(std_gcd(84, 30)) say word_from_int(std_lcm(4, 6)) say word_from_int(std_factorial(6)) say"},{"p":"stdlib","t":"Standard library","a":"stdmath-integer-maths","h":"`std::math` \u2014 integer maths","x":"word_from_int(std_fibonacci(12)) say word_from_bool(std_is_even(4)) say word_from_int(std_sign(0 - 3)) } Task Signature `std_abs(x)` `int -> int` `std_sign(x)` `int -> int` `std_min(a, b)` / `std_max(a, b)` `int, int -> int` `std_clamp(x, lo, hi)` `int, int, int -> int` `std_pow(base, exp)` `int, int -> int` `std_gcd(a, b)` / `std_lcm(a, b)` `int, int -> int` `std_factorial(n)` `int -> int` `std_fibonacci(n)` `int -> int` `std_is_even(x)` / `std_is_odd(x)` `int -> bool` `int_to_word(n)` `int -> word`"},{"p":"stdlib","t":"Standard library","a":"stdstring","h":"`std::string`","x":"Listed in full in [Words & interpolation](words.html#standard-library-word-tasks)."},{"p":"stdlib","t":"Standard library","a":"stdconvert","h":"`std::convert`","x":"-- std/convert.fk: base conversion and safe parsing. task main() -> void { say int_to_hex(255) say int_to_bin(10) say int_to_oct(64) say word_from_int(char_to_digit(\"7\")) say word_from_int(word_to_int_safe(\"not a number\")) say word_from_int(word_to_int_safe(\"123\")) say bool_to_word(true) } Task Signature `int_to_hex(n)` / `int_to_bin(n)` / `int_to_oct(n)` `int -> word` `char_to_digit(c)` `word -> int` `word_to_int_safe(s)` `word -> int` \u2014 `0` on failure, never panics `bool_to_word(b)` `bool -> word`"},{"p":"stdlib","t":"Standard library","a":"stdalgorithm","h":"`std::algorithm`","x":"See [Arrays](arrays.html#stdalgorithmfk-helpers), including the two functions that are broken."},{"p":"stdlib","t":"Standard library","a":"stdjson","h":"`std::json`","x":"A complete JSON parser in pure FREAK. Values are `int` handles. -- std/json.fk is a pure-FREAK parser. Values are `int` handles. task main() -> void { json_init() pilot doc: int = json_parse(\"{\\\"unit\\\":\\\"Valkyries\\\",\\\"members\\\":4,\\\"ready\\\":true}\") say json_get_type(doc) say word_from_int(json_obj_len(doc)) say word_from_bool(json_obj_has(doc, \"unit\")) say json_get_str(json_obj_get(doc, \"unit\")) say word_from_int(json_get_int(json_obj_get(doc, \"members\"))) say word_from_bool(json_get_bool(json_obj_get(doc, \"ready\"))) pilot list: int = json_parse(\"[10, 20, 30]\") say word_from_int(json_arr_len(list)) say word_from_int(json_get_int(json_arr_get(list, 1))) } Task Signature `json_init()` `->"},{"p":"stdlib","t":"Standard library","a":"stdjson","h":"`std::json`","x":"void` \u2014 call once before parsing `json_parse(source)` `word -> int` `json_stringify(handle)` `int -> word` `json_get_type(h)` `int -> word` \u2014 `o`, `a`, `s`, `n`, `b`, `z` `json_get_str(h)` / `json_get_int(h)` / `json_get_bool(h)` `int -> word` / `int` / `bool` `json_is_null(h)` `int -> bool` `json_obj_len(h)` / `json_obj_has(h, key)` / `json_obj_get(h, key)` / `json_obj_key_at(h, i)` object access `json_arr_len(h)` / `json_arr_get(h, i)` array access"},{"p":"stdlib","t":"Standard library","a":"stdversion","h":"`std::version`","x":"Semantic-version parsing and constraint matching \u2014 the machinery behind Hangar's dependency resolution. -- std/version.fk implements semver parsing and constraint matching. task main() -> void { pilot parsed: word = ver_parse(\"2.14.1-rc.1+build7\") say word_from_int(ver_major(parsed)) say word_from_int(ver_minor(parsed)) say word_from_int(ver_patch(parsed)) say ver_pre(parsed) say ver_build(parsed) say ver_to_string(parsed) say word_from_bool(ver_lt(\"1.2.3\", \"1.10.0\")) say word_from_bool(ver_gt(\"2.0.0\", \"1.9.9\")) say word_from_bool(ver_eq(\"1.0.0\", \"1.0.0\")) say word_from_bool(ver_satisfies(\"1.4.2\", \"^1.4\")) say word_from_bool(ver_satisfies(\"2.0.0\", \"^1.4\")) } Task Signature"},{"p":"stdlib","t":"Standard library","a":"stdversion","h":"`std::version`","x":"`ver_parse(version)` `word -> word` \u2014 an encoded parse result `ver_major` / `ver_minor` / `ver_patch` `word -> int` `ver_pre` / `ver_build` / `ver_to_string` `word -> word` `ver_compare(a, b)` `word, word -> int` `ver_eq` / `ver_lt` / `ver_gt` / `ver_lte` / `ver_gte` `word, word -> bool` `ver_bump_major` / `ver_bump_minor` / `ver_bump_patch` `word -> word` `ver_satisfies(version, constraint)` `word, word -> bool` `version_matches_constraint(version, constraint)` `word, word -> bool`"},{"p":"stdlib","t":"Standard library","a":"modules-that-do-not-exist","h":"Modules that do not exist","x":"The bible's Section 7 lists many more. None of these are in V3: `std::iter` (`.map`, `.filter`, `.fold`, \u2026), `Map<K,V>`, `Set<T>` and `Lineup<T>` from `std::collections`, `std::io` beyond `say` / `ask`, `std::random`, `std::thread`, `std::anime`, `std::narrative`, `std::test`, `std::mem` (`Shared`, `Weak`, `size_of`, `alloc`), `std::ffi`, `std::os`, `std::panic`, `std::regex`, `std::crypto`. Partially covered, in a V3-specific shape rather than the bible's: Bible module V3 equivalent `std::collections` `List<T>` Typed `List<T>` with indexing and `List::filled` \u2014 no `push`/`pop`/`sort` `std::bytes` `ByteBuffer` The builtin `ByteBuffer` type `std::word` `WordBuilder` `word_builder::*` over an"},{"p":"stdlib","t":"Standard library","a":"modules-that-do-not-exist","h":"Modules that do not exist","x":"`int` handle `std::net` `tcp::socket_*`, including listen and accept"},{"p":"grammar","t":"Grammar","a":"grammar","h":"Grammar","x":"> The complete V3 grammar, transcribed from `src/compiler/v3/lexer.fk` and `parser.fk`. This is the whole language. If a construct is not derivable from these rules, it does not parse."},{"p":"grammar","t":"Grammar","a":"lexical-structure","h":"Lexical structure","x":"program-text = { token | whitespace | comment } comment = \"--\" { any-char-except-newline } whitespace = \" \" | \"\\t\" | \"\\r\" | \"\\n\" token = number | word-literal | identifier | keyword | bool-literal | punctuation number = digit { digit | \".\" } -- more than one \".\" is a lex error -- no sign, no exponent, no suffix word-literal = '\"' { char | escape } '\"' escape = \"\\\\n\" | \"\\\\r\" | \"\\\\t\" | \"\\\\\" any | \"\\\\x\" hex hex -- \"\\x00\" is rejected identifier = ( letter | \"_\" ) { letter | digit | \"_\" } -- letters are ASCII A-Z a-z only Identifiers are classified against the keyword table **case-insensitively**: an identifier whose lowercase form is a keyword becomes that keyword token. bool-literal = \"true\" |"},{"p":"grammar","t":"Grammar","a":"lexical-structure","h":"Lexical structure","x":"\"false\" | \"yes\" | \"no\" | \"hai\" | \"iie\" punctuation = \"==\" | \"!=\" | \"<=\" | \">=\" | \"+=\" | \"-=\" | \"*=\" | \"/=\" | \"%=\" | \"->\" | \"=>\" | \"**\" | \"|>\" | \"||\" | \"::\" | \"=\" | \"+\" | \"-\" | \"*\" | \"/\" | \"%\" | \"<\" | \">\" | \"!\" | \":\" | \"|\" | \"@\" | \"?\" | \"{\" | \"}\" | \"(\" | \")\" | \"[\" | \"]\" | \",\" | \".\" `**`, `=>`, `||`, `?` and `!` are produced by the lexer but have **no grammar rule** \u2014 reaching one is a parse error."},{"p":"grammar","t":"Grammar","a":"multi-word-keywords","h":"Multi-word keywords","x":"Lexed greedily: on seeing the first word, the lexer looks ahead for the second and merges them. \"give back\" \"or else\" \"trust me\" \"for each\" \"for science\" \"training arc\" \"bringing back\" \"only on\" \"PLUS ULTRA\" \"FINAL FORM\""},{"p":"grammar","t":"Grammar","a":"keyword-table","h":"Keyword table","x":"pilot fixed task say shape impl doctrine launch use as in lend mut move copy break continue if else when repeat times until done for each check result got nobody some ok err sessions max foreshadow payoff route sadly deus_ex_machina isekai eventually and or not nakama tsundere extern Many of these are reserved without having a grammar rule \u2014 see [the reserved list](language-basics.html#the-full-reserved-list)."},{"p":"grammar","t":"Grammar","a":"program","h":"Program","x":"program = { statement } There is no distinction between top-level and nested statements: the same `statement` rule is used everywhere. A `task`, `shape` or `impl` is syntactically just a statement, so it may legally appear nested inside another block."},{"p":"grammar","t":"Grammar","a":"statements","h":"Statements","x":"statement = [ annotation ] statement-body annotation = \"@\" identifier -- exactly one, no arguments statement-body = say-stmt | shape-decl | impl-decl | eventually-stmt | fixed-decl | pilot-decl | give-back | break-stmt | continue-stmt | if-stmt | repeat-stmt | training-arc | when-stmt | extern-decl | task-decl | block | assign-or-expr say-stmt = \"say\" expression fixed-decl = \"fixed\" \"pilot\" identifier [ \":\" type ] \"=\" expression pilot-decl = \"pilot\" [ \"mut\" ] identifier [ \":\" type ] \"=\" expression give-back = \"give back\" [ expression ] break-stmt = \"break\" continue-stmt = \"continue\" if-stmt = \"if\" expression block [ \"else\" ( if-stmt | block ) ] repeat-stmt = \"repeat\" \"until\" expression"},{"p":"grammar","t":"Grammar","a":"statements","h":"Statements","x":"block | \"repeat\" expression \"times\" block training-arc = \"training arc\" \"until\" expression \"max\" expression \"sessions\" block when-stmt = \"when\" expression \"{\" { when-arm } \"}\" when-arm = ( \"_\" | expression ) \"->\" statement eventually-stmt = \"eventually\" block block = \"{\" { statement } \"}\" assign-or-expr = expression [ assign-op expression ] assign-op = \"=\" | \"+=\" | \"-=\" | \"*=\" | \"/=\" | \"%=\" Every type position \u2014 `pilot-decl`, `fixed-decl`, shape fields, parameters, return types, `extern` signatures \u2014 goes through one rule: type = identifier | \"List\" \"<\" identifier \">\" `List<T>` is the language's only generic form, and it is a hard-coded special case in `parser_take_type` rather than a"},{"p":"grammar","t":"Grammar","a":"statements","h":"Statements","x":"general mechanism: the element must be a plain identifier, so `List<List<int>>` is rejected. That single rule is why `maybe<T>`, `result<T,E>`, `Map<K,V>`, fixed arrays, tuples and raw pointers have no syntax."},{"p":"grammar","t":"Grammar","a":"declarations","h":"Declarations","x":"shape-decl = \"shape\" identifier \"{\" { field } \"}\" field = identifier \":\" type [ \",\" ] impl-decl = \"impl\" identifier [ \"for\" identifier ] \"{\" { impl-method } \"}\" impl-method = \"task\" identifier \"(\" params \")\" [ \"->\" type ] block task-decl = \"task\" identifier \"(\" params \")\" [ \"->\" type ] block extern-decl = \"extern\" \"task\" identifier \"(\" ext-params \")\" \"->\" type params = [ param { \",\" param } ] param = \"self\" | identifier \":\" type ext-params = [ identifier \":\" type { \",\" identifier \":\" type } ] In `impl D for S`, `S` owns the methods and `D` is recorded as provenance only \u2014 it is never validated. A method's mangled global name is `S_method`. `self` is written bare; the compiler substitutes"},{"p":"grammar","t":"Grammar","a":"declarations","h":"Declarations","x":"the owning shape's type."},{"p":"grammar","t":"Grammar","a":"expressions","h":"Expressions","x":"Loosest to tightest: expression = and-expr { \"or\" and-expr } and-expr = cmp-expr { \"and\" cmp-expr } cmp-expr = add-expr { ( \"==\" | \"!=\" | \"<\" | \">\" | \"<=\" | \">=\" ) add-expr } add-expr = mul-expr { ( \"+\" | \"-\" | \"NAKAMA\" ) mul-expr } mul-expr = unary { ( \"*\" | \"/\" | \"%\" ) unary } unary = ( \"not\" | \"-\" | \"PLUS ULTRA\" | \"FINAL FORM\" | \"TSUNDERE\" ) unary | postfix postfix = primary { postfix-op } postfix-op = \".\" identifier [ \"(\" args \")\" ] | \"[\" expression \"]\" | \"|>\" identifier [ \"(\" args \")\" ] primary = number | word-literal | bool-literal | identifier [ \"::\" identifier ] [ ctor-args | \"(\" args \")\" ] | \"(\" expression \")\" | \"[\" [ expression { \",\" expression } ] \"]\" ctor-args = \"{\" [ field-init"},{"p":"grammar","t":"Grammar","a":"expressions","h":"Expressions","x":"{ \",\" field-init } ] \"}\" field-init = identifier \":\" expression args = [ expression { \",\" expression } ] `ctor-args` is only taken when the identifier is an **already-registered shape name**, which is why a shape must be declared before it is constructed in token order. `x |> f(a)` desugars to `f(x, a)`; the pipe target must be a bare identifier."},{"p":"grammar","t":"Grammar","a":"string-interpolation","h":"String interpolation","x":"Interpolation is resolved by the parser after lexing, over the literal's text. interp-path = identifier { \".\" identifier } A `{...}` whose body matches `interp-path`, where every segment is a valid identifier and not a keyword, becomes an interpolated expression. Anything else \u2014 including `{1 + 2}` and `{f()}` \u2014 stays literal text, braces included. An unmatched `{` is literal. The resolved path must have type `word`, `int`, `num` or `bool`."},{"p":"grammar","t":"Grammar","a":"error-recovery","h":"Error recovery","x":"The parser reports a diagnostic, skips one token, and continues. After 100 syntax errors it stops: error: too many errors, giving up (>100 syntax errors in <file>) There are no `ErrorNode` or `IncompleteNode` tolerant-AST nodes \u2014 those are V4. Codegen is skipped whenever any frontend diagnostic fired."},{"p":"grammar","t":"Grammar","a":"reading-the-real-thing","h":"Reading the real thing","x":"The authoritative sources, in the order they are concatenated: File Role `src/compiler/v3/globals.fk` Identity, compiler state, token and AST tables `src/compiler/v3/helpers.fk` Arrays, diagnostics, AST allocation, emit helpers `src/compiler/v3/lexer.fk` Tokenisation `src/compiler/v3/parser.fk` Recursive descent into parallel AST arrays `src/compiler/v3/checker.fk` Type checks and the opt-in Phase-1 borrow checker `src/compiler/v3/emit_c.fk` Streaming C lowering `src/compiler/v3/emit_llvm.fk` Streaming LLVM IR lowering `src/compiler/v3/main.fk` Standalone `freakc` entry point The whole compiler is one flattened translation unit with globally unique symbols; the file split is a maintenance"},{"p":"grammar","t":"Grammar","a":"reading-the-real-thing","h":"Reading the real thing","x":"convention, not a module system."},{"p":"conformance","t":"Bible vs V3","a":"bible-vs-v3","h":"Bible vs V3","x":"> A section-by-section comparison of `freak-full-bible.md` against what the V3 compiler accepts, established by compiling probe programs."},{"p":"conformance","t":"Bible vs V3","a":"method","h":"Method","x":"The bible carries its own status table in \u00a70.2, but that table describes the project as a whole \u2014 it counts a feature as landed when it works in the **V4** compiler under `src/compiler/v4/`. Many rows marked \"\u26a0\ufe0f Partial\" are entirely absent from the shipping V3 compiler. The verdicts below come from a different method: writing a probe program for each feature and running it through a V3 binary built from source. \"Compiles\" means the frontend accepted it. \"Rejected\" quotes the actual diagnostic. A recurring pattern is worth stating once. Many bible features are **lexed but not parsed**: the lexer knows the keyword, so it can never be an identifier, but no grammar rule consumes it. These fail"},{"p":"conformance","t":"Bible vs V3","a":"method","h":"Method","x":"with *\"unexpected 'X' \u2014 this token cannot start an expression\"*. The feature is reserved and unusable at the same time."},{"p":"conformance","t":"Bible vs V3","a":"section-1-syntax","h":"Section 1 \u2014 Syntax","x":"Bible status: \u26a0\ufe0f Partial. Real V3 status: a small core works. \u00a71 feature Bible V3 Evidence 1.1 `pilot` / `fixed pilot` / `pilot mut` \u2705 **Compiles** 1.1 Type annotation `x: T` \u2705 **Compiles** \u2014 one identifier, plus `List<T>` 1.2 `task name(p: T) -> R { }` \u2705 **Compiles** 1.2 `give back` \u2705 **Compiles** 1.2 Arrow body `=> expr` \u2705 **Rejected** `expected '{', found '=>'` 1.2 `done` block terminator \u2705 **Rejected** `expected '{', found 'give back'` 1.2 Named call arguments V4 **Rejected** 1.2 `say` \u2705 **Compiles** \u2014 scalars only, not shapes 1.2 `{path}` interpolation \u2705 **Compiles** \u2014 paths only, never calls 1.3 `int` `num` `word` `bool` `void` \u2705 **Compiles** 1.3 `uint` `tiny` `char` `big` `float`"},{"p":"conformance","t":"Bible vs V3","a":"section-1-syntax","h":"Section 1 \u2014 Syntax","x":"`float32` `never` V4 **Absent** 1.3 `[T; N]`, tuples, `*T` / `*mut T` V4 **Rejected** 1.4 `maybe<T>` / `some` / `nobody` \u2705 **Rejected** `expected '{', found '<'` 1.4 `result<T,E>` / `ok` / `err` \u2705 **Rejected** 1.4 `List<T>` \u2705 **Compiles** \u2014 typed, indexable, `List::filled`; no `push`/`sort`/iterators 1.4 `Map<K,V>` `Set<T>` `Lineup<T>` \u2705 **Absent** No map literal either 1.5 `shape` declaration \u2705 **Compiles** 1.5 Shape construction `S { f: v }` \u2705 **Compiles** LLVM backend 1.5 `impl S { task m(self) }` \u2705 **Compiles** 1.5 Generic shapes `shape Pair<A,B>` V4 **Rejected** 1.6 `doctrine` declaration \u26a0\ufe0f **Rejected** `unexpected 'doctrine'` 1.6 `impl D for S { }` \u26a0\ufe0f **Compiles** \u2014 `D` unvalidated"},{"p":"conformance","t":"Bible vs V3","a":"section-1-syntax","h":"Section 1 \u2014 Syntax","x":"1.6 Operator dispatch via `Add` \u26a0\ufe0f **Rejected** `operator '+' does not accept Vec2 and Vec2` 1.6 `dyn Doctrine`, vtables V4 **Absent** 1.7 `if` / `else if` / `else` \u2705 **Compiles** 1.7 `when` with literal arms and `_` \u26a0\ufe0f **Compiles** \u2014 statement only 1.7 `when` with destructuring V4 **Absent** 1.7 `for each x in list` \u2705 **Rejected** `unexpected 'for each'` 1.7 `repeat N times` / `repeat until` \u2705 **Compiles** 1.7 `training arc ... max N sessions` \u26a0\ufe0f **Compiles** 1.7 `training arc ... with growth` V4 **Rejected** 1.7 `break` / `continue` \u2705 **Compiles** 1.8 Closures `\\|x\\| => expr` \u26a0\ufe0f **Rejected** `unexpected '\\|'` 1.8 Capture modes `copy` / `move` / `mut` V4 **Absent** 1.9 Pipe `\\|>` \u2705"},{"p":"conformance","t":"Bible vs V3","a":"section-1-syntax","h":"Section 1 \u2014 Syntax","x":"**Compiles** \u2014 bare task names only 1.10 `?` propagation \u2705 **Rejected** `unexpected '?'` 1.10 `check` over maybe / result \u2705 **Rejected** `unexpected 'check'` 1.10 `or else` \u2705 **Rejected** `unexpected 'or else'` 1.11 Generics `<T>`, bounds \u26a0\ufe0f **Rejected** `expected '(', found '<'`. `List<T>` is a hard-coded special case, not a mechanism 1.12 `lend` / `lend mut` \u26a0\ufe0f **Rejected** `expected an identifier for parameter name, found 'lend'` 1.13 `use module::{...}` \u26a0\ufe0f **Stripped** \u2014 a build hint, not an import 1.13 `use module::*` V4 **Stripped** 1.13 `launch` visibility \u26a0\ufe0f **Rejected** `unexpected 'launch'` 1.14 `variant` V4 **Rejected** 1.14 `alias` V4 **Rejected** 1.14 Root `fixed pilot`"},{"p":"conformance","t":"Bible vs V3","a":"section-1-syntax","h":"Section 1 \u2014 Syntax","x":"constants \u26a0\ufe0f **Compiles** \u2014 and V3 also allows mutable root bindings and root statements, which the bible forbids 1.15 `[a, b, c]` literal \u26a0\ufe0f **Compiles** \u2014 infers `List<T>` from its elements 1.15 `[expr; N]` repeat-fill V4 **Rejected** 1.15 No implicit tail return \u2705 **Enforced**"},{"p":"conformance","t":"Bible vs V3","a":"section-2-advanced-type-system","h":"Section 2 \u2014 Advanced type system","x":"Bible: \ud83d\udd1c V4, entire section. V3: **entirely absent**, as documented. `power<N>`, `prob[lo..hi]`, `causality<T>` and `mood` do not lex or parse."},{"p":"conformance","t":"Bible vs V3","a":"section-3-concurrency","h":"Section 3 \u2014 Concurrency","x":"Bible: \ud83d\udd1c V4, entire section. V3: **entirely absent**. No `xm3`, `sortie`, `debrief`, `formation`, `BriefingRoom`, `wingman` or `Comms`. None of those words are even keywords in V3's lexer. `||` lexes as a single token but has no grammar rule. There is no concurrency in V3 at all \u2014 not even `std::thread::spawn`."},{"p":"conformance","t":"Bible vs V3","a":"section-4-borrow-checker","h":"Section 4 \u2014 Borrow checker","x":"Bible: \u26a0\ufe0f Partial, with an extensive description of V4's Meiya checker. V3 ships **Phase-1 only**, behind `--strict-borrow`: Rule V3 `pilot` immutable, `pilot mut` reassignable Enforced under the flag Single-owner moves for `word`, arrays, shapes Enforced under the flag `int` / `num` / `bool` are Copy Enforced under the flag Nominal member validation **Always on**, flag or not `lend` / `lend mut` parameters Do not parse Lifetimes `'a`, outlives bounds `'long: 'short` Do not lex `Shared<T>` / `Weak<T>` Absent `trust me ... on my honor as .level` Does not parse `direct_order [arch] { }` Does not parse Without the flag, V3 does no ownership checking whatsoever. Everything the bible's \u00a74"},{"p":"conformance","t":"Bible vs V3","a":"section-4-borrow-checker","h":"Section 4 \u2014 Borrow checker","x":"describes at length \u2014 returned loans, provenance memoisation, fixed-layout aggregate children, snapshot restore, invalidation report fields \u2014 is V4 work in `src/compiler/v4/`. None of it exists in the shipping compiler."},{"p":"conformance","t":"Bible vs V3","a":"section-5-anime-layer","h":"Section 5 \u2014 Anime layer","x":"\u00a75 feature Bible V3 5.1 `@annotation` \u26a0\ufe0f **Parses, ignored.** One per statement, no arguments 5.1 `@deprecated` enforcement \u26a0\ufe0f Not enforced 5.1 Caller prefixes `sadly`, `for science,`, `knowing this will hurt,` \u26a0\ufe0f **Rejected** 5.2 `foreshadow` / `payoff` \u2705 **Rejected** \u2014 the auditor scans text only 5.3 `route`, `check route`, `only on` V4 **Rejected** 5.4 `PLUS ULTRA` `NAKAMA` `FINAL FORM` `TSUNDERE` \u26a0\ufe0f **Compile**, with different position and semantics 5.5 `deus_ex_machina` \u26a0\ufe0f **Rejected** 5.6 `training arc` \u26a0\ufe0f **Compiles** 5.6 `with growth` V4 **Rejected** 5.7 `isekai { } bringing back { }` \u26a0\ufe0f **Rejected** 5.7 `eventually { }` \u26a0\ufe0f **Compiles**, emitted inline \u2014 not deferred, not LIFO 5.7"},{"p":"conformance","t":"Bible vs V3","a":"section-5-anime-layer","h":"Section 5 \u2014 Anime layer","x":"`eventually if cond { }` \u26a0\ufe0f **Rejected** The bible marks \u00a75.2 foreshadowing as \u2705 Implemented and the `deus_ex_machina` 20-word minimum as enforced. Both are true of the **Python auditor**, which scans source text. Neither construct parses in the native V3 compiler \u2014 a file using them audits cleanly and fails to build. The four anime operators also diverge in form, not just in enforcement: Operator Bible V3 `FINAL FORM` postfix, `base FINAL FORM` \u2192 `base * base`, 5s build pause **prefix**, `int` only, `x * x`, no pause `PLUS ULTRA` infix, `base PLUS ULTRA emotion` \u2192 `base * (1 + e\u00b2)` **prefix**, `int` only, `x * 2` `TSUNDERE` postfix; `!x` for bool, `-x` for num **prefix**, `int` only, `0 -"},{"p":"conformance","t":"Bible vs V3","a":"section-5-anime-layer","h":"Section 5 \u2014 Anime layer","x":"x` `NAKAMA` infix, `a + b + (a * b * 0.1)` infix, `int` only, `a + b`"},{"p":"conformance","t":"Bible vs V3","a":"section-6-modules-and-hangar","h":"Section 6 \u2014 Modules and Hangar","x":"Feature Bible V3 `use module::{names}` \u26a0\ufe0f Line is **replaced with a comment** before parsing `use module::*` V4 Same \u2014 stripped `launch` / `launch(package)` \u26a0\ufe0f **Rejected** Module-private visibility \u26a0\ufe0f **No visibility system at all** `hangar.toml` \u26a0\ufe0f Works, but as `[project]` + `[dependencies]` only \u2014 not the bible's `[unit]`/`[build]`/`[profile.*]` `hangar init/add/remove/install/update` \u26a0\ufe0f Work, plus `outdated`, `version`, `audit`, `login`, `publish` `hangar search` V4 Absent **Consuming an installed package** \u26a0\ufe0f **Impossible** \u2014 the build never reads `hangar_modules/` or `[dependencies]` V3 has **no module system**. Everything \u2014 your file, and every standard library file linked into the"},{"p":"conformance","t":"Bible vs V3","a":"section-6-modules-and-hangar","h":"Section 6 \u2014 Modules and Hangar","x":"build \u2014 shares one flat global namespace. A `use` line only signals the build to link `std/math3d.fk`, `std/zip.fk`, `std/ui/window.fk` or the cockpit package. Every other module is already in scope without it. That also means Hangar can install a dependency it cannot then link. `use pkg::{\u2026}` is stripped, the package source is never concatenated, and the first call fails with `unknown callable`. Verified against the shipping compiler \u2014 see [Hangar & packages](hangar.html)."},{"p":"conformance","t":"Bible vs V3","a":"section-7-standard-library","h":"Section 7 \u2014 Standard library","x":"Bible module V3 `std::word` methods Partial \u2014 as builtin `word` methods, plus `.repeated(n)`. `.substring` takes a **length**, not an end index; no `.split`, `.chars` `std::word` `WordBuilder` Present as `word_builder::*` over an `int` handle `std::num` Absent \u2014 `math::*` builtins plus `std_*` int tasks instead `std::collections` `List<T>` Partial \u2014 typed and indexable, but no `push`, `pop`, `sort` or iterators `std::collections` `Map`/`Set`/`Lineup` **Absent** `std::iter` **Absent** `std::io` `say` and `ask` only `std::fs` Partial \u2014 7 builtins, no `copy`/`move`/`is_file`/`is_dir` `std::net` Partial \u2014 the older `tcp::` calls plus a `tcp::socket_*` family with listen, accept, timeouts and"},{"p":"conformance","t":"Bible vs V3","a":"section-7-standard-library","h":"Section 7 \u2014 Standard library","x":"`ByteBuffer` transfer `std::time` `time::now_ms()` and `time::monotonic_ns()` `std::math` 8 `math::` builtins; no `log`, `exp`, `atan2`, `gcd`, `simd` `std::random` **Absent** `std::process` Partial \u2014 adds `pid()` and `set_env()`; `args()` deliberately rejected in favour of `args_count()` / `arg(i)` `std::thread` **Absent** `std::bytes` Present as the builtin `ByteBuffer` type, with `status()` instead of `result<T,E>` `std::anime`, `std::narrative`, `std::test` **Absent** `std::mem`, `std::ffi` **Absent** `std::json`, `std::version`, `std::string`, `std::convert`, `std::algorithm` **Present** as FREAK source \u2014 not in the bible's shape, but real and working Two `std/algorithm.fk` helpers are"},{"p":"conformance","t":"Bible vs V3","a":"section-7-standard-library","h":"Section 7 \u2014 Standard library","x":"defective in the shipping compiler: `array_sort_word` segfaults, `array_unique` returns an empty array. Both were confirmed against the binary that verified this site."},{"p":"conformance","t":"Bible vs V3","a":"section-8-lexer","h":"Section 8 \u2014 Lexer","x":"The V3 lexer implements a **subset** of the bible's token table. Present: the 47 single-word keywords, 6 bool literals, 10 multi-word keywords, and the punctuation set. Absent: `variant`, `alias`, `dyn`, `never`, `const`, all concurrency keywords, `knowing`, `on my honor as`, `declare was`, `prob when`, `direct_order`, lifetime tokens `'a`, numeric suffixes (`42u`, `3.14f`, `42t`, `999b`), and `...` ellipsis. The bible's \u00a78.3 requires V4 keywords to be matched **case-sensitively** so that `Pilot` and `Some` stay usable as identifiers. **V3 does the opposite** \u2014 it lowercases before comparing, so `Pilot`, `Result` and `Max` are keywords and cannot be names. This is the single largest"},{"p":"conformance","t":"Bible vs V3","a":"section-8-lexer","h":"Section 8 \u2014 Lexer","x":"practical divergence for someone writing V3 code."},{"p":"conformance","t":"Bible vs V3","a":"sections-917","h":"Sections 9\u201317","x":"Section Bible V3 \u00a79 Parser AST nodes \u26a0\ufe0f Core nodes only. No `ErrorNode` / `IncompleteNode`, no node ids, no incremental parsing \u00a710 Type checker \u26a0\ufe0f Basic inference, arity and type checks, nominal member validation. None of the listed advanced rules \u00a711 Code generation \u26a0\ufe0f LLVM and C backends work. No mood, variant, `dyn`, `Shared<T>` or classified codegen. LLVM emits LineTablesOnly DWARF \u00a712 Build modes V4 Absent \u2014 only `--opt=0..3` and backend choice \u00a713 Compiler CLI \u26a0\ufe0f Most commands present; `-o`, `--voice=`, `--clearance=`, `--build-mode=`, and in-language `test` blocks absent \u00a714 Error voices V4 Absent \u2014 generic diagnostics, plus two signature borrow-checker lines \u00a715 Cheatsheet \u26a0\ufe0f"},{"p":"conformance","t":"Bible vs V3","a":"sections-917","h":"Sections 9\u201317","x":"Describes the full language; roughly a fifth applies to V3 \u00a716 FFI V4 Only bare `extern task f(...) -> T`. No blocks, conventions, `link=`, layout attributes, raw pointers or `trust me` \u00a717 Compiler internals / IDE V4 Absent. Parse errors abort; no tolerant AST, no panic infrastructure, no LSP"},{"p":"conformance","t":"Bible vs V3","a":"summary","h":"Summary","x":"Counting the bible's own feature inventory against compile evidence: Compiles in V3 Absent \u00a71 Syntax ~20 of 45 sub-features ~25 \u00a72 Advanced types 0 all \u00a73 Concurrency 0 all \u00a74 Borrow checker 3 Phase-1 rules, opt-in the rest \u00a75 Anime layer 4 of 20 16 \u00a76 Modules Hangar CLI only the module system \u00a77 Stdlib ~8 of 19 modules, reshaped 11 \u00a716 FFI 1 declaration form all else V3 is not standing still inside its version number. `List<T>`, `ByteBuffer`, `word_builder::*`, the `tcp::socket_*` family, `word.repeated(n)`, `time::monotonic_ns()`, `process::pid()` and `ui::set_clip` all landed after the first pass of this site was written, with `VERSION` unchanged at `0.14.1`. The compiler generation is"},{"p":"conformance","t":"Bible vs V3","a":"summary","h":"Summary","x":"frozen for *new semantics*; the builtin table clearly is not. Re-run the verification harness when you pull. V3 is a small, sharp, genuinely self-hosting language: scalars, words, shapes, methods, three loops, two branch forms, flat arrays and a C FFI. The bible describes where FREAK is going. This site describes where it is. For what to write instead of the missing features, see [Not in V3](not-in-v3.html)."},{"p":"not-in-v3","t":"Not in V3","a":"not-in-v3","h":"Not in V3","x":"> Every major bible feature the shipping compiler rejects, with the diagnostic you will see and what to write instead. Use this page when something from `freak-full-bible.md` will not compile. Each entry gives the exact error and a working V3 substitute. Some things on the bible's missing list have quietly arrived. V3 now has a typed [`List<T>`](arrays.html), a builtin [`ByteBuffer`](stdlib.html#bytebuffer), a [`word_builder::*`](words.html#building-words-incrementally) accumulator, and a `tcp::socket_*` family that can listen and accept. Check [Bible vs V3](conformance.html) before assuming something is absent."},{"p":"not-in-v3","t":"Not in V3","a":"optional-and-fallible-values","h":"Optional and fallible values","x":"error: expected '{', found '<' -- maybe<T>, result<T,E> error: unexpected 'check' error: unexpected '?' error: unexpected 'or else' There is no `maybe<T>`, `result<T,E>`, `some`, `nobody`, `ok`, `err`, `?` or `or else`. Signal absence and failure with sentinel values, the way the compiler's own source does. -- A word task returns \"\" for \"no value\". task find_name(id: int) -> word { if id == 1 { give back \"Takeru\" } give back \"\" } -- An int task returns -1 for \"not found\". task index_of_flag(flags: int, needle: word) -> int { pilot mut i: int = 0 repeat array_len(flags) times { if array_get(flags, i) == needle { give back i } i += 1 } give back 0 - 1 } task main() -> void { pilot name: word"},{"p":"not-in-v3","t":"Not in V3","a":"optional-and-fallible-values","h":"Optional and fallible values","x":"= find_name(2) if name.length() == 0 { say \"not found\" } else { say name } } For richer errors, return a shape with a status field: shape Outcome { ok: bool value: int message: word }"},{"p":"not-in-v3","t":"Not in V3","a":"collections","h":"Collections","x":"`List<T>` **does** exist now \u2014 typed, indexable, with `List::filled` \u2014 but it has no `push`, `pop`, `insert`, `remove`, `sort` or iterators, and it does not nest. See [Lists & arrays](arrays.html). Still absent: `Map<K,V>`, `Set<T>`, `Lineup<T>`, and the `{ \"k\": v }` map literal. For a map, keep two parallel collections: task map_get(keys: int, values: int, key: word) -> word { pilot at: int = array_find(keys, key) if at < 0 { give back \"\" } give back array_get(values, at) }"},{"p":"not-in-v3","t":"Not in V3","a":"iteration","h":"Iteration","x":"error: unexpected 'for each' \u2014 this token cannot start an expression Use an index and a counted loop: pilot mut i: int = 0 repeat array_len(items) times { say array_get(items, i) i += 1 } There is no `std::iter` either \u2014 no `.map`, `.filter`, `.fold`, `.any`, `.collect`. Write the loop."},{"p":"not-in-v3","t":"Not in V3","a":"closures","h":"Closures","x":"error: unexpected '|' \u2014 this token cannot start an expression No lambdas, no capture modes, no `Callable` / `MutCallable` / `OneShot`. Since tasks cannot be passed as values, the substitute is a named task plus a dispatch `when`: task apply(op: word, value: int) -> int { when op { \"double\" -> give back value * 2 \"square\" -> give back value * value _ -> give back value } give back value }"},{"p":"not-in-v3","t":"Not in V3","a":"generics","h":"Generics","x":"error: expected '(', found '<' -- task first<T>(...) No type parameters, no doctrine bounds, no monomorphisation. Write one task per concrete type. `List<T>` is the sole exception, and it is not a general mechanism \u2014 the annotation is a hard-coded special case in `parser_take_type`, so `List<T>` works while `List<List<int>>` and every other composed type do not."},{"p":"not-in-v3","t":"Not in V3","a":"doctrines-and-dynamic-dispatch","h":"Doctrines and dynamic dispatch","x":"error: unexpected 'doctrine' \u2014 this token cannot start an expression type error: operator '+' does not accept Vec2 and Vec2 `doctrine` declarations do not parse. `impl D for S { }` **does** parse, but `D` is never validated and the operator token is not rewritten \u2014 call the method by name. impl Add for Vec2 { task add(self, other: Vec2) -> Vec2 { ... } } pilot c: Vec2 = a.add(b) -- not a + b No `dyn Doctrine`, no vtables, no heterogeneous collections."},{"p":"not-in-v3","t":"Not in V3","a":"variants-and-aliases","h":"Variants and aliases","x":"type error: unknown binding 'variant' type error: unknown binding 'alias' Neither `variant` nor `alias` is even a keyword in V3's lexer, so they parse as identifiers and fail in the checker. Model a sum type as a shape with a tag: shape Contact { kind: word -- \"soldier\" | \"laser\" | \"fort\" id: int armor: int hive: word } task engage(c: Contact) -> void { when c.kind { \"soldier\" -> say \"engaging soldier {c.id}\" \"laser\" -> say \"smoke now\" \"fort\" -> say \"requesting strike on {c.hive}\" _ -> say \"unknown contact\" } } For an alias, just repeat the underlying type \u2014 a type annotation is one identifier (or `List<T>`) anyway, so there is little to abbreviate."},{"p":"not-in-v3","t":"Not in V3","a":"borrowing-lifetimes-and-shared-ownership","h":"Borrowing, lifetimes and shared ownership","x":"error: expected an identifier for parameter name, found 'lend' No `lend` / `lend mut` parameters, no `'a` lifetimes, no outlives bounds, no `Shared<T>` / `Weak<T>`, no `.clone()`, no `trust me`. Every parameter is by value, and shapes are handles into runtime storage \u2014 so a task that mutates a shape parameter's field affects the caller. Return a fresh shape when you want isolation. `--strict-borrow` enables a Phase-1 checker for immutability and single-owner moves; that is the whole of V3's ownership story."},{"p":"not-in-v3","t":"Not in V3","a":"concurrency","h":"Concurrency","x":"Nothing at all. No `xm3`, `sortie`, `debrief`, `formation`, `BriefingRoom`, `wingman`, `Comms`, `std::thread`, atomics or channels. None of those words are keywords. `||` lexes as one token and has no grammar rule. Shell out if you must: pilot code: int = process::exec(\"some-command --flag\")"},{"p":"not-in-v3","t":"Not in V3","a":"modules-and-visibility","h":"Modules and visibility","x":"error: unexpected 'launch' \u2014 this token cannot start an expression `use` lines are replaced with comments before parsing. There is no import system and no visibility system \u2014 one flat global namespace holds your file and every linked standard-library file. Consequences worth planning around: Name collisions with `std/` tasks are real. Prefix your tasks. `use std::math3d`, `use std::zip`, `use std::ui` and `use cockpit` still matter as **build triggers** \u2014 the text switches on linking that module. **Hangar packages cannot be consumed.** `hangar install` downloads into `hangar_modules/`, but the build never reads it. Concatenate the source instead: `cat lib/thing.fk src/app.fk > combined.fk"},{"p":"not-in-v3","t":"Not in V3","a":"modules-and-visibility","h":"Modules and visibility","x":"&& freak build combined.fk`. See [Hangar & packages](hangar.html)."},{"p":"not-in-v3","t":"Not in V3","a":"advanced-types","h":"Advanced types","x":"`power<N>`, `prob[lo..hi]`, `causality<T>` and `mood` do not lex. Model the same intent with ordinary values and runtime checks: task engage_fort(power_level: int) -> word { if power_level < 7000 { give back \"insufficient power\" } give back \"engaging\" }"},{"p":"not-in-v3","t":"Not in V3","a":"anime-layer","h":"Anime layer","x":"Rejected: `foreshadow`, `payoff`, `route`, `check route`, `only on`, `deus_ex_machina`, `isekai`, `bringing back`, `trust me`, `direct_order`, `sadly`, `for science,`, `knowing this will hurt,`, `training arc ... with growth`, `eventually if`, `prob[...] chance`, `prob_when`, `declare was`. Accepted: `training arc`, plain `eventually`, single bare `@annotation`, and the four anime operators. See [Anime layer](anime-layer.html)."},{"p":"not-in-v3","t":"Not in V3","a":"ffi","h":"FFI","x":"Only `extern task name(p: T) -> R`, one per line. No `extern [C] { }` blocks, calling conventions, `link=`, `@link_name`, variadics, `std::ffi` aliases, raw pointers, `@layout(C)`, `@repr(...)`, `@extern_callback` or `trust me`. Stick to `int`, `num`, `bool` and `void` across the boundary \u2014 `word` is an `i64` runtime handle, not a `char*`. See [extern & FFI](ffi.html)."},{"p":"not-in-v3","t":"Not in V3","a":"syntax-shorthands","h":"Syntax shorthands","x":"Bible form V3 `task f(x) => expr` Use a braced body with `give back` `done` closing a block Use `}` `**` exponent `math::pow(a, b)` or `std_pow(a, b)` `-5` as a literal `0 - 5` `42u`, `3.14f`, `42t`, `999b` No suffixes `[0; 100]` repeat-fill `List::filled(0, 100)` `[T; N]` fixed arrays `List<T>`, or a legacy array handle `(a, b)` tuples A shape Named call arguments Positional only Stacked `@a @b` annotations One per statement `@rival(meiya)` annotation arguments Bare `@name` only"},{"p":"not-in-v3","t":"Not in V3","a":"build-modes-voices-output-paths","h":"Build modes, voices, output paths","x":"`slice_of_life`, `mecha`, `shonen_jump`, `final_form` and `alternative` build modes do not exist; nor do `--voice=`, `--clearance=`, `--build-mode=` or `-o output_path`. The knobs are `--opt=0..3`, `--c` / `--llvm`, `--target=` and `--strict-borrow`."},{"p":"not-in-v3","t":"Not in V3","a":"tooling","h":"Tooling","x":"No language server, no incremental parsing, no tolerant AST, no autocomplete, no `test \"name\" { expect ... }` framework, no JIT. Parse errors abort the build. `freak test` is a shim around a Python regression runner in the source checkout."},{"p":"examples","t":"Verified examples","a":"verified-examples","h":"Verified examples","x":"> Every program on this site, compiled and executed by a real V3 binary. Each card below shows the exact source file under `examples/`, a badge for whether it compiled and ran, and the program's captured output. Reproduce the whole set with: python tools/verify.py --freak /path/to/freak python tools/build_docs.py The harness copies each example into a clean temporary directory, runs `freak build` with the default LLVM backend, executes the resulting binary, and records stdout verbatim. A failure is recorded with its diagnostics rather than hidden."},{"p":"examples","t":"Verified examples","a":"basics","h":"Basics","x":"-- The smallest FREAK program. -- `say` is always available; no import is needed. task main() -> void { say \"Hello from FREAK\" } -- Bindings are declared with `pilot`. -- `fixed pilot` marks an immutable binding. -- `pilot mut` opts a binding into reassignment. task main() -> void { pilot answer: int = 42 pilot ratio: num = 3.5 pilot callsign: word = \"Shirogane\" pilot ready: bool = true fixed pilot MAX_SORTIES: int = 12 pilot mut score: int = 0 score = score + 10 score += 5 say word_from_int(answer) say format_num(ratio) say callsign say word_from_bool(ready) say word_from_int(MAX_SORTIES) say word_from_int(score) } -- The type annotation is optional. V3 infers from the initializer. -- When"},{"p":"examples","t":"Verified examples","a":"basics","h":"Basics","x":"you do annotate, the annotation is a single identifier: -- `int`, `num`, `word`, `bool`, or the name of a shape. task main() -> void { pilot a = 7 -- int pilot b = 2.5 -- num pilot c = \"text\" -- word pilot d = false -- bool say word_from_int(a) say format_num(b) say c say word_from_bool(d) } -- V3 matches keywords CASE-INSENSITIVELY. `pilot`, `Pilot` and `PILOT` -- are all the same keyword token, so none of them can be used as a name. -- -- This bites most often on: Pilot, Result, Max, Route, Check, Move, -- Copy, Some, Ok, Err, Got, Use, In, As, Each, Times, Done, Max. -- -- Pick a synonym instead. These all compile: shape Aviator { tag: word } task outcome(value: int) -> word { if value >"},{"p":"examples","t":"Verified examples","a":"basics","h":"Basics","x":"0 { give back \"positive\" } give back \"non-positive\" } task main() -> void { pilot a: Aviator = Aviator { tag: \"V-1\" } pilot verdict: word = outcome(3) pilot upper_bound: int = 100 say a.tag say verdict say word_from_int(upper_bound) } -- A file does not need a `main`. Top-level statements run in order, -- and top-level bindings are visible to every task in the file. pilot squadron: word = \"Valkyries\" pilot strength: int = 4 task roster() -> word { give back \"{squadron} x{strength}\" } say \"booting\" say roster() strength = 5 say roster()"},{"p":"examples","t":"Verified examples","a":"tasks","h":"Tasks","x":"-- `task` declares a function. `give back` returns a value. -- A block-bodied task that returns a non-void type must use `give back` -- on every path; there is no implicit tail return. task add(a: int, b: int) -> int { give back a + b } task shout(message: word) -> void { say message.to_upper() } task classify(n: int) -> word { if n < 0 { give back \"negative\" } if n == 0 { give back \"zero\" } give back \"positive\" } task main() -> void { say word_from_int(add(20, 22)) shout(\"stand by\") say classify(0 - 3) say classify(0) say classify(9) } -- Tasks may call themselves, and may call tasks declared later in the -- file: every task is indexed before any body is checked. task factorial(n: int) ->"},{"p":"examples","t":"Verified examples","a":"tasks","h":"Tasks","x":"int { if n <= 1 { give back 1 } give back n * factorial(n - 1) } task fib(n: int) -> int { if n < 2 { give back n } give back fib(n - 1) + fib(n - 2) } task main() -> void { say word_from_int(factorial(6)) say word_from_int(fib(12)) } -- `|>` feeds the left value in as the FIRST argument of the call on the -- right, so `x |> f(y)` is exactly `f(x, y)`. task double(n: int) -> int { give back n * 2 } task offset(n: int, by: int) -> int { give back n + by } task main() -> void { say word_from_int(5 |> double()) say word_from_int(5 |> offset(3)) say word_from_int(5 |> double() |> offset(1)) }"},{"p":"examples","t":"Verified examples","a":"control-flow","h":"Control flow","x":"-- `if` / `else if` / `else`. Conditions take no parentheses. task describe(power: int) -> word { if power > 9000 { give back \"over nine thousand\" } else if power > 5000 { give back \"high\" } else if power > 1000 { give back \"moderate\" } else { give back \"low\" } } task main() -> void { say describe(9001) say describe(6000) say describe(2000) say describe(10) } -- `when` matches a subject against literal arms. `_` is the catch-all. -- Each arm takes one statement; use a block to run several. task main() -> void { pilot code: int = 2 when code { 1 -> say \"launch\" 2 -> { say \"hold\" say \"await orders\" } 3 -> say \"abort\" _ -> say \"unknown\" } pilot phase: word = \"sortie\" when phase { \"briefing\" ->"},{"p":"examples","t":"Verified examples","a":"control-flow","h":"Control flow","x":"say \"in the briefing room\" \"sortie\" -> say \"in the air\" _ -> say \"elsewhere\" } } -- V3 has three loop forms: a counted loop, a condition loop, -- and the bounded `training arc`. task main() -> void { -- repeat N times pilot mut total: int = 0 repeat 5 times { total += 2 } say word_from_int(total) -- repeat until CONDITION (tested before each pass) pilot mut countdown: int = 3 repeat until countdown == 0 { say word_from_int(countdown) countdown -= 1 } -- break and continue work in every loop form pilot mut seen: int = 0 repeat 10 times { seen += 1 if seen == 3 { continue } if seen == 6 { break } } say word_from_int(seen) } -- `training arc` is a loop with a mandatory session cap. -- It stops"},{"p":"examples","t":"Verified examples","a":"control-flow","h":"Control flow","x":"when the condition becomes true OR when the cap is hit, -- so it cannot spin forever. task main() -> void { pilot mut power: int = 0 training arc until power >= 100 max 8 sessions { power += 20 } say word_from_int(power) -- The cap wins when the condition is never reachable. pilot mut stuck: int = 0 training arc until stuck > 1000 max 4 sessions { stuck += 1 } say word_from_int(stuck) } -- A slightly larger program: integer square root by bisection, driven by -- a `training arc` so the loop is provably bounded. shape Bounds { lo: int hi: int } task isqrt(target: int) -> int { if target < 2 { give back target } pilot mut b: Bounds = Bounds { lo: 1, hi: target } training arc until b.lo >="},{"p":"examples","t":"Verified examples","a":"control-flow","h":"Control flow","x":"b.hi max 64 sessions { pilot mid: int = (b.lo + b.hi + 1) / 2 if mid * mid > target { b.hi = mid - 1 } else { b.lo = mid } } give back b.lo } task main() -> void { say word_from_int(isqrt(0)) say word_from_int(isqrt(1)) say word_from_int(isqrt(15)) say word_from_int(isqrt(16)) say word_from_int(isqrt(17)) say word_from_int(isqrt(1000000)) }"},{"p":"examples","t":"Verified examples","a":"operators","h":"Operators","x":"-- Arithmetic, comparison and logic. task main() -> void { pilot a: int = 17 pilot b: int = 5 say word_from_int(a + b) say word_from_int(a - b) say word_from_int(a * b) say word_from_int(a / b) say word_from_int(a % b) say word_from_bool(a > b) say word_from_bool(a == 17) say word_from_bool(a != b) say word_from_bool(a <= 17) say word_from_bool(a > b and b > 0) say word_from_bool(a < b or b > 0) say word_from_bool(not false) -- Unary minus applies to an expression. A negative *literal* -- is written as a subtraction: `0 - 5`. pilot neg: int = 0 - 5 say word_from_int(-neg) } -- The anime operators that V3 actually implements. -- The three unary forms are PREFIX and accept `int` only. task"},{"p":"examples","t":"Verified examples","a":"operators","h":"Operators","x":"main() -> void { pilot base: int = 6 say word_from_int(FINAL FORM base) -- base * base say word_from_int(PLUS ULTRA base) -- base * 2 say word_from_int(TSUNDERE base) -- 0 - base -- NAKAMA is infix, at the same precedence as + and -. say word_from_int(3 NAKAMA 4) }"},{"p":"examples","t":"Verified examples","a":"words","h":"Words","x":"-- `word` is FREAK's string type. These methods are compiler builtins, -- available with no import. task main() -> void { pilot s: word = \" Muv-Luv Alternative \" say s.trim() say word_from_int(s.length()) say s.trim().to_upper() say s.trim().to_lower() say word_from_bool(s.contains(\"Luv\")) say word_from_bool(s.trim().starts_with(\"Muv\")) say word_from_bool(s.trim().ends_with(\"ive\")) say s.trim().replace(\"Luv\", \"Love\") say s.trim().substring(0, 3) say s.trim().char_at(0) -- Concatenation with `+`, or with the builtin. say \"Muv\" + \"-\" + \"Luv\" say word_concat(\"XM\", \"3\") -- Numeric conversion in both directions. say word_from_int(\"108\".to_int()) say format_num(\"2.5\".to_num()) say"},{"p":"examples","t":"Verified examples","a":"words","h":"Words","x":"char_to_word(70) } -- `{path}` inside a double-quoted word interpolates a binding. -- A path is an identifier plus zero or more `.field` hops, and must -- resolve to word, int, num or bool. Anything that is not a valid path -- stays literal text. -- (`Aviator`, not `Pilot`: V3 keywords are case-insensitive, so `Pilot` -- is the `pilot` keyword and cannot name a shape.) shape Aviator { name: word score: int } impl Aviator { task summary(self) -> word { give back \"{self.name} scored {self.score}\" } } task main() -> void { pilot callsign: word = \"Valkyrie\" pilot sorties: int = 12 pilot rate: num = 0.75 pilot active: bool = true say \"callsign={callsign} sorties={sorties}\" say \"rate={rate}"},{"p":"examples","t":"Verified examples","a":"words","h":"Words","x":"active={active}\" pilot p: Aviator = Aviator { name: \"Meiya\", score: 98 } say \"shape: {p.name} / {p.score}\" say p.summary() -- Not a path, so it is printed literally: say \"literal {1 + 2} braces\" } -- `word_builder::*` accumulates a word without the quadratic cost of -- repeated `+` concatenation. The builder is an `int` handle. task main() -> void { pilot b: int = word_builder::new() word_builder::append(b, \"Mission: \") word_builder::append(b, \"Valkyries\") word_builder::append_char(b, 32) -- a space, by codepoint word_builder::append_int(b, 4) say word_from_int(word_builder::length(b)) -- `finish` consumes the builder and returns the accumulated word. say word_builder::finish(b) --"},{"p":"examples","t":"Verified examples","a":"words","h":"Words","x":"with_capacity pre-allocates; clear reuses; discard frees unused. pilot c: int = word_builder::with_capacity(64) word_builder::append(c, \"scratch\") word_builder::clear(c) word_builder::append(c, \"reused\") say word_builder::finish(c) }"},{"p":"examples","t":"Verified examples","a":"shapes","h":"Shapes","x":"-- `shape` declares a record type. Each field is `name: type`, -- where the type is a single identifier. shape Vector2 { x: int y: int } shape Squad { callsign: word members: int ready: bool } task main() -> void { pilot v: Vector2 = Vector2 { x: 3, y: 4 } say word_from_int(v.x) say word_from_int(v.y) -- Fields are assignable. v.x = 10 say word_from_int(v.x) pilot s: Squad = Squad { callsign: \"Valkyries\", members: 4, ready: true } say s.callsign say word_from_int(s.members) say word_from_bool(s.ready) } -- `impl Shape { ... }` attaches methods. -- A method whose first parameter is `self` is an instance method, -- called as `value.method(args)`. -- A method without `self` is an associated"},{"p":"examples","t":"Verified examples","a":"shapes","h":"Shapes","x":"method, -- called as `Shape::method(args)`. shape Rect { w: int h: int } impl Rect { task square(side: int) -> Rect { give back Rect { w: side, h: side } } task area(self) -> int { give back self.w * self.h } task perimeter(self) -> int { give back 2 * (self.w + self.h) } task scaled(self, factor: int) -> Rect { give back Rect { w: self.w * factor, h: self.h * factor } } } task main() -> void { pilot r: Rect = Rect { w: 3, h: 5 } say word_from_int(r.area()) say word_from_int(r.perimeter()) pilot big: Rect = r.scaled(3) say word_from_int(big.area()) pilot sq: Rect = Rect::square(4) say word_from_int(sq.area()) } -- Shapes may hold other shapes. Field access chains through them, -- including"},{"p":"examples","t":"Verified examples","a":"shapes","h":"Shapes","x":"inside string interpolation. shape Engine { thrust: int } shape Tsf { name: word engine: Engine } impl Tsf { task report(self) -> word { give back \"{self.name} thrust={self.engine.thrust}\" } } task main() -> void { pilot unit: Tsf = Tsf { name: \"Takemikazuchi\", engine: Engine { thrust: 880 } } say unit.name say word_from_int(unit.engine.thrust) say unit.report() } -- `impl Doctrine for Shape` registers an operator implementation. -- V3 does not accept a `doctrine` *declaration*, but it does accept -- this impl form for the built-in operator doctrines, and the method -- is callable by name. shape Vec2 { x: int y: int } impl Add for Vec2 { task add(self, other: Vec2) -> Vec2 { give back Vec2"},{"p":"examples","t":"Verified examples","a":"shapes","h":"Shapes","x":"{ x: self.x + other.x, y: self.y + other.y } } } impl Vec2 { task show(self) -> word { give back \"({self.x}, {self.y})\" } } task main() -> void { pilot a: Vec2 = Vec2 { x: 1, y: 2 } pilot b: Vec2 = Vec2 { x: 10, y: 20 } pilot c: Vec2 = a.add(b) say c.show() }"},{"p":"examples","t":"Verified examples","a":"lists-arrays","h":"Lists & arrays","x":"-- `List<T>` is V3's typed sequence. The element type must be a scalar -- (`int`, `num`, `word`, `bool`) or the name of a shape. Nesting is not -- allowed: there is no `List<List<int>>`. shape Contact { tag: word } -- A List may be a parameter and a return type. It may NOT be a shape -- field: that reports \"V3 owned shape fields do not yet support List -- values\". task count_all(items: List<word>) -> int { give back items.length() } task first_three() -> List<int> { give back [1, 2, 3] } task main() -> void { -- An array literal infers List<T> from its elements. pilot names: List<word> = [\"Takeru\", \"Meiya\", \"Sumika\"] say word_from_int(names.length()) say names[1] -- Indexed assignment"},{"p":"examples","t":"Verified examples","a":"lists-arrays","h":"Lists & arrays","x":"requires `pilot mut`. pilot mut scores: List<int> = [10, 20, 30] scores[0] = 99 say word_from_int(scores[0]) -- Every scalar element type works. pilot ratios: List<num> = [1.5, 2.5] say format_num(ratios[1]) pilot flags: List<bool> = [true, false] say word_from_bool(flags[0]) -- Shapes too, and field access chains off the element. pilot contacts: List<Contact> = [Contact { tag: \"BETA-1\" }] say contacts[0].tag -- List::filled(value, count) builds a pre-populated list. pilot zeros: List<int> = List::filled(0, 4) say word_from_int(zeros.length()) -- Passed to a task, and returned from one. say word_from_int(count_all(names)) say word_from_int(first_three()[2]) -- Iterate with an index; there"},{"p":"examples","t":"Verified examples","a":"lists-arrays","h":"Lists & arrays","x":"is no `for each`. pilot mut i: int = 0 repeat names.length() times { say names[i] i += 1 } } -- V3 arrays are runtime handles of `word` elements, addressed by an -- `int` handle. Store other types by converting them to `word`. task main() -> void { pilot xs: int = array_new() array_push(xs, \"alpha\") array_push(xs, \"bravo\") array_push(xs, \"charlie\") say word_from_int(array_len(xs)) say array_get(xs, 0) say array_get(xs, 2) array_set(xs, 1, \"BRAVO\") say array_get(xs, 1) -- Iterate with an index and a counted loop. pilot mut i: int = 0 repeat array_len(xs) times { say array_get(xs, i) i += 1 } array_release(xs) } -- An array literal infers `List<T>` from its elements. Numbers stay -- numbers"},{"p":"examples","t":"Verified examples","a":"lists-arrays","h":"Lists & arrays","x":"-- there is no need to convert them to `word` any more. -- -- The older untyped `array_*` handle still exists alongside it; see -- arrays.html for when each one applies. task main() -> void { pilot names: List<word> = [\"Takeru\", \"Meiya\", \"Sumika\"] say word_from_int(names.length()) say names[1] pilot numbers: List<int> = [3, 1, 2] say word_from_int(numbers.length()) say word_from_int(numbers[0]) -- The array_* builtins accept a List<word> as well as a raw handle. say word_from_int(array_len(names)) say array_get(names, 2) -- std/algorithm.fk tasks are declared with `handle: int`, so they take -- only the legacy handle. Build one with array_new() to use them. pilot h: int = array_new()"},{"p":"examples","t":"Verified examples","a":"lists-arrays","h":"Lists & arrays","x":"array_push(h, word_from_int(3)) array_push(h, word_from_int(1)) array_push(h, word_from_int(2)) array_sort_int(h) say array_join(h, \",\") say word_from_int(array_sum_int(h)) say word_from_int(array_max_int(h)) say word_from_int(array_binary_search_int(h, 3)) array_reverse(h) say array_join(h, \"|\") say word_from_bool(array_contains(h, \"2\")) array_release(h) }"},{"p":"examples","t":"Verified examples","a":"standard-library","h":"Standard library","x":"-- std/string.fk ships as plain FREAK source and is always linked in by -- `freak build` / `freak run`. Its tasks are global; there is no import. task main() -> void { say string_repeat(\"ha\", 3) say string_reverse(\"FREAK\") say word_from_int(string_count(\"banana\", \"an\")) say string_pad_left(\"7\", 4, \"0\") say string_pad_right(\"7\", 4, \".\") say word_from_int(string_index_of(\"alternative\", \"native\")) say word_from_bool(is_digit(\"5\")) say word_from_bool(is_alpha(\"x\")) say word_from_bool(is_whitespace(\" \")) } -- Two numeric surfaces exist side by side: -- `math::*` are compiler builtins over `num` (floating point). -- `std_*` come from std/math.fk and work on `int`. task main() -> void { --"},{"p":"examples","t":"Verified examples","a":"standard-library","h":"Standard library","x":"builtins, num say format_num(math::sqrt(144.0)) say format_num(math::pow(2.0, 10.0)) say format_num(math::floor(3.9)) say format_num(math::ceil(3.1)) say format_num(math::sin(0.0)) say format_num(math::cos(0.0)) -- std/math.fk, int say word_from_int(std_abs(0 - 9)) say word_from_int(std_max(3, 8)) say word_from_int(std_min(3, 8)) say word_from_int(std_clamp(42, 0, 10)) say word_from_int(std_pow(2, 8)) say word_from_int(std_gcd(84, 30)) say word_from_int(std_lcm(4, 6)) say word_from_int(std_factorial(6)) say word_from_int(std_fibonacci(12)) say word_from_bool(std_is_even(4)) say word_from_int(std_sign(0 - 3)) } -- std/convert.fk: base conversion and safe parsing. task main() -> void { say"},{"p":"examples","t":"Verified examples","a":"standard-library","h":"Standard library","x":"int_to_hex(255) say int_to_bin(10) say int_to_oct(64) say word_from_int(char_to_digit(\"7\")) say word_from_int(word_to_int_safe(\"not a number\")) say word_from_int(word_to_int_safe(\"123\")) say bool_to_word(true) } -- std/json.fk is a pure-FREAK parser. Values are `int` handles. task main() -> void { json_init() pilot doc: int = json_parse(\"{\\\"unit\\\":\\\"Valkyries\\\",\\\"members\\\":4,\\\"ready\\\":true}\") say json_get_type(doc) say word_from_int(json_obj_len(doc)) say word_from_bool(json_obj_has(doc, \"unit\")) say json_get_str(json_obj_get(doc, \"unit\")) say word_from_int(json_get_int(json_obj_get(doc, \"members\"))) say word_from_bool(json_get_bool(json_obj_get(doc, \"ready\"))) pilot list: int ="},{"p":"examples","t":"Verified examples","a":"standard-library","h":"Standard library","x":"json_parse(\"[10, 20, 30]\") say word_from_int(json_arr_len(list)) say word_from_int(json_get_int(json_arr_get(list, 1))) } -- std/version.fk implements semver parsing and constraint matching. task main() -> void { pilot parsed: word = ver_parse(\"2.14.1-rc.1+build7\") say word_from_int(ver_major(parsed)) say word_from_int(ver_minor(parsed)) say word_from_int(ver_patch(parsed)) say ver_pre(parsed) say ver_build(parsed) say ver_to_string(parsed) say word_from_bool(ver_lt(\"1.2.3\", \"1.10.0\")) say word_from_bool(ver_gt(\"2.0.0\", \"1.9.9\")) say word_from_bool(ver_eq(\"1.0.0\", \"1.0.0\")) say word_from_bool(ver_satisfies(\"1.4.2\", \"^1.4\")) say word_from_bool(ver_satisfies(\"2.0.0\", \"^1.4\")) }"},{"p":"examples","t":"Verified examples","a":"console","h":"Console","x":"-- Coloured console output. There is no colour library: you build the -- ANSI escape sequences yourself as `word` values and concatenate them. -- -- `\\x1b` is the ESC byte. The V3 lexer preserves `\\xNN` escapes all the -- way through to the backend, so they reach the terminal intact. pilot C_RESET = \"\\x1b[0m\" pilot C_BOLD = \"\\x1b[1m\" pilot C_DIM = \"\\x1b[2m\" pilot C_RED = \"\\x1b[31m\" pilot C_GREEN = \"\\x1b[32m\" pilot C_CYAN = \"\\x1b[36m\" pilot C_BRED = \"\\x1b[1;31m\" -- 24-bit truecolour: ESC [ 38;2;R;G;B m pilot C_PINK = \"\\x1b[38;2;255;100;200m\" task rgb(r: int, g: int, b: int) -> word { give back \"\\x1b[38;2;\" + word_from_int(r) + \";\" + word_from_int(g) + \";\" + word_from_int(b) + \"m\" } task"},{"p":"examples","t":"Verified examples","a":"console","h":"Console","x":"main() -> void { say C_BRED + \"error\" + C_RESET + \": engine failure\" say C_GREEN + \"ok\" + C_RESET + \": all systems nominal\" say C_DIM + \"hint: check the coolant line\" + C_RESET say C_BOLD + C_CYAN + \"XM3\" + C_RESET + \" reflex link online\" say C_PINK + \"freak\" + C_RESET + \" 0.14.1\" say rgb(255, 180, 0) + \"amber warning\" + C_RESET } -- A tour of the SGR (Select Graphic Rendition) code space, to confirm -- every family survives the V3 lexer, both backends and the runtime. -- -- Form: ESC [ params m where ESC is \\x1b pilot RESET = \"\\x1b[0m\" task sgr(code: word) -> word { give back \"\\x1b[\" + code + \"m\" } task swatch(label: word, code: word) -> void { say sgr(code) + label + RESET } task main()"},{"p":"examples","t":"Verified examples","a":"console","h":"Console","x":"-> void { -- Attributes swatch(\"bold\", \"1\") swatch(\"dim\", \"2\") swatch(\"italic\", \"3\") swatch(\"underline\", \"4\") swatch(\"blink\", \"5\") swatch(\"reverse\", \"7\") swatch(\"hidden\", \"8\") swatch(\"strike\", \"9\") -- Standard foreground 30-37 swatch(\"black\", \"30\") swatch(\"red\", \"31\") swatch(\"green\", \"32\") swatch(\"yellow\", \"33\") swatch(\"blue\", \"34\") swatch(\"magenta\", \"35\") swatch(\"cyan\", \"36\") swatch(\"white\", \"37\") -- Bright foreground 90-97 swatch(\"bright red\", \"91\") swatch(\"bright green\", \"92\") swatch(\"bright cyan\", \"96\") -- Background 40-47 and bright 100-107 swatch(\"on red\", \"41\") swatch(\"on blue\", \"44\") swatch(\"on bright yellow\", \"103\") -- Combined parameters in one sequence swatch(\"bold + underline +"},{"p":"examples","t":"Verified examples","a":"console","h":"Console","x":"red\", \"1;4;31\") swatch(\"white on magenta\", \"37;45\") -- 256-colour: 38;5;N foreground, 48;5;N background swatch(\"256 fg 208\", \"38;5;208\") swatch(\"256 bg 22\", \"48;5;22\") -- 24-bit truecolour: 38;2;R;G;B and 48;2;R;G;B swatch(\"rgb fg\", \"38;2;255;100;200\") swatch(\"rgb bg\", \"48;2;20;20;90\") -- Selective reset: 22 clears bold/dim, 24 clears underline, -- 39 resets foreground, 49 resets background. say sgr(\"1\") + \"bold \" + sgr(\"22\") + \"normal again\" + RESET say sgr(\"31\") + \"red \" + sgr(\"39\") + \"default again\" + RESET }"},{"p":"examples","t":"Verified examples","a":"system","h":"System","x":"-- `fs::*` are compiler builtins. Paths are plain `word` values. task main() -> void { fs::write(\"sortie.log\", \"launch\\n\") fs::append(\"sortie.log\", \"engage\\n\") if fs::exists(\"sortie.log\") { say fs::read(\"sortie.log\").trim() } fs::make_dir(\"hangar_out\") say word_from_bool(fs::exists(\"hangar_out\")) -- `fs::delete` is file-only in V3, and reports true when the file is -- gone -- whether this call removed it or it was already absent. say word_from_bool(fs::delete(\"sortie.log\")) say word_from_bool(fs::exists(\"sortie.log\")) } -- `ByteBuffer` is a real builtin type with methods, unlike the `int` -- handles used elsewhere. It is a seekable binary read/write cursor. task main() -> void { pilot buf:"},{"p":"examples","t":"Verified examples","a":"system","h":"System","x":"ByteBuffer = ByteBuffer::new() buf.write_word(\"hi\") buf.write_int(7) buf.write_byte(255) say word_from_int(buf.length()) say word_from_int(buf.position()) -- Rewind and read the same values back. buf.seek(0) say buf.read_word(2) say word_from_int(buf.read_int()) say word_from_int(buf.read_byte()) say word_from_int(buf.remaining()) -- status() reports the last failure; 0 means clean. say word_from_int(buf.status()) buf.release() } -- Process and clock builtins. -- Note: `process::args()` is deliberately rejected by V3. Use the -- indexed pair `process::args_count()` / `process::arg(i)` instead. task main() -> void { pilot argc: int = process::args_count() say word_from_int(argc) pilot mut i:"},{"p":"examples","t":"Verified examples","a":"system","h":"System","x":"int = 0 repeat argc times { say \"arg {i}: \" + process::arg(i) i += 1 } pilot started: int = time::now_ms() pilot mut spin: int = 0 repeat 1000 times { spin += 1 } pilot elapsed: int = time::now_ms() - started say word_from_bool(elapsed >= 0) say word_from_bool(process::env(\"PATH\").length() > 0) } -- V3's `extern` is a single flat declaration -- no `extern [C] { ... }` -- block, no calling convention, no `link=`. Parameter and return types -- are single identifiers. -- -- The declared symbol must exist at link time and must not start with -- the reserved `__freak_` prefix. extern task abs(v: int) -> int task main() -> void { say word_from_int(abs(0 - 41)) say word_from_int(abs(41)) }"},{"p":"examples","t":"Verified examples","a":"anime-layer","h":"Anime layer","x":"-- `eventually { ... }` marks a cleanup block. -- -- IMPORTANT V3 caveat: V3 emits the block INLINE at the point it is -- written. It is not deferred to the end of scope and it does not run -- on `give back`, `break` or `panic`. Place it where you want it to run. task main() -> void { say \"engaging\" eventually { say \"cleanup\" } say \"disengaged\" } -- V3 parses a bare `@name` annotation and then ignores it. -- It carries no semantics, and it must NOT take arguments: -- `@rival(meiya)` does not parse as an annotation in V3. @protagonist task hero() -> word { give back \"Shirogane Takeru\" } @deprecated task legacy() -> word { give back \"old path\" } @season_finale task main() -> void { say hero()"},{"p":"examples","t":"Verified examples","a":"anime-layer","h":"Anime layer","x":"say legacy() }"},{"p":"examples","t":"Verified examples","a":"tutorials","h":"Tutorials","x":"-- Tutorial 1, finished program. -- -- A greeter that takes a name, decides how formal to be, and reports. -- Everything here is core V3: bindings, a shape, a method, a task, -- interpolation, a conditional and a counted loop. shape Aviator { name: word sorties: int } impl Aviator { -- An associated method: called as Aviator::recruit(...). task recruit(name: word) -> Aviator { give back Aviator { name: name, sorties: 0 } } -- An instance method: `self` is the receiver. task rank(self) -> word { if self.sorties >= 50 { give back \"veteran\" } if self.sorties >= 10 { give back \"regular\" } give back \"rookie\" } task greeting(self) -> word { -- Interpolation substitutes PATHS only, never calls."},{"p":"examples","t":"Verified examples","a":"tutorials","h":"Tutorials","x":"Writing -- \"{self.rank()}\" would print those characters literally, so the -- call result goes into a binding first. pilot rank: word = self.rank() give back \"{self.name} - {self.sorties} sorties, rank {rank}\" } } task main() -> void { pilot mut takeru: Aviator = Aviator::recruit(\"Shirogane Takeru\") -- Fly a few missions. repeat 12 times { takeru.sorties += 1 } say takeru.greeting() pilot rank: word = takeru.rank() say \"logged as: {rank}\" if takeru.sorties > 10 { say \"cleared for the next operation\" } else { say \"needs more flight hours\" } } -- Tutorial 2, finished program. -- -- A small command-line tool: read a file, count its lines, words and -- characters, then print a coloured report."},{"p":"examples","t":"Verified examples","a":"tutorials","h":"Tutorials","x":"Falls back to a sample file -- it writes itself when given no argument, so it always has input. pilot mut C_RESET = \"\\x1b[0m\" pilot mut C_BOLD = \"\\x1b[1m\" pilot mut C_DIM = \"\\x1b[2m\" pilot mut C_RED = \"\\x1b[1;31m\" pilot mut C_GREEN = \"\\x1b[32m\" pilot mut C_CYAN = \"\\x1b[36m\" -- Honour the NO_COLOR convention. Blanking the constants keeps every -- call site working unchanged. task configure_output() -> void { if process::env(\"NO_COLOR\") != \"\" { C_RESET = \"\" C_BOLD = \"\" C_DIM = \"\" C_RED = \"\" C_GREEN = \"\" C_CYAN = \"\" } } task sample_path() -> word { pilot path: word = \"tut_sample.txt\" fs::write(path, \"the beta do not negotiate\\nhumanity answers with steel\\nand with pilots\\n\") give back path }"},{"p":"examples","t":"Verified examples","a":"tutorials","h":"Tutorials","x":"-- Which file are we working on? Argument 0 is the executable itself. task target_path() -> word { if process::args_count() > 1 { give back process::arg(1) } give back sample_path() } task count_words(text: word) -> int { pilot mut total: int = 0 pilot mut in_word: bool = false pilot mut i: int = 0 repeat text.length() times { pilot c: word = text.char_at(i) pilot blank: bool = c == \" \" or c == \"\\n\" or c == \"\\t\" or c == \"\\r\" if blank { in_word = false } else { if not in_word { total += 1 } in_word = true } i += 1 } give back total } task count_lines(text: word) -> int { if text.length() == 0 { give back 0 } pilot mut total: int = 1 pilot mut i: int = 0 repeat text.length() times { if"},{"p":"examples","t":"Verified examples","a":"tutorials","h":"Tutorials","x":"text.char_at(i) == \"\\n\" { total += 1 } i += 1 } -- A trailing newline does not start a new line. if text.char_at(text.length() - 1) == \"\\n\" { total -= 1 } give back total } task row(label: word, value: int) -> void { say \" \" + C_DIM + label + C_RESET + \" \" + C_BOLD + word_from_int(value) + C_RESET } task main() -> void { configure_output() pilot path: word = target_path() if not fs::exists(path) { say C_RED + \"error\" + C_RESET + \": no such file: \" + path process::exit(1) } pilot text: word = fs::read(path) say C_CYAN + C_BOLD + \"report\" + C_RESET + C_DIM + \" for \" + path + C_RESET row(\"lines \", count_lines(text)) row(\"words \", count_words(text)) row(\"characters\", text.length()) if"},{"p":"examples","t":"Verified examples","a":"tutorials","h":"Tutorials","x":"count_words(text) > 5 { say \" \" + C_GREEN + \"ok\" + C_RESET + \" enough material to work with\" } fs::delete(\"tut_sample.txt\") } -- Tutorial 3, finished program. -- -- Model a squadron with shapes, keep the members in a List, and compute -- a summary. Shows the two collection surfaces working side by side and -- the one restriction that catches people: a shape cannot hold a List. shape Airframe { model: word thrust: int } shape Member { callsign: word sorties: int frame: Airframe } impl Member { task enlist(callsign: word, model: word, thrust: int) -> Member { give back Member { callsign: callsign, sorties: 0, frame: Airframe { model: model, thrust: thrust } } } task veteran(self) -> bool {"},{"p":"examples","t":"Verified examples","a":"tutorials","h":"Tutorials","x":"give back self.sorties >= 20 } task line(self) -> word { give back \"{self.callsign} {self.frame.model} thrust {self.frame.thrust} sorties {self.sorties}\" } } -- A List may be a parameter and a return type. It may NOT be a field on -- a shape: that reports -- \"V3 owned shape fields do not yet support List values\" task total_sorties(counts: List<int>) -> int { pilot mut sum: int = 0 pilot mut i: int = 0 repeat counts.length() times { sum += counts[i] i += 1 } give back sum } task main() -> void { pilot mut ichi: Member = Member::enlist(\"Valkyrie-1\", \"Takemikazuchi\", 880) pilot mut ni: Member = Member::enlist(\"Valkyrie-2\", \"Shiranui\", 640) pilot mut san: Member = Member::enlist(\"Valkyrie-3\","},{"p":"examples","t":"Verified examples","a":"tutorials","h":"Tutorials","x":"\"Gekishin\", 520) repeat 24 times { ichi.sorties += 1 } repeat 8 times { ni.sorties += 1 } repeat 31 times { san.sorties += 1 } say ichi.line() say ni.line() say san.line() -- A typed List holds the numbers we want to aggregate. pilot counts: List<int> = [ichi.sorties, ni.sorties, san.sorties] say \"total sorties: \" + word_from_int(total_sorties(counts)) say \"squadron size: \" + word_from_int(counts.length()) -- Names in a List<word>, indexed like any other list. pilot names: List<word> = [ichi.callsign, ni.callsign, san.callsign] pilot mut i: int = 0 repeat names.length() times { say \" member \" + word_from_int(i) + \": \" + names[i] i += 1 } -- Counting veterans needs the shapes themselves, so"},{"p":"examples","t":"Verified examples","a":"tutorials","h":"Tutorials","x":"loop over them -- directly rather than trying to put Members in a List. pilot mut veterans: int = 0 if ichi.veteran() { veterans += 1 } if ni.veteran() { veterans += 1 } if san.veteran() { veterans += 1 } say \"veterans: \" + word_from_int(veterans) -- The legacy handle is still the right tool when you need the -- std/algorithm.fk helpers, which are declared with `handle: int`. pilot ranking: int = array_new() array_push(ranking, word_from_int(ichi.sorties)) array_push(ranking, word_from_int(ni.sorties)) array_push(ranking, word_from_int(san.sorties)) array_sort_int(ranking) say \"sorted sorties: \" + array_join(ranking, \", \") say \"busiest pilot flew \" + word_from_int(array_max_int(ranking))"},{"p":"examples","t":"Verified examples","a":"tutorials","h":"Tutorials","x":"array_release(ranking) }"},{"p":"examples","t":"Verified examples","a":"project-structure","h":"Project structure","x":"-- Reusing code across files in V3. -- -- The compiler takes exactly one source file, and Hangar packages are -- never linked into it, so a \"library\" is just source you concatenate -- ahead of your program: -- -- cat lib/text.fk src/app.fk > build/combined.fk -- freak build build/combined.fk -- -- This file is what that produces. Everything lands in one flat global -- namespace, so a library must prefix its names to avoid collisions -- -- with your code, and with the std/ tasks the build already links. -- =================================================================== -- lib/text.fk -- the \"package\" -- =================================================================== shape TextStats {"},{"p":"examples","t":"Verified examples","a":"project-structure","h":"Project structure","x":"words: int longest: word } task text_is_blank(c: word) -> bool { give back c == \" \" or c == \"\\n\" or c == \"\\t\" or c == \"\\r\" } task text_count_words(source: word) -> int { pilot mut total: int = 0 pilot mut in_word: bool = false pilot mut i: int = 0 repeat source.length() times { if text_is_blank(source.char_at(i)) { in_word = false } else { if not in_word { total += 1 } in_word = true } i += 1 } give back total } task text_longest_word(source: word) -> word { pilot mut best: word = \"\" pilot mut current: word = \"\" pilot mut i: int = 0 repeat source.length() times { pilot c: word = source.char_at(i) if text_is_blank(c) { if current.length() > best.length() { best = current } current = \"\" }"},{"p":"examples","t":"Verified examples","a":"project-structure","h":"Project structure","x":"else { current = current + c } i += 1 } if current.length() > best.length() { best = current } give back best } task text_analyse(source: word) -> TextStats { give back TextStats { words: text_count_words(source), longest: text_longest_word(source) } } -- =================================================================== -- src/app.fk -- your program -- =================================================================== task main() -> void { pilot report: TextStats = text_analyse(\"the beta will not negotiate with humanity\") say word_from_int(report.words) say report.longest -- `use` lines are stripped before parsing, so leaving one in a -- concatenated build is harmless -- and equally, it"},{"p":"examples","t":"Verified examples","a":"project-structure","h":"Project structure","x":"imports nothing. }"},{"p":"examples","t":"Verified examples","a":"complete-programs","h":"Complete programs","x":"-- FizzBuzz, written the way V3 wants it: a counted loop, a mutable -- counter, and `when` over a computed tag. task tag(n: int) -> word { if n % 15 == 0 { give back \"FizzBuzz\" } if n % 3 == 0 { give back \"Fizz\" } if n % 5 == 0 { give back \"Buzz\" } give back word_from_int(n) } task main() -> void { pilot mut n: int = 1 repeat 15 times { say tag(n) n += 1 } } -- A complete small program: split a sentence into words, count them, -- and report the longest one. Uses shapes, arrays, loops and std tasks. shape Report { total: int longest: word } impl Report { task show(self) -> word { give back \"total={self.total} longest={self.longest}\" } } task split_words(text: word, out: int) -> void { pilot"},{"p":"examples","t":"Verified examples","a":"complete-programs","h":"Complete programs","x":"mut current: word = \"\" pilot mut i: int = 0 repeat text.length() times { pilot c: word = text.char_at(i) if c == \" \" { if current.length() > 0 { array_push(out, current) current = \"\" } } else { current = current + c } i += 1 } if current.length() > 0 { array_push(out, current) } } task analyse(text: word) -> Report { pilot words: int = array_new() split_words(text, words) pilot mut longest: word = \"\" pilot mut i: int = 0 repeat array_len(words) times { pilot w: word = array_get(words, i) if w.length() > longest.length() { longest = w } i += 1 } -- `result` is a reserved word in V3, so the binding is `summary`. pilot summary: Report = Report { total: array_len(words), longest: longest }"},{"p":"examples","t":"Verified examples","a":"complete-programs","h":"Complete programs","x":"array_release(words) give back summary } task main() -> void { pilot r: Report = analyse(\"the beta will not negotiate with humanity\") say r.show() say word_from_int(r.total) say r.longest.to_upper() }"}];