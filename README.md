# Freak-docs

Documentation for the **FREAK V3 "Maverick"** compiler — the shipping,
self-hosted generation in
[`src/compiler/v3/`](https://github.com/FREAK-lang-dev/Freak-lang/tree/main/src/compiler/v3).

Written against what the compiler **actually accepts**, not what the
specification promises. Every FREAK snippet on the site is a real file in
[`examples/`](examples/), compiled by a real V3 binary and — where it produces
output — executed, with stdout captured verbatim into the page.

**43 / 43 examples compile and run** under `freak 0.14.1 (Maverick)`, built
from source with a verified self-host fixed point.

---

## What's here

| | |
|---|---|
| **3 tutorials** | First program · A command-line tool · Modelling data |
| **9 language pages** | Bindings, tasks, control flow, operators, words, shapes, lists, colour, FFI |
| **5 reference pages** | Standard library, grammar, bible-vs-V3 conformance, what's missing, the example gallery |
| **Full-text search** | Client-side, no server, `/` or `Ctrl`/`Cmd`+`K` |

The two pages worth reading even if you know FREAK:

- **[Bible vs V3](content/conformance.md)** — a feature-by-feature comparison
  against `freak-full-bible.md`, with the actual compiler diagnostics. Roughly
  a fifth of the specification compiles today.
- **[Not in V3](content/not-in-v3.md)** — a working substitute for each missing
  feature.

## Repository layout

```text
content/           documentation source (a small Markdown subset)
examples/          .fk programs; every code block on the site comes from here
  verified.json    compile/run results, regenerated by tools/verify.py
tools/
  verify.py        builds and runs every example with a real V3 compiler
  build_docs.py    renders content/ + verified.json into site/
  search_smoke.js  headless check that the search index answers real queries
site/              the generated website — this is what Vercel serves
vercel.json        static deployment config
```

`site/` is committed deliberately: the build needs a working FREAK compiler,
which Vercel has no way to produce, so the generated HTML is the deployment
artefact.

## Local preview

No server needed — the site is self-contained, with no CDN and no `fetch`:

```sh
open site/index.html          # macOS
start site\index.html         # Windows
xdg-open site/index.html      # Linux
```

Or serve it:

```sh
python -m http.server 8000 --directory site
```

## Deploying

Vercel, as a static project with no build step:

| Setting | Value |
|---|---|
| Framework preset | Other |
| Build command | *(leave empty)* |
| Output directory | `site` |
| Install command | *(leave empty)* |

`vercel.json` already sets `outputDirectory`, so importing the repo should need
no manual configuration.

## Regenerating

You need a V3 `freak` binary built from the **same checkout** you are
documenting — see the caveat below.

```sh
# in a Freak-lang checkout
python -u tests/v3_fixed_point.py

# back here
python tools/verify.py --freak /path/to/freak --jobs 6
python tools/build_docs.py
node tools/search_smoke.js
```

`verify.py` copies each example into a clean temporary directory, runs
`freak build` with the default LLVM backend, executes the binary, and writes
`examples/verified.json`. **It exits non-zero if any example fails to
compile** — that is the guard that keeps the docs honest.

`build_docs.py` renders the site and rebuilds the search index. Coloured
program output is preserved by the harness and re-rendered as HTML: the
generator implements a stateful SGR renderer (attributes, 16 colours,
backgrounds, 256-colour, truecolour, selective resets) and drops non-SGR
control sequences.

Adding a page means adding `content/<slug>.md` and an entry in the `NAV` table
at the top of `build_docs.py`.

## Two compiler defects documented here

Found while writing these docs, reproduced against the current `main`, and
documented rather than worked around silently:

- **`array_sort_word()` segfaults.** It routes through
  `extern task freak_word_compare(a: word, b: word) -> int`, and that
  declaration does not bridge the LLVM backend's `word` handle representation.
- **`array_unique()` returns an empty array.**

## Caveat: V3 moves inside its version number

The compiler *generation* is frozen for new semantics, but the builtin table is
not. Between two passes over these docs — with `VERSION` unchanged at `0.14.1`
— V3 gained a typed `List<T>`, a builtin `ByteBuffer`, the `word_builder::*`
accumulator, a `tcp::socket_*` family that can listen and accept,
`word.repeated(n)`, `time::monotonic_ns()`, `process::pid()`,
`process::set_env()` and `ui::set_clip`. An older compiler can no longer
compile the current `std/`.

So: rebuild the compiler from the checkout you are documenting, and re-run
`tools/verify.py` after every pull.

## Contributing

1. Edit `content/*.md`, or add an example under `examples/`.
2. Run `tools/verify.py` — it must pass before the docs can claim anything.
3. Run `tools/build_docs.py` and commit the regenerated `site/`.
4. Run `tools/search_smoke.js`.

Prose rule: if a page states that something works, there should be a verified
example backing it. If something is broken, say so and show the diagnostic.

## Licence

Documentation and examples follow the licence of the
[FREAK language repository](https://github.com/FREAK-lang-dev/Freak-lang).
